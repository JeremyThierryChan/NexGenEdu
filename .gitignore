# dependencies
node_modules/
.pnp
.pnp.js

# next.js
.next/
out/
build/
next-env.d.ts

# production
dist/

# misc
.DS_Store
**/.DS_Store
*.pem
.vercel

# next.js static export output
out/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env
.env
.env*.local

# typescript
*.tsbuildinfo

# local tooling cache (this machine keeps npm's global cache read-only)
.npm-cache/

# 同步时间戳（每次同步都会变，仅用于让打包器重新编译）
data/site/.sync-stamp.ts

# 构站时从后端取的那份数据（构建期产物，不进版本库）
data/site/.backend-snapshot.ts

# 后端数据库文件与备份（开发阶段；数据不进 git）
server/data/
server/backups/

# 本机环境变量（不提交）
.env.local
.env*.local

# 机构自己的思维导图（用户的文件，不提交；早先一次 git add -A 误入库）
*.xmind
