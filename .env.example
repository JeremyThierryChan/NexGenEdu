# 后端地址（本机使用）
#
# 设置这一项 = 后台的数据进后端（SQLite）；不设置 = 数据存在浏览器里（线上演示用）。
# 见 docs/后端开发方案.md §5.4。
#
# 本机使用步骤：
#   1. 复制本文件为 .env.local（Next 会自动读取，且不会被提交）
#   2. 终端 A：npm run server      （后端，端口 4000）
#   3. 终端 B：npm run dev         （前端，端口 3000）
#   4. 打开 http://localhost:3000/admin
NEXT_PUBLIC_API_BASE=http://localhost:4000
