# 后端地址（本机使用）
#
# 设置这一项 = 后台走本机后端，数据进 SQLite（这是**唯一**真正能用的形态）。
# 不设置 = 后台不去连后端：登录页会说明"没有后端可连"，进入 /admin 显示"后台需要本机后端"。
#   （它不是"数据改存浏览器"——那条路已经退休了，见 docs/后端开发方案.md §5.4）
#
# 本机使用步骤：
#   1. 复制本文件为 .env.local（Next 会自动读取，且不会被提交）
#   2. 终端 A：npm run server      （后端，默认 127.0.0.1:4000；首次启动会打印登录口令）
#   3. 终端 B：npm run dev         （前端，端口 3000）
#   4. 打开 http://localhost:3000/admin，用终端 A 打印的口令登录
#   5. 首次使用：到「课程库」页点一次「从网站同步课程」（否则排课/报课的下拉里没有科目）
NEXT_PUBLIC_API_BASE=http://localhost:4000
