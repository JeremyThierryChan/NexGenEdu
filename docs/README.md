# 文档索引

这个目录放**系统文档**（怎么写、怎么用、怎么接手）。内容源在 `data/site/`，代码结构与「谁负责哪一块」见 [技术架构](./技术架构.md)。

**当前形态一句话**：宣传站是纯静态站（部署在 GitHub Pages）；教务后台已经接上**本机真实后端**
（Node + SQLite + 服务端会话认证 + 每天自动备份），只在这台电脑上用 —— 线上那份后台连不上后端，
页面会提示「后台需要本机后端」。

## 按你的身份选一条路

| 我是… | 读这个顺序 |
| --- | --- |
| **机构员工**（前台 / 教务老师） | [使用手册](./使用手册.md) —— 打开后台照着做即可，不用看别的 |
| **运营 / 负责人**（要改网站内容） | [内容维护手册](./内容维护手册.md) → 遇到「为什么会这样」再看 [PROJECT.md](../PROJECT.md) |
| **开发者 / 接手的人** | [README](../README.md) → [技术架构](./技术架构.md) → [部署与发布](./部署与发布.md) |
| **要维护 / 扩展本机后端的人** | [后端开发方案](./后端开发方案.md)（怎么起、怎么备份、怎么改） → [后台API约定](./后台API约定.md)（接口与校验清单） → [技术架构 § 转真后端](./技术架构.md#8-转真后端时改什么)（当时是怎么接上的，留着对照） |

## 文档清单

| 文档 | 读者 | 内容 |
| --- | --- | --- |
| [使用手册](./使用手册.md) | 机构员工 | 后台每个页面怎么用：学生建档、报课与课时、收费与退费、排课与冲突、请假补课、咨询与可行性、报价与试算、待跟进、统计、备份，以及出错怎么办 |
| [内容维护手册](./内容维护手册.md) | 运营 / 负责人 | 网站内容都在哪、格式怎么写、课程卡片与价格怎么改、改完怎么验证与发布、常见症状对照 |
| [技术架构](./技术架构.md) | 开发者 | 架构全景、三条红线、目录职责、内容管线、服务层分层与领域模块、数据不变式与迁移纪律、报价与教师分成、自检体系、已知边界、术语表 |
| [部署与发布](./部署与发布.md) | 开发者 | 本机环境与全部命令（含起后端、登录口令与备份）、本地验证线上形态、发布流程与 CI 步骤（以及 CI 不跑哪些）、上线核对清单、常见故障、回滚 |
| [后台API约定](./后台API约定.md) | 接后端的人 | 当前 106 个接口按「服务端怎么做」分 7 类、服务端必须自己复核的 12 项校验、迁移六步 |
| [后端开发方案](./后端开发方案.md) | 要写/维护后端的人 | 本机 Node + SQLite（不上 Docker）、动手顺序与每一步的验收、**服务端会话认证**、**两种后端跑同一套自检**、**每天自动备份与恢复演练**、迁移六步与备份纪律、什么时候才需要 Postgres/Docker |
| [../PROJECT.md](../PROJECT.md) | 开发者 | 分主题的设计决策记录：每个功能「当时为什么这么定」、踩过的坑 |

## 机构自有资料（不是系统文档）

| 文件 | 说明 |
| --- | --- |
| [陈林维祎老师.md](./陈林维祎老师.md) | 教师个人资料（人工整理） |
| [教师信息登记表.md](./教师信息登记表.md) | 教师信息登记模板 |
| [screenshots/](./screenshots/README.md) | 截图目录（放 README 与手册里引用的图片） |

## 写文档时的约定

1. **简体中文**，句子写完整；每条规则都要能回答「为什么」，而不只是「怎么做」。
2. **口径与阈值必须能从代码核对**（例如请假 24 小时、教室利用率按上课时间 14 小时/天、教师分成 40% + 每人 10%）：写之前去 `lib/backend/*` 或内容文件里确认一遍，不确定就写「以页面提示为准」。
3. **不要把数量写死**（卡片数、教师数、接口数会变）：需要引用时写「当前 N 个」并说明在哪儿看。
4. **有边界的要说清楚**：例如「单用户、没有并发控制」「后端只绑本机 `127.0.0.1`」「后端重启要重新登录」「线上那份后台连不上后端」这类前提，每个相关章节都要提一次，别只在开头说一遍。**已经解决掉的边界必须把旧说法改掉**：接上服务端之后，凡是指向「浏览器本地存储」的旧说法都成了假话，留着比没有更糟（员工会按错误的前提做事）。这类旧句子不要当例子照抄 —— `npm run check` 会扫描文档里有没有残留的旧说法，引用了也算残留；当前的事实以 [技术架构 § 已知边界](./技术架构.md#10-已知边界与技术债) 为准。
5. **命令与现状同步**：`package.json` 里新增 / 改名命令后，根 [README](../README.md) 的「可用命令」表与这份索引要一起改；**CI 不跑的命令**（要起后端的那几条）要在文档里写明它是「本机门禁」，否则没人会跑。
6. **改了行为就改文档**：`npm run check` 会检查 README 与这份索引里链接的文档是否都存在、核心文档是否都被索引、两份索引有没有说明当前边界、同一份文档里的锚点链接是否指得到标题，还会检查 `docs/后台API约定.md` 是否提到了每一个接口方法 —— 文档漏掉接口时自检会红。
