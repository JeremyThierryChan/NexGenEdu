# 技术架构

> 读者：将来接手这个仓库的人、以及把它从「纯前端」接到真实服务端的人。
> 如果你只想改内容或使用后台，看 [使用手册](./使用手册.md) 与 [内容维护手册](./内容维护手册.md) 就够了。

**一句话架构**：一个 Next.js 静态导出的网站（`out/`，部署在 GitHub Pages）+ 一个和它共享同一套内容数据、但把教务数据放在浏览器里的「伪后端」（`lib/backend/`）。

- 网站与后台的数据**不同源**：网站读构建期打包进去的 Markdown 内容；后台读写运行期浏览器里的 localStorage。
- 全站没有服务端。所有「服务端应该做的事」（校验、事务、鉴权、审计）**已经按服务端的形状写好**，只是暂时跑在浏览器里 —— 这是本项目最重要的一条设计决定，见 [第 8 节](#8-转真后端时改什么)。

## 1. 全景

```
                   构建期（npm run build）                        运行期（浏览器）
┌──────────────────────────────────────────────┐   ┌───────────────────────────────────────┐
│ data/site/*.md                               │   │ 宣传网站 app/(site)/*                 │
│   │ npm run sync-content（脚本转成 TS）       │   │   ← lib/data/*（构建期已打包进来）      │
│   ▼                                          │   │                                       │
│ data/site/*.ts（生成物，已提交）               │   │ 教务后台 app/admin/(dashboard)/*       │
│   │ lib/data/content.ts 解析（标题层级=结构）  │   │   ↓ 只调这一层                        │
│   ▼                                          │   │ lib/backend/api.ts（97 个方法，async） │
│ lib/data/{site,pages,pricing,featured}.ts     │   │   ↓                                   │
│   │ 页面 / 组件                               │   │ lib/backend/storage.ts（KeyValueStore）│
│   ▼                                          │   │   ↓ 浏览器里是 localStorage            │
│ out/ 静态产物（79 个页面）                     │   │ nexgenedu.admin.db.v1（JSON 快照）      │
└──────────────────────────────────────────────┘   └───────────────────────────────────────┘
```

两条数据链**刻意不交叉**：

- 宣传网站是静态的，家长浏览器里没有后台的数据；
- 后台的数据只在那台浏览器里，改价改了也不会自己出现在网站上（报价配置要「导出 → 替换内容文件」才上线，见 [第 7 节](#7-报价与教师分成)）。

## 2. 三条红线

改动仓库时不要破坏这三条，它们各自都对应过一次真实事故：

1. **页面不解析 Markdown、不读文件。** 页面只调 `lib/data/*` 的访问器。历史上解析器层级写错时，构建依然成功、首页也正常，只有课程正文是空的 —— 靠肉眼发现太晚，因此把解析集中在一处，并让 `npm run check` 覆盖它。
2. **内容改动必须过 `npm run sync-content`。** `data/site/*.ts` 是生成物（同时提交进仓库，这样 CI 不需要先生成就能过类型检查）。手工改 `.ts` 会在下次同步时被覆盖。
3. **后台的一切改动必须走 `lib/backend/api.ts`。** 页面不直接读 localStorage；`export type BackendApi = typeof api` 就是未来服务端要实现的那份形状。

## 3. 目录结构与职责

| 路径 | 职责 |
| --- | --- |
| `app/(site)/` | 宣传网站页面：首页、课程、教师、学生案例、常见问题、时间安排、智能报价、关于、联系我们 |
| `app/admin/(dashboard)/` | 教务后台页面（13 个），整棵子树被 `RequireAuth` 包住 |
| `app/not-found.tsx` | 404 页面：一份产物、网站版与后台版两种内容（按路径分流） |
| `components/site/` `components/courses/` `components/teachers/` `components/pricing/` | 网站展示组件 |
| `components/admin/` | 后台表单控件（`AdminFields.tsx`）与业务面板（学生档案、排课、报价…） |
| `components/ui/` `components/layout/` | 基础 UI 与页头页脚侧边栏 |
| `data/site/*.md` | **内容源**（唯一手改的地方） |
| `data/site/*.ts` | 内容生成物（`sync-content` 产出，已提交） |
| `lib/data/` | 数据访问层：`content.ts`（解析器）、`site.ts`、`pages.ts`、`pricing.ts`、`featured.ts` |
| `lib/backend/` | 伪后端：服务层 `api.ts` + 领域纯函数模块（见第 6 节） |
| `lib/auth/session.ts` | 假登录（会话存 localStorage） |
| `lib/pricing/quote.ts` | 报价公式的**站点侧薄适配**（公式实现在 `lib/backend/pricing.ts`） |
| `lib/site/` | 导航配置：`nav.ts`（网站）、`admin-nav.ts`（后台）、`featured-routes.ts`（路径规则） |
| `scripts/` | `sync-content.mjs`、`dev.mjs`、`check.mts`（自检）、`check-404.mjs`、`check-links.mjs` |
| `docs/` | 使用手册、内容维护手册、本文、部署与发布、后台 API 约定 |
| `PROJECT.md` | 分主题的设计决策记录（「当时为什么这么定」都在这里） |

## 4. 内容管线

### 4.1 解析器：标题层级即数据结构

`lib/data/content.ts` 把一个 Markdown 文件解析成「页面 → 分组 → 节 → 条目」：

```md
# NexGenEdu · 新锐教培                     ← 文档标题

## 页面: 课程                              ← 页面边界

---
title: 课程                                ← 页面字段（--- 包裹的键值块）
---

### 小学课内                                ← 分组

#### 小学语文 | 路径: primary-chinese        ← 条目（| 后是字段）

正文段落…
```

设计要点：

- **不引 Markdown 库**：结构完全由标题层级决定，因此「内容里少写一个 `#`」只会丢内容、不会让构建失败；相应地，自检必须覆盖结构。
- **容错优先**：解析器遇到不认识的块只记警告、不抛错（构建不能因为一段内容写歪就整站挂掉）。
- 字段块用 `---` 包裹；`- · 字段: 值` 是卡片正文里的元数据行。

### 4.2 为什么 `.md` 还要生成 `.ts`

`·`（U+00B7）经过 webpack 会被序列化成非法转义 `\xb7`，导致内容在运行时坏掉。因此内容在构建前由 `scripts/sync-content.mjs` 转成 TypeScript 字符串模块。生成物同时提交：这样 CI 与类型检查不依赖「先生成」，也便于在 PR 里看清内容改了什么。

## 5. 宣传网站

- **静态导出**：`next.config.ts` 里 `output: "export"`、`trailingSlash: true`、`images.unoptimized: true`；子路径部署由 `NEXT_PUBLIC_BASE_PATH` 注入 `basePath`/`assetPrefix`。
- **页面全部静态**：没有服务端渲染、没有 API 路由、没有数据库。报价页的计算在浏览器里进行。
- **课程的三层结构**：`/courses`（6 个栏目 → 卡片）→ `/courses/<栏目拼音>`（学段页）→ `/courses/<卡片 slug>`（一张卡片一个页面，卡片内的「标签」是同一页面里的不同阶段，不单独成页）；特色课程在 `/courses/featured/<path>`。
- **404 与链接**：`check-404.mjs` 校验产物里的 404 分流与样式；`check-links.mjs` 遍历产物中所有站内链接（当前 2544 个）确认都有对应文件。

## 6. 伪后端（`lib/backend/`）

### 6.1 分层

```
页面（客户端组件）
  ↓ 只调这一层，方法签名与 REST 一致、全部 async
lib/backend/api.ts        97 个方法：CRUD / 关联查询 / 业务动作 / 咨询 / 报价 / 看板 / 运维
  ↓
lib/backend/storage.ts    KeyValueStore 抽象：浏览器里是 localStorage，Node 里是内存
  ↓ 首次访问灌入
lib/backend/seed.ts       示例数据（教师与教室取自站点真实内容，学生/排课为示例）
```

`types.ts` 是数据契约（`Student`/`Teacher`/`Classroom`/`Lesson`/`Payment`/`Enrollment`/`Inquiry`/`PricingConfig`…），`version.ts` 是结构版本（**当前 v11**），`contract.ts` 是接口清单与「服务端必须自己复核的校验」。

### 6.2 领域纯函数模块

业务规则全部抽成**不碰存储的纯函数**，因此既能在浏览器里跑，也能在 Node 里被自检完整验证，将来还能原样搬到服务端：

| 模块 | 职责 | 关键约定 |
| --- | --- | --- |
| `enrollment.ts` | 报课记录、剩余课时 | 剩余 = 有效正向流水 − 已用 |
| `finance.ts` | 金额、退费口径 | 实收 = 收款合计 − 退款合计；`REFUND_POLICIES` 两种口径，默认 `prorata`（按实付比例退） |
| `attendance.ts` | 出勤与请假扣课时 | `LEAVE_NOTICE_HOURS = 24`：提前 24 小时请假不扣，临时缺课扣 |
| `followup.ts` | 待跟进信号与话术草稿 | `FOLLOWUP_RULES`：剩余 ≤5 提醒 / ≤2 紧急、7 天未排课、作业未交 2 次、旷课 1 次、分数下降 10 分… |
| `stats.ts` | 经营视图 | `CLASS_HOURS_PER_DAY = 14`（上课时间 8:00–22:00 口径，不是营业时间 9:00–21:00）；利用率 ≤ 1 |
| `inquiry.ts` | 咨询可行性判定 | **检查的是一整串日期**，不是一天；判定只看不改，落库前再复核一次 |
| `pricing.ts` | 报价配置、公式、校验、导出 | 公式**只有这一份实现**；报价只收「选择」不收价格 |
| `teacher-share.ts` | 教师课时费（分成） | `小时数 × 课程单价/小时 × (0.4 + (人数−1) × 0.1)`；按计价方式判定适用范围 |
| `search.ts` | 全局搜索 | 只做不区分大小写的子串匹配，刻意不做拼音模糊 |
| `backup.ts` | 导出/导入校验、条数统计 | 导入三道保险：结构校验 → 自动备份 → 版本迁移 |
| `availability.ts` | 教室可用时段 | 不在开放时段内的排课被拒绝并说明原因 |
| `student-profile.ts` | 学生信息采集表字段定义 | 键值对存储，加字段不需要迁移 |
| `options.ts` | 后台表单候选项 | **从站点内容取**（科目、班型、标准时段），不另抄一份 |
| `format.ts` | 日期时间与金额格式化 | 全站统一，避免各处各写一套 |

### 6.3 数据不变式（自检逐条盯着）

- 报课实收 = 收款合计 − 退款合计；
- 已用课时 = 有效「上课」流水合计；总课时 = 有效正向流水合计；
- 教室利用率 ≤ 1；
- 课时、收款、流水**留痕不删**（撤销是写反向记录，不是删行），只有档案类允许真删；
- 每一次影响课时/金额的改动都要落操作日志（含摘要）。

### 6.4 迁移纪律（踩过的坑）

- 版本号只有一个来源：`version.ts` 的 `CURRENT_VERSION`，`seed.ts` 必须用它（曾因 seed 写旧版本，导致刚灌入的新数据在下次读取时被当成旧数据迁移，把报课记录压成一条「未指定科目」）；
- `migrate()` 里的分支**必须按版本升序排列**（曾写反顺序，v1 数据停在 v3、`migrate()` 返回 null，整库被当成坏数据重新灌种子）；
- `migrate()` 是就地修改，比较版本号要在改动**之前**记下来；
- 老库缺新字段时补默认值，**不要让字段是 `undefined`**（v10 → v11 就是给报价配置补教师分成规则）。

## 7. 报价与教师分成

价格是**数据**，不是常量：

- 配置类型 `PricingConfig` 存在数据库里（基础价、科目系数、班级系数、时长乘数、手续费、试课门槛、教师分成规则），随备份一起迁移；
- 公式只有一份实现（`lib/backend/pricing.ts` 的 `calculateQuote`）：前台报价页与后台试算器调的是同一个函数 —— 「宣传页一个价、后台另一个价」是这一块最不能出的事故，自检里有 7 个用例逐个比对两边算出的数；
- `lib/pricing/quote.ts` 只是站点侧的薄适配（取内容里的规则转发给后端公式）；
- 内容里的「计费规则」「教师分成」分组与后台配置是**同一个形状**：`api.pricing.exportMarkdown()` 导出的片段可以直接替换进 `data/site/pricing.md`，自检会把导出结果回读一遍，确认它与后台配置一致。

**伪后端阶段的必然限制**：后台数据在管理员那台浏览器里，而家长看到的报价页是静态的、读内容文件。所以「后台改价 → 家长看到」之间必须有人做一次「导出 → 替换内容文件 → 提交」。接上服务端后这一步消失。

## 8. 转真后端时改什么

完整清单在 [后台API约定.md](./后台API约定.md) 与 `lib/backend/contract.ts`（97 个方法按「服务端怎么做」分成 7 类）。要点：

1. **前端只改一个文件**：`lib/backend/api.ts` 的实现从 localStorage 换成 `fetch`，对外形状由 `export type BackendApi = typeof api` 固定，页面代码不动；
2. **服务端必须自己复核的 11 项校验**（冲突、容量、幂等、课时非负、金额不变式、退费重算、请假判定用服务端时间、报价配置合法性、报价金额由服务端算、鉴权、审计）—— 每一项都对应一个已经踩过的坑；
3. **搬数据**：旧站点导出 JSON → 服务端用同一份导入逻辑灌入 → 核对条数（导出页会显示各表条数）；导入前自动备份，导错可回滚；
4. **登录换成真的**：`lib/auth/session.ts` 改为调服务端，口令不再出现在前端代码里；
5. **退出条件**：服务端能通过同一套自检口径（课时不变式、金额不变式、冲突检测边界、迁移链），且前端不再有任何一处直接读 localStorage。

## 9. 自检体系

| 命令 | 盯什么 |
| --- | --- |
| `npm run check` | 内容解析、课程与卡片结构（路径 slug、条目、标签）、报价数据与公式、伪后端全流程（增删改查、课时/金额不变式、冲突边界、迁移链）、咨询判定、报价配置前后台一致性、接口契约与文档一致性（**600+ 项断言**（数量会随功能增长，以命令输出为准）） |
| `npm run check:404` | 404 产物：分流、标题、样式、未残留默认文案 |
| `npm run check:links` | 产物里的全部站内链接是否都有目标文件 |
| `npm run lint` / `npm run typecheck` | 常规质量门 |

**写自检的原则**：只把「框架与口径」写死（栏目名、班型名、不变式），**不写死数量**（卡片数、教师数、方法数会变；写死会让「改内容」和「自检通过」互相打架）。另外，曾经出现过几条**永远为真**的断言（`a === a`、用 `||` 兜底导致恒真、子串匹配让 `lessons.list` 匹配到 `lessons.listByDate`）—— 新增断言时要确认它**会失败**。

## 10. 已知边界与技术债

诚实清单（都是当前阶段的已知限制，不是「以后再说」的托词）：

- **后台数据只在本机浏览器**（localStorage），换设备/清缓存会丢；导入导出是唯一的搬运方式；
- **报价是双源**：后台配置与站点内容各自一份，靠「导出 → 替换」对齐；
- **假登录不是安全机制**：口令写在 `lib/auth/session.ts` 里，任何访问者都能看到 —— 别用它保护真实敏感数据；
- **无并发控制**：两个人同时改同一份数据会互相覆盖（真服务端要用事务/版本号解决）；
- **教师与课程是部分关联**：教师少，加教师时会按科目自动出现在课程页，但仍有课程没有对应教师；
- **学生「可上课时间」记录了但排课未使用**；咨询里的可上课时间也还没有带进学生档案；
- **教师推荐规则很朴素**（当前取本周课时最少者）；
- **「每两周一次」是固定间隔**，不处理单双周调课；
- **通知/短信/微信不存在**：待跟进只生成话术草稿，人来发；
- **图片未优化**（`images.unoptimized`），也没有真实截图。

## 11. 术语表

| 词 | 含义 |
| --- | --- |
| 学段 / 栏目 | 课程页的六个大栏目：小学课内、初中课内、高中课内、外语、课外兴趣、成人课程 |
| 卡片 | 一门课程 = 一张卡片 = 一个页面；卡片内的「标签」是同一页面的不同阶段 |
| 班型 | 一对一定制课、一对二、一对三小组课、一对多小班课、9 人以上大班课、晚托管、预习课… |
| 课时 | 一节课消耗 1 课时（与时长无关） |
| 标价 / 约定应缴 / 实收 | 标价 = 单价 × 课时；应缴 = 与家长谈定的总额；实收 = 收款合计 − 退款合计 |
| 有效流水 | 未被撤销的课时流水（撤销是写反向记录） |
| 营业时间 / 上课时间 | 接待 9:00–21:00；上课 8:00–22:00（统计口径用后者，每天 14 小时） |
| 伪后端 | 跑在浏览器里的服务层，接口形状与真实服务端一致 |
