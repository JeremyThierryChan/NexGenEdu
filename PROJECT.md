# NexGenEdu · 辅导机构轻量化管理系统 V1

## 1. 项目目标

为辅导机构提供一个「先把每天真正会用到的功能做好」的轻量化系统。V1 聚焦五件事：

**学生 · 教师 · 教室 · 课程 · 课时**

终极验收目标：**管理员打开系统，10 秒内知道今天谁上课、在哪里上课、老师是谁，以及每个学生还剩多少课时。**

本项目不是 ERP，当前阶段不是 SaaS。

## 2. 当前范围

### 本阶段做（立项时写的是「纯前端」，现在要分两半读）

- 对外宣传网站：首页 / 课程 / 教师 / 关于我们 / 联系我们 —— **仍然是文件驱动**，
  Markdown 作为内容源经数据访问层读取；
- 教务后台 UI 框架：Dashboard / 学生 / 教师 / 教室 / 课程 / 日历 —— 界面还是这一套，
  但数据已经不在页面这一侧；
- 排课、冲突检测、课时扣减、快捷搜索 —— 算法没变，**跑的地方变了**。

> 这一节写于立项时，当时"前端"就是全部。接上真实后端那一步之后，后台的业务数据
> 搬到了**本机 Node + SQLite**（见第 8 节「教务后台的后端架构」与「后端开发方案」），
> 因此这段补一句说明，免得读者仍按"纯前端"去理解现在的系统 —— 那会得出
> "换台电脑就看不到数据""清缓存会丢"这类**已经不成立**的结论。

### 本阶段明确不做

数据库（PostgreSQL / MySQL / MongoDB / Redis）、API Server、ORM、真实登录与 RBAC、支付、
微信登录、短信、AI 功能、微服务、多校区 / 多机构 / SaaS、财务与工资、CRM、题库与组卷、
复杂报表、家长 / 学生 / 教师账号。

这份清单里有**两项已经做了**，其余仍然不做：

- **API Server 已经做了**：本机 Node 服务端提供 `/api/call` 等接口（不是上面列的
  PostgreSQL / MySQL / MongoDB / Redis，而是 SQLite 单文件）；**ORM 仍然不做** ——
  服务端只用了一个 KeyValueStore，没有引入 ORM 这一层；
- **真实登录已经做了**：后台改成服务端会话（见「登录：从假登录到服务端会话」），
  而 **RBAC 当时不做** —— 彼时只有一个账号、没有权限区分，是有意的范围判断
  （**后来做了**：四个角色 + 服务端按方法拦 + 行级范围，见「权限与账号」一节）；
- 支付 / 微信登录 / 短信 / AI / 微服务 / 多校区 / 家长与学生教师账号：仍然不做，
  V1 要的是"每天真正会用到的功能"，不是功能齐全。

## 3. 技术栈

| 项目 | 选择 | 说明 |
| --- | --- | --- |
| 框架 | Next.js 15.5.25（App Router） | 界面与静态导出；**本机使用时另有一个 Node 后端**（`server/`），线上静态站不带它 |
| 语言 | TypeScript 5.9（`strict` + `noUncheckedIndexedAccess`） | 不使用 `any`；服务端直接跑 `.mts`（`--experimental-strip-types`），不另建一套编译产物 |
| 样式 | Tailwind CSS 4.3（CSS-first `@theme`） | 设计令牌集中在 `app/globals.css` |
| UI | 自建小组件库（`components/ui`） | 不引入重型组件库 |
| 数据 | 网站内容：Markdown + 数据访问层；后台数据：服务端 SQLite | 前者是构建期内容源，后者才是业务数据（钱、课时、家长联系方式） |
| 后端 | 本机 Node（只用标准库 http）+ `better-sqlite3` | 不引框架、不上 Docker，理由见「后端开发方案」一节 |
| 包管理 | npm（项目本地 cache：`.npm-cache/`） | 受本机权限限制，见第 9 节 |
| 字体 | 系统字体栈 | 不依赖外部字体 CDN，构建可离线完成 |

**架构主线（保证未来迁移成本低）**

现在有两条数据通路，界面都不感知数据存在哪里：

```
① 宣传网站（构建期读取，线上是静态文件）
   data/site/*.md  →  lib/data（数据访问层）  →  React / Next.js UI

② 教务后台（本机使用，数据在服务端）
   后台页面  →  lib/backend/remote.ts  ──POST /api/call──→  server/index.mts
                                                                   ↓ 跑的是同一份实现
                                                          lib/backend/api.ts
                                                                   ↓ KeyValueStore 接口
                                                          server/kv-store.mts → SQLite

未来（两边都不改界面 / 函数签名）：
   PostgreSQL  →  API  →  lib/backend/api.ts（同一套签名）  →  UI 不变
```

UI 不感知数据来自 Markdown 文件、服务端 SQLite 还是将来的 PostgreSQL。
（接后端之前，这句里写的选项是 localStorage —— 那条路已经走完，见第 8 节。）

## 4. 目录结构

```
app/
├── layout.tsx            # 根布局：html/body + 全局样式
├── globals.css           # 设计令牌（品牌色 / 字体 / 圆角）与基础样式
├── (site)/               # A. 对外宣传网站
│   ├── layout.tsx        #   顶部导航 + 页脚
│   ├── page.tsx          #   首页 /
│   ├── about/            #   /about
│   ├── courses/          #   /courses
│   ├── teachers/         #   /teachers
│   ├── contact/          #   /contact
│   ├── cases/            #   /cases 学生案例
│   ├── faq/              #   /faq 常见问题
│   ├── schedule/         #   /schedule 课程时间安排
│   ├── featured/         #   /featured 特色课程（含各课程独立页）
│   └── quote/            #   /quote 智能报价（下拉选择 + 报价计算）
└── admin/                # B. 教务后台（页面齐全；数据走 lib/backend/api.ts）

components/
├── ui/                   # Button / Card / Badge / Container / Section / PageHeading
├── layout/               # Logo / Header / Footer / AdminSidebar
├── site/                 # 宣传网站区块组件（PageHeader / FeatureCard / EmptyState）
├── courses/              # CourseCard
├── teachers/             # TeacherCard
├── pricing/              # EstimateForm（报价表单）
└── admin/                # 后台业务组件（学生详情 / 排课表单 / 报价面板 / 登录壳 …）

lib/
├── data/
│   ├── site.ts           # 数据访问层：页面唯一的数据入口
│   └── content.ts        # 单文件内容源解析（## 页面 / ### 分组 / #### 条目）
├── backend/              # 后台服务层：api.ts 是业务口径的唯一实现
│   ├── api.ts            #   全部后台方法；本机使用时被 remote.ts 换成对后端的代理
│   ├── remote.ts         #   POST /api/call 代理 + 令牌（仅 NEXT_PUBLIC_API_BASE 有值时启用）
│   ├── storage.ts        #   KeyValueStore 抽象（浏览器内存 / localStorage）
│   ├── contract.ts       #   接口契约（按「服务端怎么做」分组，自检双向比对）
│   ├── initial.ts        #   初始状态：业务表全空 + 网站课程 + 报价配置
│   ├── seed.ts           #   测试 / 演示夹具（只有 NEXGENEDU_ALLOW_SEED=1 才灌）
│   └── pricing / finance / attendance / stats / followup / timetable / export …
├── auth/                 # 登录：token.ts（令牌存取）+ session.ts（登录/登出/会话）
├── markdown.ts           # frontmatter / Markdown 极简解析与渲染
├── types/site.ts         # 内容类型定义
├── pricing/quote.ts      # 报价公式（改报价规则只改这个文件）
├── site/nav.ts           # 导航与页头 CTA 的结构配置
├── scheduling/           # 排课与冲突检测（Phase 5）
└── utils/cn.ts

data/site/
├── content.md            # 网站内容唯一来源（品牌 / 5 个页面文案与数据）
├── pricing.md            # 报价页的可选项与基础价格
├── faq.md                # 常见问题页 /faq
├── cases.md              # 学生案例页 /cases
├── featured.md           # 特色课程页与各课程独立页
├── schedule.md           # 课程时间安排页 /schedule
└── *.ts                  # 由 scripts/sync-content.mjs 从上面的 .md 自动生成

scripts/
├── dev.mjs               # 开发入口：监听 .md 改动自动同步 + 启动 next dev
├── sync-content.mjs      # 把 .md 编译为 .ts 内容模块
└── check*.mts / check-*.mjs
                          # 自检：业务口径、真实 HTTP、认证、链接、404 产物、逐页验收

server/                   # 本机后端：只在 npm run server 时运行，不进线上静态站
├── index.mts             # HTTP 入口：/health、登录、会话闸门、POST /api/call、自动备份
├── auth.mts              # 口令与令牌（口令来自环境变量或首次启动随机生成，不落库）
├── db.mts / kv-store.mts # SQLite 连接与 KeyValueStore（kv 表存整份 JSON 快照）
├── backup.mts / backup-cli.mts   # 每日自动备份 + 命令行（npm run server:backup）
├── migrate.mts / migrations/     # 建表与升级
├── data/                 # nexgenedu.db（不进 git）
└── backups/              # 每日备份文件（不进 git）
```

## 4.1 内容数据层（主体在 1 个文件，另有 4 个页面各自成文）

**所有面向访客的文案、数字、联系方式都在 `data/site/content.md` 一个文件里。**
改完保存，刷新页面立即生效（dev 模式下该文件是构建依赖，保存即触发重新编译）。

文件按页面分段，共 6 段：

| 段落 | 覆盖范围 | 影响页面 |
| --- | --- | --- |
| `## 页面: 全站` | 品牌名、标语、描述、关键词、联系方式、版权 | 全站标题 / 页头 / 页脚 |
| `## 页面: 首页` | 首屏、首屏数据、教学特色、课程卡片、教室格位、底部 CTA | `/` |
| `## 页面: 课程` | 课程页文案 + 每门课程介绍 | `/courses` |
| `## 页面: 教师` | 教师页文案 + 每位教师的科目/职务/教龄/简介/介绍 | `/teachers`、首页教师卡片 |
| `## 页面: 关于` | 教学理念、校区数据、校区介绍 | `/about` |
| `## 页面: 联系我们` | 联系方式清单、到校路线、预约按钮 | `/contact` |

> **补充（写这一节时"只有这一个文件"是真的，后来不再是了）**：宣传网站扩展之后，
> 常见问题（`faq.md`）、学生案例（`cases.md`）、特色课程（`featured.md`）、
> 时间安排（`schedule.md`）各自成文 —— 它们也是 `data/site/*.md`，同样由
> `sync-content.mjs` 生成同名 `.ts`，只是各自有更贴题的结构（问题 / 案例 / 课程层级 / 时段表）。
> 上面那张表只覆盖 `content.md` 的 6 段；改这几页的内容要去对应的文件，别在 `content.md` 里找。

### 解析器：标题层级即数据结构

`lib/data/content.ts` 的 `buildTree()` 按 Markdown 标题的井号数**如实建树**，
不做「哪一层是分组、哪一层是条目」的推断（早期版本靠层级加减推断，
在 `## 分组 → ### 阶段 → #### 条目` 这类多层结构下反复出错）。
数据层按名字取节点，需要更深层级就继续读 `children`。

约定：

| 写法 | 含义 |
| --- | --- |
| `## 页面: 首页` | 页面分段 |
| `## 分组` / `### 分组` | 分组（页面级） |
| `#### 名称 \| 值` | 条目（值用半角冒号时也可写作 `名称: 值`） |
| `#### 学段：一句话` | 内容小节（正文写在下方）；全角冒号是区分信号 |

### 自检脚本

`npm run check` 会校验数据层条目数量、首页标签与课程页小节的**双向**对应、
以及报价公式的关键用例（断言数量见运行输出）。

**为什么需要它**：解析器层级出错时构建依然成功、首页也正常，
只有课程正文是空的 —— 这类问题靠肉眼检查发现太晚。

### 首页 / 课程页的「栏目 → 子标题 → 卡片」结构

### 课程的三层结构

```
/courses                    课程总览（6 个栏目 → 卡片）
  /courses/primary          学段（栏目）页 —— 这一阶段各科目的简介
    /courses/primary-chinese 课程卡片页 —— 这门课的具体内容（阶段小节）
```

- **班型的反向关联**：在班型页（`/courses/featured/in-class/one-on-one` 等）列出
  **开设这个班型的科目**，按「阶段（栏目）→ 子栏目」分组，用与课程总览一致的
  卡片网格展示（32 门课竖着排成一列会很长、不好找），栏目标题指向该阶段页。关系是**反向查出来的**（`getCardsForForm()`），
  数据只在卡片行的 `· 班型:` 里写一次，改卡片两处同时变；
  自检逐项比对正反两个方向，多一个少一个都会失败。
  两个不是「按人数分」的班型，语义按业务固定（自检会拦住改动）：

  | 名称 | 语义 | 关联规则 |
  | --- | --- | --- |
  | 晚托管 | 单独的服务，**不分科目**，按学段分小学晚托 / 初中晚托 | 不挂任何学科；页面正文写明「不按科目分班」 |
  | 周中预习课 | **只有初中有** | 只挂在初中课内的 5 门科目上 |

  此外「预习课」按时间分成两种，都是**二级课程**：

  | 名称 | 含义 | 下级 |
  | --- | --- | --- |
  | 周中预习课 | 学期中提前于校内进度上课（只有初中有） | 无 |
  | 假期预习课 | 假期提前预习下一学段，衔接初一 / 高一 | 精品小升初 / 精品初升高 / 基础小升初 / 基础初升高 |

  假期预习课原先把 4 门课拆在「一对多小班课」「9 人以上大班课」下（按人数分），
  现改为按**用途**成组 —— 代价是这 4 门课的网址从
  `…/mini-class/junior-prep` 变为 `…/holiday-preview/junior-prep`。
- **栏目页**（`primary` / `junior` / `senior` / `languages` / `interests` / `adults`）
  回答「先定学段，再看科目」：列出该阶段每个科目 + 一句简介（**从现有正文派生**，
  不重复写字：学科导语 → 选修课介绍 → 第一阶段正文的第一段），点标题进科目页。
  栏目路径写死在 `COLUMN_PATHS`（栏目名是结构，自检固定了六个），
  自检还会校验栏目路径不重复、且与卡片路径不重叠 —— 两层共用 `/courses/<一段>`，
  重叠会让某一层被遮住。
- **课程卡片页**额外回答两个家长问题：
  - **这门课开设的班型**：来自卡片行里的 `· 班型:`，写的是「特色课程」里的班型名
    （一对一定制课 / 一对二·三小组课 / 一对多小班课 / 9 人以上大班课），
    自检会核对每个班型名都真实存在于特色课程；没填的课程不显示这一块。
    当前 32 张卡片都填了这四种按人数分的班型。
  - **这门课谁来上**：只按教师页的「科目」字段匹配（教师的某个科目出现在
    卡片名或阶段名里），匹配不到就明说「授课教师会按学生的年级与薄弱环节安排」，
    **不硬塞老师**。

    > **当前是「部分关联」，这是刻意的**：教师人数还少，暂时有 12 门课关联不到老师。
    > 关联逻辑完全由数据驱动 —— 在 `content.md` 的教师段补一位老师、把 `科目`
    > 写成课程里用的叫法（语文 / 物理 / 雅思…），对应课程页立刻出现这位老师，
    > **不需要改代码**。`npm run check` 会打印「暂未关联到教师的课程」清单，
    > 补人时照着清单看进度；因此自检**不要求**每门课都有老师。

### 课程卡片页：一张卡片一个页面

点课程总览里的卡片进入 `/courses/<路径>`（路径写在卡片的 `· 路径:` 里，必须是 ASCII）。

**卡片上的标签不是独立页面，而是同一页面内的「阶段」**：

```
/courses/senior-physics   高中物理 → 学考、选考 两段（页内锚点互跳）
/courses/ielts            雅思 → 总览 + 口语 / 听力 / 阅读 / 写作
/courses/gaokao-languages 高考外语 → 六种语言各一段
/courses/primary-chinese  小学语文 → 自己一段 + 关联 初中语文 / 高中语文
```

页面上给出与其他阶段的关联：**同一学科的其他学段**（小学语文 → 初中语文 / 高中语文）
与**同栏目（同子栏目）的其他课程**（高中物理 → 化学 / 生物 / …），
前者让家长看清进阶路径，后者提供横向对比。

卡片自己有总览小节时（如「雅思｜按目标分数提分」），它不对应任何标签，
但内容不能丢，因此作为页面开头的「课程说明」渲染 —— 见 `CoursePageData.overview`。

**两个页面的课程区职责不同**：

| 页面 | 课程区内容 | 回答的问题 |
| --- | --- | --- |
| 首页 | **按班型**（一对一定制课 / 一对二·三小组课 / 一对多小班课 / 9 人以上大班课 / 晚托管 / 周中预习课），数据取自「特色课程」，用 `CourseTree` 渲染 | 「你们怎么上课」 |
| 课程页 | 「课程总览」按 **栏目 → 子标题 → 卡片** 列出全部学科与课程 | 「你们教哪些课」 |

首页的六个区块顺序固定为：**教学特色 → 负责的教师团队 → 学生案例 → 试课体验 →
按班型选课 → 教室环境**（首屏与结尾的免费测评行动号召在外层）。

栏目结构定义在 `content.md` 的
**「页面: 全站 → `### 课程栏目`」**（`getCourseColumns()`），避免两处各写一遍走偏。

**核心约定：一张卡片 = 一门课程。课程内部没有细分时不要写标签，整张卡片就是入口。**

```
小学课内   小学语文 / 小学数学 / 小学英语 / 小学科学                  （4 张，无标签）
初中课内   初中语文 / 初中数学 / 初中英语 / 初中科学 / 初中社会        （5 张，无标签）
高中课内   ├ 必考科目  高中语文 / 高中数学                          （2 张，无标签）
           ├ 外语      高考外语 → 高考英语、高考日语、高考俄语、
           │                   高考德语、高考法语、高考西班牙语      （1 张，6 个标签）
           └ 七选三    高中物理 → 学考、选考（化学/生物/政治/历史/地理/技术相同）
                                                                     （7 张，各 2 个标签）
外语       雅思（无标签）
           日语 → N5、N4、N3 · 俄语/法语/德语/意大利语/西班牙语 → A1、A2、B1、B2
课外兴趣   书法与硬笔字 / 围棋入门 / 编程与信息素养 / 阅读与写作兴趣班（4 张，无标签）
成人课程   成人英语口语 / 成人零基础外语 / 出国语言备考 / 职场与商务英语（4 张，无标签）
```

「暂未开放」有两处来源，**必须一致**（`npm run check` 会拦住不一致）：

| 写在哪 | 显示在哪 |
| --- | --- |
| 卡片行里 `· 状态: 暂未开放` | 课程总览卡片的右上角标记 |
| 课程分组里 `- · 状态: 暂未开放` | 课程详情区该课程标题旁的标记 |

课外兴趣类课程（3D建模、编程与信息素养）与学科一样是 `### 课程名` + `#### 小节｜一句话`，
因为它们的内部确有细分（按工具 / 按语言分）。选修组（`- · 状态` 的叶子课程）只用于
没有细分、仅一张卡片的课程（当前是 4 门成人课程）。

格式为 `#### 卡片名 | 栏目: X · 子栏目: Y · 标签: A、B→目标小节`：

- `子栏目` 只在栏目需要再分组时写（目前只有高中课内的 必考科目 / 外语 / 七选三）。
- `标签` 只在课程内部确有细分时才写；不写 `→` 时标签名即目标小节名。
  标签文字与目标分开，例如 `学考→高中物理学考`（7 科都用「学考」「选考」两个词，
  但锚点必须各自唯一）。
- **卡片标题点哪里**：这门课在详情区有自己的说明小节时指向它；没有时（例如
  「高中物理」被拆成学考/选考两节、没有总览小节）退回到它的第一个标签。
  规则实现在 `getCourseColumns()` 里，依据是 `courseSectionNames()` 收集的真实锚点集合。
- 卡片与标签都指向课程页**详情区的小节**（`id` 锚点），小节名的唯一来源是
  `content.md` 的「页面: 课程」段。
- `npm run check` **只校验性质，不校验具体名单**：六个栏目固定、高中课内固定三个子标题、
  每个子标题下都有卡片、卡片名不重复、卡片内标签不重复、七选三每卡都带学考+选考、
  外语栏目每张语言卡都有级别标签；再加上卡片/标签与课程页小节的**双向**锚点校验
  （不能跳空，也不能有「从哪都进不去」的孤立小节）。

  > 这里刻意**不写死**课程名、卡片数、教师人数。`content.md` 是手工维护的，
  > 增删课程与教师都是正常编辑；把名单写进断言会让「改内容」和「自检通过」互相打架，
  > 而 `check` 会卡住部署，等于正常编辑也推不上去。
- 卡片标题刻意不用标题标签：层级已经是 h1 → h2 区块 → h3 栏目 → h4 子标题，
  再往下用 h5 只会让大纲更碎，链接本身已经可读。

### 文件格式

````markdown
## 页面: 首页              ← 页面分段
---
title: 让学习真正发生      ← 该页面的短字段（frontmatter）
---

### 首屏数据               ← 分组
格式为「标签 | 数值」。这段说明不会显示在页面上。

#### 班级规模 | 4–8 人      ← 条目：竖线分隔（列表项）
#### 师生比 | 1 : 6
````

- **短字段**：段落开头的 `--- ... ---` 里，`key: value` 一行一个；数组用 `- 项`。
- **分组**：`### 分组名`；分组标题下的说明文字只写给人看，不显示在页面上。
- **条目**：`#### 名称 | 内容`（列表项）或 `#### 字段: 值`（教师字段）。
  名称取最左侧分隔符：竖线优先，因此「1 : 6」这类含冒号的数值不会被误拆。
- **长文本**：直接写在分组标题下，支持 `**粗体**`、`- 列表`。
- 解析实现在 `lib/markdown.ts` 与 `lib/data/content.ts`，只支持上述子集，零依赖。

### 为什么 .md 还要生成 .ts

内容以 Markdown 维护（便于手改），但 Next 无法可靠地直接把 `.md` 当字符串导入：
其内置处理会把 `·`（U+00B7）序列化成非法的 `\xb7` 转义，产物直接语法报错。
因此 `scripts/sync-content.mjs` 会把 `data/site/*.md` 生成同名的 `.ts` 模块
（`export const contentSource = \`...\``），再由数据层 import。

这一步已自动化，正常使用无需关心：

- `npm run dev` → 启动时同步一次，并监听 `.md` 改动自动同步（刷新页面即生效）
- `npm run build` → 构建前同步一次
- `npm run sync-content` → 手工同步（备用）

### 报价页（/quote）

**报价模型**（数据在 `data/site/pricing.md`，公式在 `lib/pricing/quote.ts`）：

```
1. 课时价   = 基础价 × 人数系数               （一对一 / 一对二 / 一对三 / 一对多）
   课时价   = 教师课时总费用 ÷ 班级人数           （班课）
2. 时长调整 = 课时价 × 时长乘数                    （1 小时 1.0 / 1.5 小时 1.5 / 2 小时 2.0）
3. 手续费   = 1 节 +10%，其余不加收
   最终单价 = 时长调整后价格 × (1 + 手续费百分比)
4. 正课总价 = 最终单价 × 节数
5. 试课：试课后报课满 10 节则免费；否则按课程原价收 1 节试课费
   总价 = 正课总价 + 试课费
```

| 分组 | 内容 |
| --- | --- |
| 学习阶段 | 6 个阶段 → 课程 → 基础价（元 / 课时），共 22 门 |
| 班级类型 | 一对一 1.0 / 一对二 0.7 / 一对三 0.6 / 一对多 0.5 / 班课（按人数分摊）—— 这一处系数现在叫**人数系数** |
| 课时选择 | 1 小时（×1.0）、1.5 小时（×1.5）、2 小时（×2.0）；参与计算但不展示换算 |
| 课程 | `## 学习阶段` → `### 阶段` → `#### 课程: 名称: 价格`（**没有「科目」这一维**，见 E10） |
| 报课节数 | **由用户在页面上手动输入**，不写在数据文件里 |
| 手续费规则 | 1 节 +10%，其余不加收（写在 `quote.ts`） |
| 试课 | 试课后报课满 10 节免费；未满 10 节按课程原价收 1 节（写在 `quote.ts`） |
| 其他项目 | 课后晚辅导、考试大师网课等独立产品，不参与课时公式 |

实现要点：

- 「暂未开放」写在字段值里即表示不可选：下拉框中显示但禁选（专业英语全部、
  成人/兴趣的跨国交友、高中政治）。
- 阶段与课程联动：切换阶段会清空课程选择，避免出现不匹配组合。
- 班课模式（coefficient 值不是数字，例如「按人数分摊」）会额外显示
  「班级人数」与「教师课时总费用」两个输入框，并按 `费用 ÷ 人数` 计算
  （「班级人数」这个名字保留 —— 它问的确实是班级人数；只有"系数"那一处改叫**人数系数**）。
- 所有价格与档位均可增删，页面会自动适配。

**展示口径**：结果只给两项 —— **课单价（元 / 节）** 与 **总价**（课单价是"每节课的价"：
基础价按**元/小时**定，再乘每节课的小时数）。
刻意不展示「每课时」与时长换算过程：家长关心的是每节课多少钱、一共多少钱，
中间换算属内部逻辑，摆出来反而增加理解成本。时长仍参与计算，只是不作为展示项。

⚠️ 待确认项：

1. **手续费语义**按「加价」实现（1 节 = 课时价 × 1.1）。若应为折扣需改 `quote.ts`。
2. **试课计入的节数**：正课节数按用户填写值计算，试课另计（不免正课节数）。
3. **时长乘数**按线性实现（1.5 小时 = 1.5 倍）。若实际不是简单相乘，
   改 `pricing.md` 里「课时选择」的「乘数」即可，代码不用动。
4. 「考试大师网课 + 针对性答疑课」价格未定，当前显示「暂未定」。

> （原先第 1 条「**科目系数**目前全部按 1.0，等实际系数确定后补一行」已经在 E10 里结掉：
> 机构定的是**把这一维删掉**，不是等它确定 —— 详见 E10。）

### 页面读取方式

页面只调用 `lib/data/site.ts` 的函数（`getSiteBrand` / `getHomeContent` /
`getCoursesPage` / `getTeachersPage` / `getAboutContent` / `getContactContent`），
不直接读文件、不解析 Markdown。未来替换为数据库实现时页面无需修改。

## 5. 数据模型（Phase 2 落地）

五个核心实体，字段设计直接对齐未来 PostgreSQL Schema：

| 实体 | 关键字段 |
| --- | --- |
| `Student` | `id` `name` `grade` `phone` `status` `creditBalance` `createdAt` `updatedAt` |
| `Teacher` | `id` `name` `subjects[]` `phone` `status` `createdAt` `updatedAt` |
| `Classroom` | `id` `name` `capacity` `status` `createdAt` `updatedAt` |
| `Lesson` | `id` `studentId` `teacherId` `classroomId` `date` `startTime` `endTime` `status` `creditCost` `note` |
| `CreditTransaction` | `id` `studentId` `lessonId?` `amount` `type` `balanceAfter` `createdAt` |

设计约定：

- 课时余额不以 `credit_balance` 为唯一真实来源，同时记录 `CreditTransaction` 流水，便于未来对账。
- 所有实体带稳定 `id` 与 `createdAt` / `updatedAt`。
- 关系通过 `studentId` / `teacherId` / `classroomId` 外键字段表达。

## 6. 开发进度

| Phase | 内容 | 状态 |
| --- | --- | --- |
| Phase 1 | 项目基础：Next.js + TS + Tailwind + Layout + 基础 UI + 宣传网站骨架 | ✅ 已完成 |
| Phase 2 | Markdown 数据层：解析器 + 数据访问层 | ✅ 已完成 |
| Phase 3 | 宣传网站内容接入 Markdown | ✅ 已完成 |
| Phase 4 | 后台：Dashboard / 学生 / 教师 / 教室 / 课程 / 日历 | ✅ 已完成（当时是伪后端版本，页面齐全） |
| Phase 5 | 核心业务：排课 / 冲突检测 / 课程状态 / 课时扣减 / 搜索 / 收费 / 请假补课 / 统计 | ✅ 已完成（当时是伪后端版本；口径沿用至今） |
| Phase 6 | 响应式与 UI Polish | ⏳ 各页面已按断点适配，细节打磨与页面渲染逐页人工确认待补 |
| Phase 7 | 部署：GitHub Actions → GitHub Pages | ✅ 已完成 |
| — | 宣传网站扩展：学生案例 / 常见问题 / 时间安排 / 特色课程独立页 | ✅ 已完成 |
| — | 咨询线索与排课可行性（数据库 v9） | ✅ 已完成 |
| — | 报价进后端 + 教师分成规则（数据库 v10 / v11） | ✅ 已完成 |
| — | 文档体系：使用手册 / 内容维护手册 / 技术架构 / 部署与发布 | ✅ 已完成 |
| — | **接真实后端**：本机 Node + SQLite、服务端会话登录、每日自动备份与恢复演练 | ✅ 已完成（页面代码未改，见第 8 节「教务后台的后端架构」） |
| 下一阶段 | 网站内容进库做只读镜像（表已建好，同步脚本还没写）/ 页面渲染逐页人工确认 / 公网部署（机构已定暂不上服务器） | 计划中 |

### Phase 1 交付说明

- 完成 Next.js 15 + TS strict + Tailwind 4 脚手架，`lint` / `typecheck` / `build` 全部通过。
- 建立设计系统：品牌色（深墨蓝 + 暖琥珀）、状态色（成功 / 预警 / 危险）、中性色阶、圆角与字体。
- 建立布局与基础组件：`Header`（含移动端折叠菜单）、`Footer`、`Logo`、`Button`、`Card`、`Badge`、`Container`、`Section`。

### 「营业时间」与「上课时间」是两个字段

这两个概念很容易被写成同一个值，产生矛盾的说法（改之前站内有三种写法）。
现在分开定义，两处都从内容文件读：

| 字段 | 含义 | 当前值 | 显示位置 |
| --- | --- | --- | --- |
| `business_hours` | **营业时间**：工作人员在校区接待咨询 | 每日 9:00–21:00（**含节假日**） | 页脚（标为「接待时间」）、关于页、联系页 |
| `class_hours` | **上课时间**：教师与学生实际上课的时段 | 每日 8:00–22:00（**含节假日**） | 页脚、关于页、联系页、时间安排页 |

- 页脚标签刻意写成「**接待时间**」与「上课时间」：只写「营业时间」家长会以为
  8:00 就有人在；
- 时间安排页的 `notice` 把两者的差别说清（早课 8:00 开始、晚课 22:00 结束，
  但 9:00 前或 21:00 后到校区可能没人接待）；
- FAQ 里也有一条「你们几点上班？几点能上课？」；
- **教室利用率的估算基准用「上课时间」（14 小时/天）而不是营业时间**：
  教室能不能用取决于能不能上课，而不是前台有没有人。这条是这次澄清之后
  才发现的连带错误（原先按 13.5 小时估）。

自检会校验两个字段都有值、不是同一句话、上课时间的范围包住营业时间，
以及**节假日口径两处一致**（一处写「含节假日」另一处不写，家长会不知道信哪个）。

### 页脚布局

按「家长想做什么」分四栏，而不是把导航平铺成一列（以前 10 个入口混在一起，
家长找「报价」要先扫过「首页 / 学生案例 / 常见问题」）：

```
品牌 + 预约试听 / 查看报价 │ 选课与价格 │ 了解我们 │ 联系我们
──────────────────────────────────────────────────────────
© 版权                                        教务后台
```

- 导航分组在 `lib/site/nav.ts` 的 `FOOTER_GROUPS`（结构配置，不是文案）；
- **电话与邮箱是可点链接**（`tel:` / `mailto:`）：页脚常是家长在手机上最后看到
  联系方式的地方，只给一串没法点的文字等于没有。`tel:` 只保留数字与开头的 `+`，
  括号里的备注与连字符不能进去，否则手机拨不出去；
- **不放「首页」**：页头 Logo 就是回首页的入口；
- 「联系我们」的标题本身是链接（家长要找的是那一页，不只是这几个字）；
- 空值不渲染：地址、营业时间没填就不显示该行，而不是留一个空标签。
- （**当时**）宣传网站那几个页面全部可访问；还没接内容的区块一律用明确的空状态
  （`EmptyState`）而不是假数据 —— 这条原则照旧，"哪几个页面"和"哪些区块待接"
  以现在的 `app/(site)/` 与 `data/site/*.md` 为准，不再用当时的清点数说话。

## 7. 常用命令

```bash
# 前端与内容
npm run dev          # 开发服务器 http://localhost:3000
npm run lint         # ESLint
npm run typecheck    # tsc --noEmit
npm run build        # 生产构建（会先同步内容）
npm run sync-content # 手工同步 data/site/*.md（一般不需要）

# 本机后端（不运行它时后台用不了：登不进去，页面也只显示一句说明）
npm run server          # 启动后端 http://127.0.0.1:4000（首次启动会打印生成的口令）
npm run server:health   # 探活（公开的 /health 只回服务名与库文件名）
npm run server:migrate  # 建表 / 升级
npm run server:import   # 把导出的 JSON 导进服务端库（迁移与恢复用）
npm run server:backup   # 手动备份（-- --list 看现有备份，-- --force 强制再备一份）
npm run drill:restore   # 恢复演练：备份 → 验备份文件 → 删库 → 只靠备份恢复 → 核对账目

# 自检与验收（check:both / check:auth / accept / drill:restore 会自己起临时后端与临时库，
# 不碰 server/data 里的真实库与备份目录）
npm run check       # 内容结构 + 全部业务口径（跑本地内存实现）
npm run check:404   # 404 产物（实际跑一遍内联脚本）
npm run check:links # 站内链接与锚点
npm run check:all   # 上面三个一起跑
npm run check:both  # 同一套自检对「内存实现」与「真实 HTTP 后端」各跑一遍
npm run check:auth  # 服务端认证（未登录一律 401、口令不落库等）
npm run accept      # 逐页验收：自己起临时后端，逐页打开后台页面
```

日常改业务逻辑：`lint` → `typecheck` → `npm run check:all` → 更新本文件 → Git commit。
改到后端或登录相关的东西，再加跑 `check:both` 与 `check:auth`；
`check:both` 是"换后端没改口径"最硬的证据 —— 同一套断言必须两遍都过。

### 运行命令的环境要求

- **npm 必须带本地 cache**：`npm install --cache ./.npm-cache`（原因见第 9 节）。
  构建 / 运行（`dev` / `build` / `start`）**不需要**额外的 cache 参数，可直接 `npm run dev`。
- 若需在非交互环境验证页面，可用 `npx next start -p <端口>` 配合 `curl` 做冒烟测试。
- **想跟线上一致地验证，构建要带 basePath**：
  `NEXT_PUBLIC_BASE_PATH=/NexGenEdu npm run build`。
  `npm run check:404` 按 `NEXT_PUBLIC_BASE_PATH` 推断站内链接前缀，
  不带该变量做本地构建时前缀为空（这本身是正确的，不是失败）。
- **本机使用要显式指后端**：前端只在 `NEXT_PUBLIC_API_BASE` 有值时才把 `api` 换成
  远端代理（写在 `.env.local`，该文件不进 git）。不设它，后台就退回到"没有后端可连"
  的那种界面 —— 这是**刻意**的：判据是环境变量，而不是"能不能连上后端"，
  这样线上构建（不设该变量）永远保持静态站该有的样子。
- **自检与验收用的都是临时库**：脚本会把后端起在临时数据库上
  （`NEXGENEDU_DB` 指向临时文件、`NEXGENEDU_NO_BACKUP=1` 关掉自动备份），
  不会往 `server/data/nexgenedu.db` 或备份目录里写东西。

## 7.1 部署（GitHub Pages）

线上地址：**https://jeremythierrychan.github.io/NexGenEdu/**

| 项 | 值 |
| --- | --- |
| 方式 | GitHub Actions 自动部署 |
| 工作流 | `.github/workflows/deploy-pages.yml` |
| 触发 | push 到 `main`，或手动 `workflow_dispatch` |
| Pages 配置 | `build_type: workflow`（已通过 API 设置，`https_enforced: true`） |
| 产物 | 静态导出 `out/`（`output: "export"`） |

工作流步骤：`lint` → `sync-content` → `typecheck` → **`check`** → `build`（注入 basePath）
→ `.nojekyll` → **`check:404`** → **`check:links`** → 上传 → 部署。

`check` / `check:404` / `check:links` 会**卡住部署**：内容结构被改错（卡片跳空、栏目错位）
或 404 产物异常时构建直接失败，而不是把坏页面发到线上。
`check:404` 与 `check:links` 的前缀按 `NEXT_PUBLIC_BASE_PATH` 推断，因此部署步骤里也注入了同一个变量。

`check:links` 会把 out 下每个 HTML 的站内链接全部取出来验证：目标页面文件存在，
且带锚点时锚点 id 真的在目标页面里（防止「跳过去了但停在页首」）。
（**历史**：这份脚本里曾经有一份「显式放行」清单，放行后台尚未实现的几个入口 ——
它们点了会显示内部版 404，属于当时的刻意行为。那些页面后来都实现了，
清单现在是空的；留着一条不再需要的放行，等于给"点了 404"发一张通行证，所以必须删干净。）

**线上那份后台连不上后端，因此无法登录** —— 部署是静态导出，`NEXT_PUBLIC_API_BASE`
在线上没有值（也不该有：值会被打进前端包，等于把机构自己的后端地址公开出去）。
所以线上点进 `/admin` 会看到一句「后台需要本机后端」和两条本机启动命令，
而不是一个登录表单。这不是故障，是当前形态的必然结果：宣传站是给家长看的，
后台只在本机用。详见第 8 节「登录：从假登录到服务端会话」。

### 区块间距：Section 的 compact 档

`Section` 默认留白很大（`py-16 sm:py-20`、标题下方 `mb-10`），单个区块里好看，
但首页六个课程栏目依次排列时，上一个的下留白与下一个的上留白会**相加**
（约 128–160px），中间像断了一截，页面显得很空。

因此 `Section` 提供 `compact` 档：`py-6 sm:py-8`、标题下方 `mb-5`、说明文字降到 `text-sm`，
栏目间距从约 128–160px 收到 48–64px，仍靠 `border-t` 分隔，不会糊成一片。
**规则：连续排列的同类区块用 `compact`，独立的大区块保持默认。**

### 404 页面：一份产物，两版内容

静态托管（GitHub Pages）只认站点根目录的**一个** `404.html`，服务器没有机会
按路径返回不同内容。因此两版都渲染进同一个文件，由 `app/not-found.tsx` 里的
一段内联脚本读 `location.pathname` 打上 `html[data-nf-route="backoffice"]`，
再由 `app/globals.css` 里的两条规则决定显示哪一版。

| 触发场景 | 显示 | 内容 |
| --- | --- | --- |
| 网站路径（如 `/courses/` 打错） | 网站版 | 「这一页还没学到」+ 页码意象 + **完整站内目录**，让误入者能继续逛 |
| `/admin` 下的路径 | 教务后台版 | 工具化排版（等宽字、状态码、请求路径）+「该功能尚未完善，请联系管理员陈林维祎」+ 返回后台首页，**不铺站点导航、不显示页脚** |

几个刻意的处理：

- **默认显示网站版**：后台版带 `hidden`，只有脚本确认路径后才显示。JS 没跑或
  脚本出错时，用户看到的仍是一版正常的 404，而不是空白页。
- **脚本放在最前面且只写属性、不碰 DOM**：此刻两版内容还没解析出来；属性一打上
  CSS 立即生效，不会先闪一下网站版。脚本不依赖 React 水合，即使 JS 包加载失败也已执行。
- 后台的 404 当时大多是**功能还没做**（侧边栏的学生 / 教师 / 教室 / 课程安排 / 日历
  那时都还没实现），不是地址打错 —— 所以后台版的重点是「找谁」，而不是「继续浏览」。
  **现在那些页面都有了**，后台的 404 基本等于地址打错；但「找谁」这版文案留着，
  因为它对"某个深层功能还没做"的情况仍然成立，而这类情况还会再出现。
- 必须用 `!important`：Tailwind 的 preflight 用
  `[hidden]:where(...){display:none!important}` 压制 hidden 属性，
  要让后台版显示出来得用同等权重覆盖。
- `npm run check:404` 不只看字符串：它把产物里的内联脚本抠出来，用桩对象
  实际跑一遍后台路径与网站路径，确认只有后台路径被打上标记、标题被改写、请求路径被回填。

### 静态导出与子路径

- `next.config.ts` 设置 `output: "export"` + `trailingSlash: true`，导出目录形式的 URL
  （`/courses/` → `courses/index.html`），并关闭 `next/image` 优化（静态托管无优化服务）。
- 项目站点位于 `/<repo>/` 子路径，前缀通过 `NEXT_PUBLIC_BASE_PATH` 在**构建时**注入：
  `NEXT_PUBLIC_BASE_PATH=/NexGenEdu npm run build`。CI 中使用
  `${{ github.event.repository.name }}` 动态取得仓库名，fork 后无需修改。
- **本地开发绝对不要设置该变量**，否则 `localhost:3000` 会被重定向到 `/NexGenEdu`。
- 产物中的 `_next/` 目录以 `_` 开头，会被 GitHub Pages 的 Jekyll 处理忽略，
  因此工作流会写入 `out/.nojekyll`。

### 静态导出的架构含义

**宣传网站**的所有数据必须在构建时可得，所以内容仍然留在文件里（Markdown 数据层在构建期读取），
将来这部分接 PostgreSQL 时数据层签名也不变。

**教务后台**则已经从这条约束里出来了：后台的数据与服务端逻辑在本机 Node + SQLite 上跑，
前端在 `NEXT_PUBLIC_API_BASE` 有值时才把调用指向它（见第 8 节「教务后台的后端架构」）。
因此：

- **本机使用时，排课、课时扣减这些写操作已经可以跨设备共享了** —— 数据在服务端的库里，
  换台电脑连同一个后端看到的是同一份数据（接后端之前，数据存在访问者各自的浏览器存储里，
  换个设备就等于换一份）；
- **线上那份静态站不同**：构建时没有配后端地址，后台连不上、也登录不了，
  点进去显示「后台需要本机后端」（见 7.1 节）。线上只有给家长看的宣传页在工作；
- **静态导出与"数据必须构建期可得"这条约束仍然管着宣传页** —— 后台改了价、
  改了课程库，不会自己出现在线上，仍然要导出内容替换进 `data/site/*.md` 再重新构建。

## 8. 关键设计决策记录

- **不引入组件库**：UI 需求克制（卡片、按钮、徽章、表格），自建组件可保持体积与设计语言可控。
- **宣传网站与后台共用设计语言，但信息密度不同**：`(site)` 路由组大留白、大标题；`admin` 路由组紧凑、表格优先。
- **`app/(site)` 路由组**：让宣传网站与后台拥有各自独立的 layout，互不干扰。
- **`cn()` 不做 Tailwind 冲突消解**：保持零依赖与可预测性，约定调用方 `className` 最后拼接。
- **`next.config.ts` 显式设置 `outputFileTracingRoot`**：本机 HOME 下存在其它 lockfile，需固定项目根目录。

### 教务后台的后端架构（**已从「伪后端」接到真实后端**）

> 这一节分两半：**上半是当时**（服务端还没有，后台按「将来会有服务端」的样子写），
> 保留下来是因为它解释了这套分层为什么长成这样；**下半是现状**。看代码请以下半为准。

**当时（伪后端阶段）**：服务端还没有，但后台的所有页面都按「将来会有服务端」的样子写：

```
页面（app/admin/*，客户端组件）
   ↓ 只调用这一层，方法签名与 REST 一致
lib/backend/api.ts        list / get / create / update / remove / today / reset
   ↓ 读写一份 JSON 快照
lib/backend/storage.ts    KeyValueStore 抽象：浏览器里是 localStorage，Node 里是内存
   ↓ 首次访问灌入
lib/backend/seed.ts       示例数据（教师与教室取自站点真实数据，学生/排课为示例）
lib/backend/pricing.ts    报价：计价模型、公式与规则（价格是数据，不是常量）
```

当时写的三个设计要点，逐条对照现在的结果：

- **异步到底**：所有方法都是 `async` 并带轻微延迟，页面上不会出现「同步读 localStorage」
  这种接服务端时必须重写的写法 —— **这一条兑现了**。正因为它本来就没有同步写法，
  后来换成远端代理时页面一行都没改（代理返回的本来就是 Promise）；
- **存储可替换**：`KeyValueStore` 让同一套服务层既能在浏览器里跑（localStorage），
  也能在 Node 里跑（内存）—— **这一条也兑现了**，而且代价比预想的还小：
  接后端时只多写了一个 SQLite 版实现（`server/kv-store.mts`，整库当一份 JSON 存进 `kv` 表），
  业务代码没有改动。（`lib/backend/storage.ts` 那套本地实现仍在代码里 ——
  `npm run check:both` 的**第一遍**（内存后端）跑的就是它，第二遍才是真实 HTTP 后端。）
- **换真后端时**：只替换 `lib/backend/api.ts` 的实现（改成 `fetch`），`types.ts` 与
  全部页面代码不动 —— **结论对了一半，做法和这里预想的完全不同，值得记下来**：
  `api.ts` 并没有被改写成 fetch 版。那样等于把方法清单与参数再抄一遍，
  少一个方法、错一个字段都是**静默**出错。实际做法是加一层**代理**
  （`lib/backend/remote.ts`）：它遍历本地 `api` 的结构，把每个方法换成 `POST /api/call`，
  方法名、参数个数、返回类型全部与本地一致。**页面代码确实一行没改**，连类型都没改 ——
  `export type BackendApi = typeof api` 这条约束同时管住了代理（代理必须与本地同形）。

**现状（接后端之后）**：

```
页面（app/admin/*，客户端组件）—— 与伪后端阶段是同一份代码
   ↓ 只调用 api 这一层
lib/backend/api.ts（业务口径的唯一实现：课时、金额、冲突判定、报价都在这里）
   ↓ 本机使用时 api 被换成一个代理（只有 NEXT_PUBLIC_API_BASE 有值才换）
lib/backend/remote.ts     POST /api/call { method, args } + Bearer 令牌
   ↓ HTTP（默认 127.0.0.1，见「登录」一节）
server/index.mts          登录 / 会话闸门 / 调用分发 / 自动备份
   ↓ 同一个 KeyValueStore 接口，另一个实现
server/kv-store.mts       SQLite：kv 表存整份 JSON 快照（server/data/nexgenedu.db，不进 git）
```

最要紧的一点是**业务口径只有一份实现**：服务端跑的就是 `lib/backend/api.ts` 这个文件，
不是"照着它再写一遍"。所以"课时怎么扣、金额怎么算、冲突怎么判、报价怎么算"
不存在两个版本 —— 这正是 `npm run check:both` 在做的事：同一套断言对
「本地内存实现」与「真实 HTTP 后端」各跑一遍，两边必须一致。换后端没改口径，
靠的不是人记得，是这条自检。

**数据边界（现状）**：

| 场景 | 数据存在哪 | 换设备 / 清缓存会怎样 |
| --- | --- | --- |
| 本机使用（配了 `NEXT_PUBLIC_API_BASE`） | 服务端 SQLite（整库一份快照） | 不会丢；后端每天自动备份一份 |
| 线上静态站（没配后端） | **没有能存数据的地方**，后台也无法登录 | 后台根本用不了，点进去是一句「后台需要本机后端」的说明 |

（**历史**：伪后端阶段数据只存在访问者当前这一份浏览器存储里，换设备看不到、清缓存会丢、
多标签页并发写入以最后写入为准；那时界面上有「示例数据」提示条与一键重置入口。
接后端之后这两处都改了：`components/admin/DataNotice.tsx` 现在显示的是**连没连上后端**，
**一键重置也去掉了** —— 它在真实数据上是个随时可能误触的破坏性按钮，
而它想解决的"从头来过"用「数据与备份 → 导入空库」就够了，那条路还会先自动备份。）

仍然存在的边界（不是 bug，是当前范围）：**会话不落盘（后端重启要重登）、角色是登录时的快照、
跨记录的事务性只有「整份落盘」这一层、换多进程要换 Postgres**；
（多账号与权限区分、同一条记录的乐观锁、单写者锁**都已经做了**）
存储是**快照式**的（整库当一份 JSON 存着，没有按行查询）。
将来要多人同时用、要按行查数据，才需要把 KeyValueStore 换成真正的表结构（见「后端开发方案」）。

### 后台模块现状

| 模块 | 路径 | 状态 |
| --- | --- | --- |
| 咨询 | `/admin/inquiries` | ✅ 家长咨询登记 + 排课可行性（含最接近方案与协调已有课） |
| 今日概览 | `/admin` | ✅ 今日课程 / 教室占用 / 课时预警 / 档案规模 |
| 学生 | `/admin/students` | ✅ 列表 + 搜索 + 建档 / 编辑 / 删除 + 详情三个页签（报课与课时 / 信息采集表 / 排课记录） |
| 教师 | `/admin/teachers` | ✅ 列表 + 在职开关 + 排课清单 |
| 教室 | `/admin/classrooms` | ✅ 用途（上课用教室 / 自习室）、容量、**可用时段**、占用情况 |
| 课程库 | `/admin/courses` | ✅ 课程台账（网站课程 + 后台新增）、可开班型、状态；排课与教师科目都取自这里 |
| 课程安排 | `/admin/lessons` | ✅ 按天排课 + 冲突检测 + 标记已上（按出勤扣课时）+ 待补课与安排补课 |
| 日历 | `/admin/calendar` | ✅ 按周查看排课密度与空档（只读，避免两处可改） |
| 课表与占用 | `/admin/timetable` | ✅ 教师课表 / 教室占用两个视角，按周切换 + 导出 ICS |
| 统计 | `/admin/stats` | ✅ 教室利用率与空档、时段分布、教师课时、退课与流失 |
| 待跟进 | `/admin/followups` | ✅ 自动挑出需联系家长的学生，附可直接复制的话术草稿 |
| 收费 | `/admin/finance` | ✅ 本月收款/退款/净收入、按方式拆分、欠费清单、退费口径说明 |
| 报价 | `/admin/pricing` | ✅ 基础价 / 系数 / 计费规则 / 教师分成可改 + 家长报测试算 + 教师课时费与人数对照表 + 导出上线 |
| 数据与备份 | `/admin/data` | ✅ 全量 JSON 导出/导入（含备份后悔药）、学生名单 CSV、课程 ICS、操作日志 |
| 账号 | `/admin/accounts` | ✅ 只有技术管理员能进：加人、改角色（多选）、绑定教师（下拉选人）、重置口令、停用 / 删除，改完立刻生效（服务端四条独立路由 `/api/accounts`，不在 `/api/call` 里） |

共用件：`components/admin/AdminFields.tsx`（后台表单控件）、
`components/admin/DataNotice.tsx`（**连没连上后端**的状态提示条；接后端之前它显示的是
「示例数据」提示与一键重置）、`lib/backend/format.ts`（日期时间格式化）。

### 接口契约与转真后端（`lib/backend/contract.ts` + `docs/后台API约定.md`）

**接口契约**是接真后端之前最重要的产出之一 —— 它按「服务端怎么做」把全部方法分成七类，
而不是平铺一张表（平铺只会让实现者挨个猜「这个要不要事务」）：

| 分组 | 方法数 | 服务端的做法 |
| --- | --- | --- |
| 通用 CRUD | 45 | 9 个资源 × 5 个方法，一套 REST 即可；id 由服务端生成 |
| 关联查询 | 18 | 查询参数，不要前端全表过滤 |
| 业务动作 | 12 | **必须一个事务**（报课要同时落报课记录 + 收款 + 课时流水） |
| 咨询线索 | 8 | 判定是纯函数；落库前必须再复核一次（判定是「看」、落库是「改」） |
| 报价配置 | 5 | 价格是数据；报价只收「选择」不收价格；公式只有一份实现 |
| 看板与统计 | 8 | 只读，算法是纯函数可原样搬走；口径不要改 |
| 运维与审计 | 9 | 导入保持三道保险；按需导出照数据集清单实现；日志改为服务端不可篡改的审计表 |

`docs/后台API约定.md` 里另有两份清单：

- **服务端必须自己复核的 12 项校验**（冲突、容量、幂等、课时非负、金额不变式、
  退费重算、请假判定用服务端时间、**报价配置合法性**、**报价金额由服务端算**、
  鉴权、审计）—— 每一项都对应一个已经踩过的坑；
- **迁移六步**（冻结格式 → 实现接口 → 前端只改 `api.ts` → 搬数据 → 换登录 → 退出条件）——
  **前五步都走完了**：接口由服务端实现、数据搬进了服务端库、登录换成了服务端会话。
  第六步（退出条件）**大部分成立、有一处要打折不装作成立**：服务端确实通过了同一套自检
  口径（这正是 `npm run check:both` 在证明的），前端也不再直接读**业务数据**；
  但"前端不再有任何一处直接读 localStorage"这句按字面还没做到 ——
  **登录令牌仍然存在 localStorage 里**（`lib/auth/token.ts`）。那是登录态、不是业务数据，
  仍然要记在这里，免得下次有人拿这条当"已经彻底不用浏览器存储"的证据。

**文档不会过期**：`npm run check` 会双向比对「契约清单 ↔ `api.ts` 真实形状」，
并按边界匹配检查文档提到了每一个方法（用子串会被 `lessons.list` /
`lessons.listByDate` 这类前缀蒙过去）。加了方法不更新文档，自检就红。

接后端这件事的实际改动点与这里原先的预想不同：**页面代码一行没改、类型没改，
但被替换的不是 `api.ts` 本身，而是它外面的那一层代理**（`lib/backend/remote.ts`）——
原因见本节上面「教务后台的后端架构」。

还有一处要分清：**上面那张表的「服务端的做法」一列是按"要写一个 REST 服务端"写的设想**，
而实际落地的是**路线 B** —— 页面走一个 `POST /api/call` 入口、服务端直接跑同一份 `api.ts`。
REST 那一套并没有被丢掉：服务端里确实有一批 `/api/students`、`/api/pricing/…` 形式的接口，
但它们是**路线 A 的参考实现**（页面不用它们，留着是为了"真要写 REST 时该长什么样"有个样子，
它们和 `/api/call` 一样受登录闸门保护）。所以右列那些建议（一套 REST、
查询参数不要前端全表过滤、业务动作必须一个事务）应当读成**另一种做法的备选**，
而不是现状描述；分组本身（哪些是 CRUD、哪些是业务动作、哪些必须复核）仍然有用。

### 咨询可行性（`lib/backend/inquiry.ts` + `/admin/inquiries`，数据库 v9）

家长口头问「每周六上午十点、指定陈老师，能不能排」——这类问题要**当场**给答复，
答错了要么白答应（事后排不出课），要么白白推掉一个学生。因此判定写成可测的纯函数。

判定的三条要点：

1. **检查的是一串日期，不是一天**：每周一次 × 12 节 = 未来 12 个同一时段都得空着。
   只看第一次是这个功能最容易犯的错（自检专门有一条「系列中途撞课」的用例）；
2. **候选时段是「备选」而不是「全要」**：家长给两个时段是「哪个都行」，
   因此依次尝试，第一个能排下的就用它；
3. **不可行要说清「挡路的是谁」**：不是「不行」，而是「9月20日 17:30 陈老师在上
   另一节课（学生：张小明）」—— 因为接着要决定是让新学生换时段，
   还是去协调那位已有学生。

最接近的方案按「改动最小」排序：同一天改时间 → 邻日同时段 → 换老师 → 换教室；
每条都用同一套判定复核，给出来的方案一定是真能排的。

配套的三处服务端约束：

- `inquiries.accept` 落库前**再复核一次**：判定是「看」、落库是「改」，
  中间的时间差不能忽略；通过后一次性建出整串课（不是一节一节点）；
- `inquiries.abandon` 记原因，线索状态为待确认 / 已安排 / 已放弃；
- `lessons.suggestMoves` 给挡路的已有课算出可挪的时间（用于「协调已有学生」），
  且**界面必须显示受影响的已有学生**再让管理员决定动不动它，绝不静默改动别人的课。

跳过周（假期、考试周）在生成日期串时跳过并**往后顺延**，总节数不变 ——
与线下机构「报 12 节就是 12 节」的约定一致。

界面（`/admin/inquiries`）：

- **登记表**：学生信息 / 科目 / 任意时段（周几 + 时间）/ 每周或每两周 / 时长 / 节数 /
  起始日期 / 指定教师或「不限」/ 指定场地或「不限」/ 跳过哪些日期；
  时段旁边给一排**学校标准时段**的快捷按钮，那些值直接从「时间安排」页的内容里读
  （`getStandardSlots()`），不另抄一份；
- **可行性报告**：每个候选取一行，可行就显示推荐教师 + 场地 + 前几次日期并可
  「采用这个方案」（一次性建出整串课）；不可行就说清挡路的是谁；
- **两个方向都摆在眼前**：新学生换时段（采用替代方案），或已有课挪走
  （点「协调已有学生」→ 列出 `suggestMoves()` 算出的可挪时间）。
  **移动前先显示受影响的已有学生**，决定权在人，系统不替他决定；
- **线索列表**：待确认 / 已安排 / 已放弃三种状态，放弃时可写原因（记进备注）。

端到端主线（自检里串起来走了一遍）：**时段被已有课挡住 → 报告指出挡路的是谁 →
把已有课挪到系统算出的时间 → 再判定通过**。挪课会写操作日志 ——
`lessons.update` 是自定义覆盖过的方法（为了处理课时撤销），不会自动记日志，
因此它自己写；「不静默改动别人的课」这句话得有日志兜着。

### 全局搜索与操作日志

**全局搜索**（后台顶栏，`lib/backend/search.ts` 纯函数）：一个框搜五类对象 ——
学生 / 教师 / 教室 / 排课 / 课程，按类别分组，点进去直达。

- 匹配规则是**不区分大小写的子串**，刻意不做拼音/模糊匹配：后台里「张」和「章」
  是两个学生，**宁可搜不到也不要搜错人**；
- 结果带上参数直达：学生 → `/admin/students?studentId=…`（自动展开详情）、
  排课 → `/admin/lessons?date=…`（自动切到那一天）。两个页面读参数用
  `window.location` 而不是 `useSearchParams` —— 后者在静态导出下会触发
  CSR bailout，把整页踢出预渲染的 HTML；
- 空关键词返回空结果，不把全部数据倒出来。

**操作日志**（`/admin/data` 页，数据库 v8）：记录「谁在什么时候改了什么」。

- **写在服务层，不由页面负责**：各业务方法自己调用 `writeLog()`，
  通用集合（学生 / 教师 / 教室 / 排课 / 作业 / 测评）自动记录新建、修改、删除，
  自定义业务动作（报课 / 续费 / 退课 / 标记已上 / 课堂记录 / 填采集表 /
  导入 / 恢复 / 重置）各自写更具体的摘要。依赖调用方自觉写日志一定会漏；
- 修改日志会记下**改了哪些字段** —— 只说「修改了学生」等于没说；
- **条数上限 500**（旧的丢弃）：日志本身也占存储，无上限地涨下去，
  几年后一份 JSON 会大到导不出来；
- 操作人**由服务端按会话决定**。**历史**：伪后端阶段是"后台外壳在登录后调用
  `api.setOperator(name)` 把操作人写给服务层"，而 `setOperator` 是个**同步**方法 ——
  经远端代理一包就静默变成 Promise，写进去的永远是默认值（前端"说了"但没生效）。
  现在操作人取自服务端会话，前端说了不算，这类"看起来写了、其实没写"的坑也就没有了；
- 日志现在随整库存在**服务端**（`/admin/data` 页面照旧可看），
  但**仍然是快照的一部分、不是独立的只追加审计表**：原始计划里写的"接服务端后
  改为服务端不可篡改的审计表"只做了一半 —— 操作人不可由前端伪造了，
  可日志本身仍会随整库导入导出一起被覆盖。要做成真正的审计表，得等存储从
  "整库一份 JSON 快照"换成按行存储。

### 经营统计（`/admin/stats`）

三个数字回答的是**决策问题**，不是「给老板看的报表」（算法在 `lib/backend/stats.ts`，纯函数）：

| 统计 | 回答什么 | 口径要点 |
| --- | --- | --- |
| 教室利用率 | 这间教室值不值？该加教室，还是把课挪到别的时段？ | 可用时长优先按教室自己的「可用时段」；没设时段的按**上课时间**估算（每天 8:00–22:00 共 14 小时，不是营业时间）。**只算未取消的课** |
| 时段分布 | 哪个时段最挤、哪个时段空 | 按小时统计课次；最忙时段排满了 → 加时段比加教室更有效 |
| 教师课时 | 谁排满了、谁还空着 | 课次 / 时长 / 涉及学生数（去重）/ 平均每节人数 / 按科目拆分 |
| 退课与流失 | 学生在什么阶段退、原因集中在哪 | 按**退掉的课时**算而不是按条数；重点是「平均已上比例」——若集中在两三成，问题在入门期 |

「空档日」只统计**有可用时段却没排课**的日子：周六日不在某间教室的可用时段内，
不算它的空档 —— 否则每间教室每周都显示两天「空档」，看着像浪费。

### 请假与补课（数据库 v7）

口径：**一节课扣一次，扣的是「这次时间被占用」**（`lib/backend/attendance.ts`）。

| 情况 | 是否占用时间 | 扣课时 |
| --- | --- | --- |
| 到课 | 是 | 扣 1 |
| **提前 ≥ 24 小时请假** | 否（机构来得及另排） | **不扣** |
| 临时缺课 / 旷课 / 请假但未记录时间 | 是 | 扣 1 |
| 补课 | 是（重新占用教师与教室） | 扣 1 |

于是：**提前请假 + 补课 = 扣 1 节**（等于正常上课）；
**临时缺课 + 补课 = 扣 2 节**（缺课占一次、补课又占一次）——
这正是 24 小时规则的威慑力所在，也方便向家长解释。

- 课堂记录因此多了一个**请假时间**字段：只记「请假」是不够的，
  得知道提前了多久才能判断扣不扣；
- **没记录请假时间按临时缺课处理**（不默认成对机构有利的解释），
  界面提示「建议补录」，补录后课时自动调整；
- **对账而不是一次性扣减**：出勤是会变的（先标到课、后发现是提前请假），
  因此 `reconcileCharge()` 让「已扣」与「应扣」保持一致 ——
  该扣没扣就补扣、不该扣却扣了就按流水退回，**幂等**（重复保存不会重复扣）；
- **待补课清单**（课程安排页）：列出请假/旷课但还没补的学生，
  一键安排补课；补课以原课为模板建一节新课，走同一套冲突检查；
  补课被取消就重新回到清单，原课被取消则不再要求补课。

### 常见问题（`data/site/faq.md`）

八个分组、43 条问题（按家长找答案的路径排）：

```
试课与报名 → 课时与收费 → 班级与排课 → 请假与补课 →
老师与教学 → 学习过程与反馈 → 服务形式 → 特殊情况
```

写答案时有两条约定（写在文件顶部，提醒后来的人）：

1. **只写有依据的说法**：价格、规则、承诺都要能对上站内其它内容
   （例如「报满 10 节试课免费」对得上报价页，「提前 24 小时请假不扣课时」
   对得上后台的请假规则）；
2. **拿不准就写「以咨询确认为准」**，不要为了好看编一个数字。

`请假与补课` 这一组直接来自后台确认过的规则；`老师与教学` 里说明了
「AI 学习伙伴是辅助工具、授课仍由教师完成」，避免家长误会成机器人上课。

### 待跟进清单与家长话术（`/admin/followups`）

这些信号本来就躺在数据里（课时、欠费、作业、测评、出勤、排课），
但要靠人一样样去翻 —— 翻的结果通常是「想起来才看」。这一页的价值是**不用想**：
打开就是今天该联系谁，以及可以直接复制的话术。

六条规则与阈值集中在 `lib/backend/followup.ts` 的 `FOLLOWUP_RULES`：

| 规则 | 触发条件 | 严重度 |
| --- | --- | --- |
| 课时不足 | 剩余 ≤ 5 节（≤ 2 节为紧急） | 提醒 / 紧急 |
| 欠费 | 约定应缴 > 实收 | 紧急 |
| 作业异常 | 最近 5 次里未交 ≥ 2 或非按时 ≥ 3 | 提醒 |
| 测评下滑 | 同科目最近一次低于上一次（≥10 分为提醒） | 提醒 / 关注 |
| 出勤异常 | 最近 5 次里旷课 ≥ 1 或请假 ≥ 2 | 提醒 / 关注 |
| 久未排课 | 有课时但未来 7 天没课（已取消不算） | 关注 |

三条原则：**规则与阈值集中一处**（要调松紧只改这里）；
**话术是草稿**（数字已填好，发之前请自己看一遍，不给家长发机器味的话）；
**不猜**（没有测评、没有作业记录时不产生条目，而不是把「没有记录」当「表现不好」）。

规则引擎是纯函数，因此自检能逐条造数据验证：命中、不命中、以及边界
（课时刚好卡在阈值、已退课的报课不参与、已取消的课不算排课、结课学生跳过）。

### 话术专区（`/admin/scripts`，`lib/backend/scripts.ts`）

机构日常要说的话按 **11 个场景组、35 条**整理成可直接复制的草稿：首次咨询与接待、
报价与价格说明、试课与转化、排课与时间协调、请假与调课、续费与课时提醒、欠费与收款、
学习反馈与成绩波动、投诉与情绪处理、退费与流失挽回、教师相关。

**为什么做成代码里的模板，而不是像报价那样做成可编辑数据？**

话术里含**口径**（提前 24 小时请假不扣课时、临时缺课扣 1 节、缺课+补课=扣 2 节、
试课满 10 节免费否则按原价收 1 节、只报 1 节加收 10%、两种退费口径）。这些口径的
权威实现在别处（`attendance.ts` / `finance.ts` / `pricing.ts`），话术与它们**必须同步改**：
口径改了话术没改，就会出现「系统按新规则扣课时、老师按旧话术跟家长承诺」——
那是纠纷。所以当前阶段让话术**随版本走**：改口径的提交里一起改话术，
`npm run check` 会盯着关键数字（24 小时 / 10 节 / 按实付 / 1 节 +10% / 8:00–22:00），
以及「内部信息不得出现」这条红线。

等机构要自己改文案时，再照报价配置的做法（数据 + 后台可编辑 + 导出）把它变成数据 ——
那时机是「口径稳定、机构想改措辞」，而不是现在。

**一条红线：内部信息不进话术。** 教师分成比例、教师课时费、机构留存、其他学生的信息
一条都不能出现在话术里（这些话是发给家长的）。自检里有断言逐条扫描关键词，
不是靠自觉，是机器拦。

页面上还能**套用某个学生**：选一名学生（取 `api.students.list()`），把
`{学生}` `{科目}` `{剩余课时}` 按真实数据填好再复制；老师、时间、金额这类
因场景而异的值**刻意留空**并显眼提示「还需要自己补哪几项」——
猜错比空着更糟（把 `{老师}` 猜成某位老师发出去就是错的）。占位符缺值时**原样保留**，
不会悄悄变成空白。

### 收费与金额（数据库 v6）

钱的规则集中在 `lib/backend/finance.ts`（纯函数），因为「金额怎么算」是最容易
各处口径不一的地方：报课表单算一次、对账单算一次、退费再算一次，迟早对不上。

三个金额概念**刻意分开**（一次报课可能分期付款，也可能给优惠）：

| 概念 | 含义 |
| --- | --- |
| 标价 | 总课时 × 单价：挂牌价，用来展示「优惠了多少」 |
| 约定应缴 | 与家长谈定的总额：可低于标价（优惠），也可高于实收（分期未付） |
| 实收 | 实际到账累计，**由收款流水累加得出**，不手工维护 |

推出：优惠 = 标价 − 约定应缴；欠费 = 约定应缴 − 实收。

- **收款与课时是两本账**：一次报课可以课时一次给、钱分几次收；也有纯补款。
  因此收款记录独立成表（`payments`），报课的 `paidAmount` 由它累加。
  **不变式（自检逐条校验）**：每条报课的实收 = 其收款合计 − 退款合计。
- **退费是策略，不是写死的公式**：`REFUND_POLICIES` 里现在有两条 ——
  「按实付比例退」（默认，最好向家长解释）与「追回已上课时的标价」
  （防上完就退）。退课前先在界面上看到金额与公式再确认；要换成实际规则，
  改这一个数组即可，服务层与界面都从那里取。
- **欠费不按月切分**：它是一笔「还没收到的钱」，跨月存在，
  按月切会让 1 月欠的费在 2 月报表里凭空消失。
- 迁移（v5 → v6）给老报课记录补金额字段，**一律记 0 而不是猜**：
  猜一个单价会让欠费清单凭空冒出金额，比留空危险得多。

### 报价在后端（`lib/backend/pricing.ts` + `/admin/pricing`，数据库 v11）

报价原先只有宣传页上那个计算器：公式在页面侧（`lib/pricing/quote.ts`），
手续费 10% 与「满 10 节试课免费」还是**硬编码的常量**。结果是调一次价要改代码、
重新构建，家长打电话来时也只能切到宣传页点一遍。现在价格是数据、公式在后端一侧。

**计价模型**（每个系数各管一件事，互不重叠 —— 这是「基础价、系数是什么意思」的答案）：

| 维度 | 字段 | 含义 | 默认 |
| --- | --- | --- | --- |
| 基础价 | `basePrice` | **每小时的价（元 / 小时）**：一对一、报 2 节及以上；课单价（元 / 节）= 它 × 每节课几小时 × 人数系数 | 分阶段设定 |
| 人数系数 | `classType.coefficient` | 人越多每人越便宜（讲的是**人数**，不是"班级"；E10 之前叫「班级系数」，含义与数值都没变） | 一对二 0.7、一对三 0.6、一对多 0.5 |
| 时长乘数 | `duration.multiplier` | 一节课上多久，1 小时为基准 | 1 / 1.5 / 2 小时 → 1.0 / 1.5 / 2.0 |
| 手续费 | `rules.singleLessonFeePercent` | 只报 1 节时加收（一次课不划算） | 10% |
| 试课 | `rules.freeTrialMinLessons` | 报满多少节后试课免费，否则按原价收 1 节 | 10 节 |

两个**不是乘系数**的例外：班课按「教师课时总费用 ÷ 班级人数」分摊；
试课费永远按课程**基础价原价**收（不带人数系数、时长与手续费）。

**教师分成（课时费）**也在这一份配置里：课内按系数计价的班型（一对一定制课 /
一对二 / 一对三小组课 / 一对多小班课，1–8 人）适用

```
教师课时费 = 小时数 × (课程单价 / 小时) × (0.4 + (学生人数 − 1) × 0.1)
```

即「第一名学生 40%，每多一名学生加 10 个百分点」（8 人 110%）；9 人以上大班课不适用，
按「教师课时总费用 ÷ 班级人数」另议。公式里的「课程单价」默认取课程标准单价
（就是**基础价**本身，不含人数折扣），也可切换为班型课时价（**基础价 × 人数系数**）—— 后台一键切换，
切换不影响家长看到的报价。算法与人话说明在 `lib/backend/teacher-share.ts`，
做成「规则原文 + 人话三条 + 人数对照表」，后台报价页直接展示，
`api.pricing.teacherFee` 负责算（教师工资与报价一样，只收选择、不收金额）。

**代码分工**：

- `lib/backend/pricing.ts`：配置类型、公式（`calculateQuote`）、配置校验、
  按名字解析选择、Markdown 导出 —— 公式只有这一份实现；
- `lib/pricing/quote.ts`：只剩一层薄适配（取站点内容的规则转发给后端公式），
  页面（`EstimateForm`、`/quote`）的调用方式完全不变；
- `api.pricing.{get,update,reset,quote,teacherFee,exportMarkdown}`：后台服务接口，
  `quote` **只收「选了哪门课、哪个班型、多少节」，不收价格** —— 价格一律由服务端查；
- `data/site/pricing.md`：新增「计费规则」分组，科目支持 `物理 ×1.1` 写法（省略即按 1，老内容不用改）。**这一条已经作废**：科目这一维在 E10 里整个删掉了，`物理 ×1.1` 这种写法不再有。

**后台与线上报价页之间的边界（重要，接后端之后依然成立）**：报价配置存在**服务端数据库**里，
而家长看到的报价页是**构建期读内容文件**的静态页面 —— 服务端库在构建时读不到、
线上也连不上，所以后台改完价**不会自动上线**：必须点「导出配置」，
把导出的片段替换进 `data/site/pricing.md`（「## 学习阶段」及其后的部分，页面文案字段不要动），
再重新构建发布。
（**历史**：这句话原先给的理由是"后台改的价格困在本地存储里，构建期根本读不到"。
**已经迁移**：结论没变，但原因换了 —— 现在卡住的是静态导出这个形态，
不是存储位置。把新旧理由都写下来，免得下次误以为"接了后端就自动上线了"。）
后台报价页顶部把这件事写在最显眼的位置，`api.pricing.get()` 返回的 `source`
也会区分「站点内容」与「后台修改（未导出上线）」。

**自检盯住的事**（这一组最容易出的事故是「宣传页一个价、后台另一个价」）：

- 同一方案在**前台公式与后台服务上必须算出同一个数**（7 个用例：含 1 节手续费、
  满门槛试课免费、班课人数分摊、2 小时、未开放课程、节数为 0 的报错）；
- 人数系数解析（每个班型一行、`系数` 写数字或 `按人数分摊`），缺「计费规则」分组时用默认规则；
- 坏配置进不了库（人数系数 0、手续费 120%、负的基础价、课程重名），且被拒后库里配置不变；
- 改价真的影响报价，并留下「哪门课从多少改到多少」的操作日志；
- 导出的 Markdown **回读后与后台配置完全一致** —— 导出的必须是能用的内容，
  而不是一段看起来像的文本；
- v9 老库升级后按站点内容补齐报价配置，且价格与升级前一致（升级不会让任何一节课变价）。

### 按需导出（`lib/backend/export.ts`，数据集 × 选中的行 × 格式）

原先只有三个固定出口（全部数据 JSON、学生名单 CSV、全部课程 ICS），于是「把这学期围棋课的
排课导出来发给老师」只能导全量再自己筛。现在导出是三段式：

| 维度 | 说明 |
| --- | --- |
| 数据集 | 学生 / 排课 / 教师 / 教室 / 课程 / 报课记录 / 收款记录 / 课堂记录（清单在 `EXPORT_DATASETS`，加实体只改这一处，界面与自检都从它取） |
| 选中的行 | `ids` 为空 = **全选**；传了 = 只导这几条 |
| 格式 | CSV / JSON 对所有数据集；**排课**另有 ICS（进手机日历）、**课程**另有 Markdown（粘回内容文件） |

几个刻意的决定：

- **CSV 列是「人看的」**：表头写中文，内容里把 id 换成名字（教室 id → 「301 教室」）——
  导出是给 Excel 和别的系统看的，一列 uuid 没有意义；
- **结果必须区分「导了 M 条」与「共 N 条」**：只报「已导出」会让人以为导全了；
- **不支持就报错**，不产生空文件（例如给「教师」导 ICS 会被拒绝并说明原因）；
- **只读**：导出不改任何数据（自检里跑完一轮导出会比对数据是否完全一致）；
- 复用了 `backup.ts` 已有的 CSV（带 BOM、转义）与 ICS（RFC 5545 转义 + 折行）实现，
  不另写一套 —— 两套实现迟早会在某个转义细节上分叉。

### 导出与备份（`/admin/data`）

这一组不只是「导出一下」，它承担三件事：

1. **留一份能拿在手上的数据** —— 备份现在由服务端负责（每天自动一份，见下一节），
   导出解决的是另一类需求：留档、交给别人、在别的机器上用、以及"库这一层出了问题"
   时手边还有一份能读的 JSON。**已经迁移**：接后端之前导出的意义是"唯一的保险"
   （那时数据只在用它的那一台机器上，换个设备就是另一份）；现在它不是唯一保险了，
   但也不是可有可无 —— 自动备份和库在同一个地方，"库连同备份一起没了"是它唯一挡不住的情况；
2. **迁移与搬库**：导出 JSON → 在服务端导入，这条路径**已经用过了**
   （`npm run server:import`），现在它同时是"从别处搬一份数据进来"和"恢复某一份旧留档"的入口；
3. **导入是唯一能一次性毁掉全部数据的操作**，因此配了三道保险：

| 保险 | 做法 |
| --- | --- |
| 结构校验 | 不是对象 / 缺 `version` / 版本比程序新 / 缺核心表 / 记录缺 `id` → **直接拒绝，不碰现有数据** |
| 导入前自动备份 | 当前数据先写一份快照（键名仍叫 `nexgenedu.admin.db.backup.v1`，现在存在服务端库里），界面上出现「恢复导入前的数据」 |
| 自动升级 | 旧版本文件先过 `migrate()` 再替换，提示会写明 `v2 → v5` 这类升级路径 |

刻意**不做业务校验**：报课科目为空之类的问题照常导进来、由界面暴露，
否则一条脏数据就会让人无法修复数据。结构不对才拒收。

导出格式：JSON（完整备份）/ CSV（学生名单，**带 BOM**，否则 Excel 打开中文乱码）/
ICS（日历）。ICS 里 UID 用 `lesson-<id>@nexgenedu`，重复导入不会产生副本；
时间用本地时间（不带 Z）；文本按 RFC 5545 转义并折行。

教师课表也可以在「课表与占用」页单独导出（选中某位老师 → 导出本周课表 ICS），
老师导进手机日历就不用回后台查课。

### 服务端备份（`server/backup.mts` + `server/backup-cli.mts`）

数据搬到服务端之后，"备份"从"记得导出"变成了**后端自己每天做一次**：

- **每天自动一份**：后端启动时会补齐当天缺的那一份，运行期间每小时再检查一次 ——
  机构不会每天都点一次按钮，所以这件事不能靠人记得；临时库（自检、演练、
  逐页验收）用 `NEXGENEDU_NO_BACKUP=1` 关掉，免得测试数据混进正式备份目录；
- **保留份数默认 90**（约三个月的回溯窗口，`NEXGENEDU_BACKUP_KEEP` 可改）：
  库是 KB 级，90 份也就十几 MB，宁多勿少 —— 真正要防的是**无上限**（目录无声涨到没人管）；
- **文件名是唯一的时间真相**：`nexgenedu-YYYY-MM-DD-HH-mm-ss.db`（本地时间），
  排序与"哪份最新"一律以**文件名里的时间戳**为准，**不用 mtime** ——
  这条是恢复演练逼出来的：复制、移动、rsync、从别处恢复都会改写 mtime，
  演练里正是按 mtime 排序把两份真备份当成"最旧的"清掉了；
- **只清理符合命名规则的文件**：目录里手放的 JSON 留档（例如某次的空库、某次的清理前快照）
  不会被自动清理碰到；
- **手动**：`npm run server:backup`（`-- --list` 列出现有备份、`-- --force` 立刻再备一份）；
- **恢复演练**：`npm run drill:restore` 走一整圈 —— 备份 → 先验备份文件本身 →
  **删库** → 只靠备份文件恢复 → 核对条数、金额、课时账本一致。
  备份最怕的是"以为有、真要用时打不开"，所以这一步刻意做成可重复执行的自检，
  而不是一份"记得每季度演练一次"的流程文档。

### 怎么用（后台）

**本机使用**要跑两个进程：`npm run server`（后端，默认 `127.0.0.1:4000`）与 `npm run dev`
（前端，`localhost:3000`），并在 `.env.local` 里设 `NEXT_PUBLIC_API_BASE` 指后端。
然后访问 `/admin` → 用账号 `admin` 与**后端启动时打印的那次口令**登录
（口令由服务端生成/持有，见下面「登录：从假登录到服务端会话」）。

**线上那份不一样**：`<站点>/admin/` 点开是「后台需要本机后端」的说明，**登不进去** ——
线上是静态导出，构建时没有后端地址，后台没有可连的服务端（见 7.1 节）。

| 想做的事 | 去哪 |
| --- | --- |
| 看今天谁上课、谁课时不够 | 今日概览 |
| 给学生建档、改课时、看 TA 的课 | 学生 → 点姓名展开 |
| 加老师、停用某位老师、看排课量 | 教师（「在职」可直接点开关） |
| 看教室今天满不满 | 教室 |
| 排课、改时间、标已上、取消 | 课程安排（保存前会报冲突） |
| 看这一周忙不忙、哪天有空档 | 日历 |
| 看数据在哪、备份有没有、谁改过什么 | 数据与备份 |

### 学生档案与课时记账

**信息采集表是「字段定义 + 键值对」**，不是几十个写死的输入框：

```
lib/backend/student-profile.ts   六节字段定义（基本与家庭 / 学业基础 / 学习习惯 /
                                 排课相关 / 心理与情绪健康 / 健康与特殊需求）
Student.profile                  键 → 值（文本 / 多选 / 表格）
```

- 表单与详情页共用同一份定义，因此不会出现「表单能填、详情页不显示」；
- **加字段不需要数据迁移**：老档案读不到新字段就是空值；
- 心理与情绪健康一节标了 `sensitive`，界面默认折叠并遮住内容 ——
  采集表写明这类内容「仅限指定人员查看」，界面至少要做到「不点开看不到」。

**课时按科目记账**（`Student.enrollments`），不再是学生身上的一个总数：

| 动作 | 服务方法 | 说明 |
| --- | --- | --- |
| 报课 | `students.enroll()` | 新开一条记录（科目 / 班型 / 课时 / 指定教师 / 日期） |
| 续费 | `students.renewEnrollment()` | 累加课时并留下流水，家长对账有据可查 |
| 退课 | `students.refundEnrollment()` | **标记退课并记日期，不删除** —— 否则课时去向说不清 |
| 上课扣课时 | `lessons.markCompleted()` | 按**该节课的科目**找对应报课记录扣减 |
| 手工调整 | `students.adjustEnrollmentLessons()` | 补录 / 纠错，同样留流水 |

扣课时找不到对应科目的报课记录时**不扣，并如实上报原因**
（`CompletionResult.skipped`）—— 排课与课时对不上时，这就是线索。

#### 课时流水（账本，v5）

只在报课记录上留一个 `usedLessons` 数字是不够的：家长问「上了 20 节为什么扣了 22 节」
答不出来，老师点错「标记已上」也无法补救。因此每一次课时变动都记一笔
`LessonTransaction`（+报课 / +续费 / -上课 / ±调整），并且**撤销是留痕**
（`reversedAt` 打时间戳）而不是删除。

- 上课扣减会记下 `lessonId`：能回答「这一节扣的是哪条报课」；
- **状态从「已上」改回去时按流水退回课时**（服务层在 `lessons.update()` 里处理）——
  之前只改状态不退课时，点错一次就白扣一节且没有痕迹；
- 退课时撤销的是「这节课对应的那些上课流水」，而不是简单减 1：
  同一节课被反复标记的边界情形下，按流水撤销才不会多退少退；
- **报课 / 续费 / 调整 / 退课也都进账本**（原先只有「上课」进账本 —— 机构刚报完 20 节，
  打开「课时流水」看到的是空的，只能怀疑数据没存上）。调整记的是**实际生效**的节数
  （课时不会被调成负数），退课记 `0` 节（只改状态、不减总课时）；
- **不变式（自检逐条校验，不是抽样）**：
  `有效流水之和 === totalLessons − usedLessons`，且 `「上课」流水之和 === −usedLessons`。
  种子数据、迁移与新录入都按这个口径生成，否则升级后对账数字会对不上。

`students.subjects`（列表里显示的在读科目）由报课记录推导，不单独维护，
避免「退了课但科目列表还挂着」。

### 动态追踪（课堂记录 / 作业记录 / 阶段测评）

采集表里的「动态追踪」是**多次记录**而不是档案字段，因此单独成三张表：

| 记录 | 挂在哪 | 入口 | 字段 |
| --- | --- | --- | --- |
| **课堂记录** | 课节（`lessonId` + `studentId`） | 课程安排 → 每节课的「课堂记录」 | 出勤 / 专注度 / 互动 / 状态评分 1–5 / 备注 |
| **作业记录** | 学生 | 学生详情 → 作业记录 | 日期 / 科目 / 提交情况 / 正确率 / 错题知识点 |
| **阶段测评** | 学生 | 学生详情 → 阶段测评 | 科目 / 分数 / **上次分数与趋势由服务算** / 薄弱知识点 |

两处刻意的服务端职责：

- **课堂记录用 upsert**（`lessonId + studentId` 唯一）：老师改完再保存必须更新同一条，
  而不是多出一条 —— 这是这类表单最容易写错的地方，自检专门有一条断言守住；
- **阶段测评的上次分数由服务带出**（新增时找同科目最近一条）：趋势是这条记录的核心
  信息，让页面自己找上一条，补录旧数据时顺序很容易对不上。

记录课堂表现**不影响课时**：扣课时只发生在「标记已上」，自检里也有一条断言
确认写记录不会改动课时余额。

### 课程库（`/admin/courses`，数据库 v12 起）

科目候选原先**只来自网站内容**（课程页卡片），于是想开一门网站上还没有的课（围棋、书法、
编程）只能手打进文本框，名字一歪（「围棋」/「围棋课」）课时与统计就对不上。
现在课程是数据：

- 网站课程在**新装系统**时作为初始数据自动进来（`origin: "网站"`，id 用卡片路径，
  因此重复同步不会重复添加）；机构自己加的课是 `origin: "后台"`，两者平起平坐 ——
  都能排课、能记课时、能挂到教师名下；
- **课程名是引用键**：排课科目、教师可带科目、报课科目都按名字引用，因此服务层强制
  「非空且唯一」（`validateCourse`）；两门都叫「数学」的课会让课时扣到哪一门说不清；
- 网站来源的课程**不能删**（删了下次同步又回来），不想再排就改成「暂未开放」——
  不给一个会自己复原的删除按钮；
- 同步**只增不改**：机构在后台改过的状态、班型、备注不会被一次同步冲掉；
- 边界：后台加课程**不会**让宣传网站上多出卡片（网站是静态内容，要改内容文件）。

课程候选项统一由 `api.courses.options()` 提供（网站课程 + 后台课程），
后台所有「选科目」的表单都用 `components/admin/useSubjectOptions.ts` 这个 hook：
先用内容里的课程立即渲染表单，挂载后再用课程库补齐 —— 服务层取不到时也是「少几门课」，
而不是「表单没法填」。

**与报价配置打通**（`lib/backend/pricing.ts` 的 `syncLibraryLinks` / `addLibraryCourseToPricing` /
`pricingStatusForCourses`）：报价配置里的课程可以带一个 `courseId` 指向课程库，于是

- 课程库改了名字 → 报价配置跟着改（否则家长看到的还是旧课名）；
- 课程库设为「暂未开放」→ 报价配置同步停用；重新开放则恢复；
- 课程库删掉某个后台新增的课程 → 报价配置里那一项**保留但置为暂未开放**（不静默消失，
  让机构自己决定去留）；
- 报价配置里与课程库**同名**的课程会自动补上关联（内容带过来的课程没有 courseId）。

同步放在**服务层**（`courses.create/update/remove` 之后自动跑）而不是页面里：
改名发生在编辑课程的那一刻，页面可能根本没打开报价页 —— 依赖页面自觉调用一定会漏。

界面上：课程库每门课下面显示「报价 200 元/小时（兴趣才艺）」或「未定价」，
报价页有「课程库课程定价」面板（多选课程 + 选阶段 / 新建阶段 + 填基础价），
给课程库的课定价（例如围棋 200 元/小时）后，后台能给家长试算、教师课时费也算得出来，
导出内容替换 `data/site/pricing.md` 之后家长就能在报价页上选到它。

一个**真实的口径差异**（不要试图强行对齐）：报价配置里的课程名是「课程包」粒度
（九年级课本 / 雅思口语 / 小升初），课程库是「学科」粒度（初中数学 / 雅思 / 小学语文），
大多数名字本来就不一样 —— 因此自动认领只在同名时生效，其余靠显式定价。

教师「可带科目」用复选下拉（`components/admin/MultiSelect.tsx`）：候选有三十多门且会继续增加，
平铺复选框会把表单撑长，原生多选又要按住 Ctrl（老师第一次用基本都会选错）。

### 2026-09 四路对抗性审计与 P0 修复（数据与钱）

四份独立审计（后端接口面与权限层 / 后端数据层 / 后台显示层 / 增删改流程）把系统过了一遍，
结论按"会不会伤到数据或钱"分档。**P0 这一批已经修完**，每条都有反向验证（改坏 → 断言报红 → 还原）：

- **删除护栏**（机构选的口径：**有账就拒绝**）。护栏原先只写在**老 REST 接口**上，而界面走
  `/api/call` —— 也就是说它是一道装饰：实测删一个交过钱、上过课的学生之后，¥2000 收款、
  2 条课时流水、测评与作业全成了孤儿，本月收入还计着那笔钱。现在护栏在**唯一的入口**
  （`collection.remove`）上，五类档案各有判据（`studentDeleteRefusal` / `teacherDeleteRefusal` /
  `classroomDeleteRefusal` / `courseDeleteRefusal` / `lessonDeleteRefusal`），
  拒绝理由逐项列出"还剩什么、该怎么办"（停用／结课／先撤销「已上」）。
  自检夹具要收尾时走**单独的夹具钩子**（`__removeFixture`，刻意不挂在 `api` 上）——
  护栏照旧严格，不为测试放宽。
- **收款的两条路**：`payments.create/update/remove` 是集合工厂白送的写方法，页面一处没调用，
  却既不写日志、又绕过「实收 = 收款 − 退款」（实测塞一笔 ¥7000 后收费页显示本月收入
  ¥1,001,499，那条报课的实收还停在 ¥1500）。**三个方法直接删掉**，收款只剩 `record` 一个写入口，
  而且它现在写操作日志（带金额、方式、备注）。
- **退费口径**：金额曾由**前端**算、服务端原样落账（实测能退 999999）。现在只认 `policyId`，
  服务端按策略重算；前端传的金额只用于对账，对不上就拒绝并要求刷新。顺带修掉一个更早就存在的
  错误：两条策略都在用 `agreedAmount`（约定应缴）计算，而名字、说明与公式里写的都是"实付 / 实收"
  —— 一个欠费 900 的报课退课时，界面会告诉家长"实付 ¥2800"（实收只有 ¥1900）。
  现在按**实收**算，并加了一条"退款不得超过实收"的兜底。
- **迁移**：v2 分支把"要不要做结构变换"和"版本号推进到哪"混在一个条件里，结构已是新版时
  版本号不推进 → `migrate()` 返回 null → `load()` 当成"首次访问"→ **整库清空并落盘**
  （实测：8 名学生 / 18 条收款，只因为版本号写着 2，读一次变 0/0）。现在分两件事：
  分支只管变换、版本号无条件推进；而且**有内容却读不出来时一律抛错、什么都不改**
  （空库起步只允许发生在"存储里确实没有内容"时）。
- **超额退款**：`Math.max(0, …)` 钳位会让"实收"与账本永久分叉（自检那条不变式只在没人多退时通过）。
  改成**拒绝**并说清"先核对收款流水"。

界面这一侧同步补了：删除确认框里写明"有账就删不掉、该怎么办"，
以及一个共用的写操作提示（`useActionNotice` + `ActionNoticeView`）—— 审计实测有一整片写调用是裸
`await`，403/400 会变成无人接的拒绝（点了没反应、按钮永久停在"保存中"）。

**P1（账目与审计一致性）也做完了**：

- **改账必须留痕**：调整课时（`students.adjustEnrollmentLessons`）、补课、阶段测评原先都不写日志 ——
  现在都写，摘要里带关键数字（"课时 4 → 7（+3）· 试听送 2 节"）。退课/续费的日志也带金额与口径。
- **课堂记录不再有通用写方法**（`create` / `update` / `remove` 删掉，只留 `save`）：
  删一条考勤记录会留下"没有任何依据的课时扣减"（下次对账会把"没有记录"当成到课扣课时），
  而且原先那个集合没给日志标签、删了不留痕。**纠错是改记录，不是删记录** —— 界面上的出勤纠错走的就是重新 `save`。
- **报课校验两路对齐**：建档时报课一直有校验（空科目、课时 ≤ 0、同科目重复、教师不存在），
  而"后来单独补报课"走的是另一条路、**一条都不查**（实测 `teacherId: "t_根本不存在"`、
  空科目、同科目报两次全都静默落库）。现在两处共用 `validateNewEnrollment`，
  并且"同科目已有在读报课"会明确提示改用「续费」（否则课时分成两条、余额看着对不上）。
- **口径合一**：① "剩余课时不足"原先四个界面四套算法（合计 / 最少的那一门 / 各自写死阈值 5），
  现在由 `lessonBalance` + `FOLLOWUP_RULES.lowLessons` 一处算，概览与待跟进给出**同一个答案**
  （判据取"剩余最少的那一门"——排课受单科限制；合计作为附注一起显示）。
  唯一的差别是"一节课都没报"的学生：概览列（那意味着这节课排不了），待跟进跳过（不是催续费场景），
  这条差别是明确的、有断言盯着。② "含不含已取消的课"原先四处不一致（同一行能写"3 节 · 2 小时"、
  同一屏两个小时数），现在统一成**取消的不算课次也不算课时**，并单独显示"另有 N 节已取消"
  （`lib/backend/lesson-stats.ts`）。③ 教室利用率那个写死的"每天 14 小时"改成**如实说明**
  （它是固定估算，与内容里的"上课时间"无关），不再谎称"按上课时间估算"。
- **日志上限说出来**：操作日志最多 500 条、超出丢最早的 —— 界面原先一个字都不提，现在写明并建议定期导出。
- **补上死掉的入口**：`students.adjustEnrollmentLessons` 服务层一直有实现、权限也齐、
  使用手册还让用户在"课时账错了"时用它，**界面上却从来没有入口**。现在报课面板上有了
  「调整课时」（正数加、负数减、原因必填，不动钱）。

**P2（界面上的坑）也做完了**：

- **读失败要说话**：10 个页面的 `load()` 原先没有 `try/catch` —— 后端没开、登录过期、
  权限不足时会停在「加载中…」，**一个字都不说**（远端代理对这些一定 reject，
  而 `setLoading(false)` 在 reject 之后那一行走不到）。现在共用一个 `LoadFailure`
  （服务端原话 + 重试 + 一句"屏幕上是上一次读到的数据"），自检里钉住"每页都有 catch 且都渲染了它"。
- **没权限的按钮不再渲染**：判定用 `canCallMethod(roles, "具体方法名")` —— **直接问服务端用的
  那个函数**（`allowedRolesForMethod`），因此界面能点的与接口放行的**天然同源**。
  原先那张 `STUDENT_ACTION_ACCESS` 全仓库没有任何运行时读者（自检拿它自己的字面值自证），已删掉。
  普通教师不再看到「新增/编辑/删除学生」「取消/恢复/编辑/删除排课」「批量导入」，
  财务管理员不再看到「标记已上」，并各有一句"这件事归谁"。
- **课程库对教师能打开了**：`pricing.get()` 对普通教师是 403，而它原先和课程列表在同一个
  `Promise.all` 里 → 教师一点进课程库就是整页「加载中…」。现在报价单独 catch，
  读不到只影响"未定价/已定价"那一块并说明原因。
- **报价页对招生老师能用了**：一挂载就自动试算，而 `teacherFee` 对招生是 403 →
  「试算结果」整块永远空白。现在家长价与教师课时费**分开取**，后者没权限就不取并说明；
  导出/恢复默认也补了 catch（原先点了没反应）。顺带改掉那句与 `DataNotice` 打脸的
  "数据存在浏览器本地"。
- **整库导入先看清再替换**：它是全系统破坏力最大的动作，却原先"选中文件就替换"、
  备份还只有一个槽（连选错两次就回不去）。现在确认框里写明"这份文件里有多少、库里现在有多少"，
  备份改成**滚动保留最近 5 份**（`BACKUP_SLOTS`，另加一个清单键，因为 KeyValueStore 不能列键）。
- 顺手修掉：账号页"删除最后一位技术管理员"不再让人白走两次确认（按钮先禁用并说明）；
  收费/教师的"共 N 笔 / 共 N 节"写明列表是截断的；咨询线索**显示家长联系方式**（原先收了存了、
  后台任何地方看不到，现在能一键复制）；教师/教室页支持全局搜索的 `?teacherId=` / `?classroomId=`
  直达；课表页一个恒真三元与一段没人用的分组计算删掉。

**P3（清理多余）进行中**。已完成的第一块是**老 REST 整条下线 + 后端进入类型检查**：

- **老 REST 接口（`/api/students`、`/api/pricing`…）已删除**：它们读写的是另一套规范化表，
  与界面（`/api/call` → kv 快照）不是同一份存储，于是会给出"写进去没人读"的假成功与
  "读到空数据"的假失败；而**删除护栏原先只挂在它们身上** —— 界面走的那条路完全绕开护栏
  （审计的第一条：删档案留下无主的收款记录）。现在一律 **410 Gone + 指路 `/api/call`**，
  实现、权限表（`REST_CONTRACT_METHODS` 43 行）、`handleCrud`、行映射器全部删掉，
  `server/index.mts` 从 **2812 行降到 1726 行**。留着 `LEGACY_REST_PATH` 只是为了给这些路径
  回一句清楚的 410，而不是"悄悄 404"。
- 顺带修掉两处"两套"：**操作日志**（账号管理与节假日抓取原先写 SQL 的 `logs` 表，
  而界面读的是快照那份 —— 老 REST 下线后那些记录就没人能看了）现在统一走
  `__appendSystemLog` → 同一份日志，界面上就能看到"谁加了账号 / 重置了谁的口令"；
  **`/api/status` 的条数**原先数那套空表（机构有 5 位教师，它报 `teachers: 0`），
  现在从快照里数，`stage` 也不再写死 `read-only`（同一个进程明明接受写入）。
- **发现并补上一个门禁缺口**：`tsconfig` 的 `**` + `/*.ts` 通配**不匹配** `.mts`，
  而服务端全是 `.mts` —— 也就是说在此之前 `npm run typecheck` **对后端一行都没检查过**
  （实测：往 `server/kv-store.mts` 里塞一句 `const x: number = "字符串"`，`tsc --noEmit` 照样 rc=0）。
  现在显式包含 `server/**/*.mts` 并开了 `allowImportingTsExtensions`，**后端类型检查全绿**。
  （`scripts/**/*.mts` 当时还有 115 处历史类型债，已在 2026-09 清账、现在也纳入类型检查 —— 当时是待办事项，
  不是"不需要"：自检脚本出错是响亮失败，而后端出错是静默改数据，所以先保后端。）

剩下的三块也做完了：

- **无人调用的方法全删了**（12 个：`assessments.create/get/list/update`、`homework.get/list/update`、
  `courses.get`、`lessonRecords.get`、`payments.get/listBetween`、`outstandingByStudent`），
  并加了一条**防"只增不减"的断言**：扫 178 个源码文件，要求契约里每一个方法都至少有一个调用点
  （外加"服务层上没有没登记进契约的方法"）。契约方法数 115（审计时）→ 110（关掉收款/课堂记录的通用写）
  → **98**。顺带清掉 `TEACHER_SCOPE_RULES` 里指向已删方法的 8 条死条目 —— 自检那条
  "行级范围表里没有死条目"的断言当场报红，正好证明它在干活。
- **错误码只有一处翻译规则**（`httpError`）：坏 JSON → 400（原先 `/api/call` 回 500、别处回 400）、
  权限 → 403、冲突 → 409、其余 → **500 + 一个编号**（真正的错误与堆栈写进服务端日志，
  不把内部异常原文回给浏览器 —— 局域网里任何设备都能调这个后端）。`POST /api/status` 也改成 404
  （它只看 GET；原先任何方法都回 200）。
- **死导出与重复实现**：删掉 20 个没有任何引用的导出（含 `teacher-share.ts` 里像"分成对照表"的
  残留、`EmptyState` 整个组件、`VERSION_NOTES` 之外的一批常量），`dateKey` 的两份实现改成
  从 `format.ts` 转发（"两处保持一致"是句空话，只有一处才是保证），"报课还在读"的两种判据
  （`endedAt === ""` vs `status === "在读"`）统一到 `activeEnrollments`。
- **`VERSION_NOTES` 终于有读者**：新断言要求它覆盖 v1–当前版本每一版、每版都有说明，
  并且 `migrate()` 里推进到的每一版都在表里 —— 加一版却忘了写说明会报红。

**四份审计的结论到此全部落地**：P0（数据与钱）、P1（账目与审计）、P2（界面坑）、P3（清理多余）
各自独立提交，每阶段都跑全部门禁。

**收尾时又自己抓出一处（审计没提到的）**：`/api/call` 把所有异常一律回 **400 + 异常原文**，
于是"代码写错了"和"你参数填错了"在界面上长得一模一样。真实的坏结果是那次
`pricing.quote` 读 `undefined` 字段（`Cannot read properties of undefined (reading 'courseName')`）：
界面把它当业务提示弹出来，排障方向被引到表单校验上；而且这句内部原文回给了**局域网里任何
设备**。现在按**异常类型**分（不猜文案）：业务拒绝（`api.ts` 里那些 `throw new Error("中文说明")`）
→ 400 原文照回；`TypeError` / `RangeError` / `ReferenceError` / 非 `Error` → 500 + 一句人话 +
`req_xxxxxx` 编号，完整堆栈只进服务端日志。`scripts/check-auth.mts` 的 `[13]` 节拿真实 HTTP
钉住这两种答复（含"500 不许出现 `TypeError` 字样"与"400 不许带 `req_` 编号"），
反向验证是把 `isBug` 改成恒 `false` → 三条断言报红。

### C1：课程类型改为维度模型（v23）—— 「四百多条叶子」其实是四个维度的乘积（当时误做成五个，见 C4）

机构把那份课程分类清单（学段 → 学科 / 项目 → 内容模块 / 等级 → 班型，铺开 **400 多条叶子**）
发过来，同时给出要求：**"有非常多的组合，如果用枚举的话完全不可能实现后续的 AI 排课以及
学生诊断课程推荐。先不要做，先告诉我你打算怎么设计"**。

设计先说清楚、确认后再动手（机构逐条认可），结论是**维度法**：

```
学段 × 学科 / 项目 × 内容模块 × 班型   ← 维度是数据
一门课 = 上面四个维度的一次组合                    ← 组合按需解析，不落库
```

机构那句"完全不可能"是对的：枚举法加一个语种 / 一级等级就让记录数翻倍，
而且"这个学生该补阅读还是补文言文"在枚举表里长得一模一样 —— 诊断推荐没法筛。

**六条口径（机构确认后的定稿）**：

1. **班型唯一口径**：以系统现行的那一套为准（`pricing.classTypes`：一对一 / 一对二 / 一对三 /
   一对多（4-8）/ 班课（9-20））。清单里的「一对一定制课 / 一对二 / 一对三小组课 / 一对多小班课 /
   9 人以上大班课」都映射到它们 —— 顺带把"班型有第二套写法"这件事清掉了
   （v20–v22 期间后台的班型候选取的是特色课程树的二级课程名）；
2. **学科与学段解耦**：清单里「小学语文 / 初中语文 / 高中语文」是**一行**语文 + 三个学段；
3. **模块分三类**：`教材进度`（一年级…九年级 / 必修选修）、`能力点`（客观题 / 阅读 / 文言文 /
   作文 / 听力）、`语言等级`（CEFR-A1…B2 / N5…N1 / CET-4·6）。**诊断推荐按类别筛**；
5. **小学也要分班型**（机构补充："是我忘记了放进去"）：班型是独立维度，与学段无关，
   因此小学自然也有四种班级规模 + 班课；
6. **先不分维护角色**：`crud` 那一档（技术管理员 / 财务管理员 / 招生老师），教师只读。

**开发中被自己的断言抓出来的一个真 bug**（值得记一笔）：种子第一版是清单的**逐行转录**，
同名学科在每个学段各出现一次 —— 于是学科 id（由名字派生）重复、"语文"变成三条互不相干的学科，
而 `catalog.save` 的闸门对这些种子会报 **91 条问题**（机构打开后台第一眼就是一片红字）。
第 30 节的第 2 条断言（"种子自己就能过闸门"）当场报红，改成"展开时按名字合并 + 学段取并集"
之后归零。同一节还抓出 `assignCatalogIds` 里模块 id 被通用填充抢先（`mod_听力` 而不是
`mod_英语·听力`，两个学科的同名模块会撞成一个 id）。

**这一版做了什么**：

- `lib/backend/catalog-seed.ts`：机构清单的逐行转录（公开、可核对）+ 展开时合并
  （5 学段 / **38 学科项目** / **98 内容模块** / 5 班型 —— v26 去掉了当时多做的「交付形态」一维），id 由名字派生、可重复灌）；
- `lib/backend/catalog.ts`（纯函数）：校验（同一层内重名 / 悬空引用 / 一层分组 / 人数区间 /
  不能删空 / 空 id）+ 派生（`subjectsOfStage` / `subjectsInGroup` / `catalogGroups` / `stageNamesOf`）
  + `assignCatalogIds`（保存前给新行补 id）；
- 数据 v23：`db.catalog`，迁移从种子灌初值；**尾闸门**与分区表同一条纪律
  （"自称 v23 却缺这张表"的文件也兜成种子，否则后台那一页整页 TypeError）；
- 接口：`catalog.list` / `catalog.save` / `catalog.resetToSeed` **三个**（不做四张表各一套 CRUD ——
  维度表是配置，整份读整份写；`validateCatalog` 在前端与服务端是同一份）；
- 后台「课程类型」页：五个页签、每行改名 / 换类别 / 勾学段 / 换分组 / 排序 / 删除、实时校验、
  保存修改、恢复种子；导航与 `PAGE_ACCESS` 都已登记；
- 班型候选改口径：`useFormOptions` 从 `catalog.formats` 取（`options.ts` 的同步种子同源），
  特色课程树那套写法废弃；`LessonSeriesForm` 遇到老数据里的旧写法会把它补在选项最前面
  （否则浏览器显示第一项「（不填）」，保存时那次静默替换就把班型改掉了）。

自检新增第 30 节（**50 条**）：种子自洽（id 不重名 / 自己就能过闸门 / 班型与报价同名）、
合并语义（语文一行三学段、日语两学段 + 分组、客观题初高中并一行）、"分组是学段内的桶"、
14 类校验拒绝、补 id 与幂等、API 与迁移（v22 老库 / 保存落库留痕 / 恢复种子 / 缺表的文件被兜住）。
逐页验收新增 8 条（**93/93**）。真实库升到 v23 实测：5 / 38 / 98 / 5 / 5。

**这一版刻意没做的一件事**（机构也已确认）：`offers` —— 每条组合是否开放、配一个可勾选的矩阵。
押后的理由写在当时：维度表还在改的时候，矩阵的行列会天天变。它落在下一节（C2）。

### C2：开放矩阵（v24）—— 「维度」之后是「组合」，稀疏存储 + 批量勾选

维度表（C1）解决的是"可以有哪些维度"，但机构真正要回答的是另一个问题：
**"我们到底开哪些组合？"** 一条组合 = 学科 × 内容模块 × 班型。

**为什么不是把组合铺成行**：133 个（学科 × 模块）格 × 5 班型 = **665 条**（v26 去掉交付形态之前是 3400 条），
而机构开的通常只是一小片。因此这一次做的是**稀疏存储**：只有机构表过态的才有行 ——
于是"开放 / 明确关闭 / 没设置"是**三件不同的事**，而这三件事都要能看出来：

- 保留"明确关闭"而不是删行：维度表一改（加个班型、加个模块），新格子会一片片冒出来，
  那才是需要机构回来看的信号；把关闭记下来，才能让"已决策"与"待决策"一眼分开。

**矩阵的形状**刻意定成"行 = 学科 + 它的模块 / 列 = 班型"（v26 之前多了一维交付形态）：
两个维度同屏才看得出规律（"这门课这个班型能不能上网课"），也才好按列整片勾。
「（不分模块）」是**合法的一行**（不指定模块的组合照样能开，诊断推荐才需要模块那一层）；
学段只用来筛行（学段 + 学科 + 模块已经唯一确定一门课）；分组（"外语等级考试"这种桶）
不出现在行里。

**实现上的两个决定**：

1. **组合的身份就是那四个 id**（`offerId` = `off_` + 四个 id）。同一条组合不可能有两行、
   批量勾选不必查重、维度改名不影响它。⚠️ 这个键**只当键用、绝不反解**：模块 id 自己就带
   `·`（`mod_语文·客观题`），按下标切分必然切错 —— 这条在实现时先踩过一遍（第一版
   `applyDecision` 就是按下标切分的），改成传结构体。
2. **批量勾选是页面上的纯函数**（`applyDecision`），服务端只有 `offers.list` / `offers.save`
   两个方法。为"整行 / 整列 / 整个学段 / 全部"各加一个接口的话，"批量"的口径会散在服务端
   好几处，而且迟早出现"单条能存、批量被拒"。

**格子点一下的循环**是"没设置 → 开放 → 明确关闭 → 没设置"：只剩两个状态的话，
机构按错一次"全选"就再也擦不掉了。

**给 AI 排课与诊断推荐留的入口**：`resolveOffer(index, key)` → `open` / `closed` / `unset`。
`unset` **不等于关**：推荐时它意味着"要先问机构"。

**实现完自己走了一遍，发现并补掉一个死角**：删掉一个班型之后，引用它的组合
① 在矩阵里不显示（列没了），② 而 `offers.save` 会因为悬空引用整份拒绝 ——
机构会卡在"一保存就报错、却找不到改哪一格"。现在：删除维度时服务层**连带清掉**受影响的
组合（条数写进日志，删除确认框里先说清有几条），而从外部进来的不一致数据由矩阵页的
「失效的组合」块列出来 + 一键清除；**读的时候绝不偷偷清**（组合开不开是机构的经营决定）。
自检第 33 节（11 条）专门钉这件事。

自检新增第 31 节（**44 条**）：矩阵形状（行 = 学科 + 模块 + 不分模块、列 = 25、
分组不在行里、模块行用自己的学段）、组合身份（确定性、带 `·` 也不影响、空模块合法）、
三种状态与批量勾选（含清除与保留备注、纯函数不改入参）、8 类校验拒绝、
"这一删会牵动几条组合"（`offersOfDimension`）、API 与迁移（v23 老库 → 空表起步、
保存落库留痕、悬空引用被拒、缺表的文件被兜住）。
逐页验收新增 7 条（**100/100**）。真实库升到 v24 后组合表为空（机构还没勾，这是对的）。
（之后又补了第 33 节 11 条 —— 上面那个死角。）

**留下的最后一处两套写法**（见下一节 C3）：报价的「班级类型」与 `catalog.formats` 仍是两处
（名字逐个同名、由自检钉住）—— 报价只该留「这个班型多少钱」的系数，名字与人数区间以维度表为准。
网站侧已经把班型下拉接上了（C1）。

### C3：网站与报价改为引用维度（v25）—— 班型的名字只有一个真源

C1 把班型收进了维度表，但**报价配置里仍然各自记着一份名字**（`pricing.md` 的
「班级类型」那一节）。机构改个班型名，报价页与网站报价器上还是旧名字 ——
而这类不一致**不会报错**，两边都显示得像正常的。这一版把它收干净：

> **名称与人数区间以维度表为准，报价只保留「这个班型多少钱」。**

- `PricingClassType` 加 `formatId`（指向 `catalog.formats`）：身份是 id，名字只是快照。
  迁移（v24 → v25）按名字给老数据补上这个字段 —— 对不上的**留空串**，不编 id、不删行、不改系数；
- 读的时候由 `syncClassTypes`（`lib/backend/class-types.ts`，纯函数）换成维度表的当前名字，
  三个读者共用：`api.pricing.get`（报价页）、`buildPublicSite`（网站报价器）、
  `api.pricing.exportMarkdown`（写回 `pricing.md` 的那一份）；
- **`catalog.save` 落库时也一起对齐**：库里不留过期名字（读时那一层是兜导入与手改过的库）；
- **试算不会因为改名而失败**：`pricing.quote` / `pricing.teacherFee` 用对齐后的配置查班型 ——
  这是改名最容易漏的一处（页面发过来的是新名字，库里那一份可能还没跟上），
  自检与逐页验收各钉了一条；
- `pricing.update` 拦住两件事：`formatId` 指向不存在的班型（那一行系数永远算不到）、
  同一个班型挂两行系数（"这个班型算哪一行"会两说）；
- 两种差异都在报价页提示（`classTypeIssuesText`，一句人话说清后果）：
  **报价里有、维度表里没有**（那一行已经不会被任何课用到）与
  **维度表里有、报价里没有**（新班型还没系数 —— 家长问到它时算不出价）。
  对不上的行原样保留：**不静默丢一行价格**。

自检新增第 32 节（**22 条**）：迁移补 id、改名之后三处一起变、按新名字试算、
导出片段用维度表名字、网站公开数据同一份名字、orphans / unpriced 两种差异与文案、
写入口的两条拒绝、改回原名。逐页验收新增 5 条（**105/105**）。
真实后端实测：在「课程类型」把「一对三」改成「一对三（小组课）」→ 报价读出来、试算、
导出的 Markdown 全是新名字，班型 id 不变；改回去即恢复。

至此"网站与报价不再手写栏目 / 班型名"这条落地：
- **班型**：名称与人数区间来自维度表（本轮），后台下拉（C1）与报价（本轮）都已改过来；
- **课程栏目 / 分区**：v18 起就是数据（`coursePartitions`，网站课程总览按它分组）；
- **静态那一份**（`data/site/*.md`）：班型名必须等于维度表的种子（自检第 30 节钉住），
  其余是"没连后端时的骨架"，不是第二套真源。

### C4：更正 —— 「网课 / 网课+答疑 / 托管」是**独立项目**，不是第五个维度（v26）

机构看完 C1–C3 之后指出：「**网课、网课+答疑、网课+一对一这些是单独的项目，
和小学初中高中这些任何课程没关系**」。这是我的设计错误，不是实现 bug：

C1 里我自作主张加了第五个维度"交付形态"（面授 / 网课 / 网课+答疑 / 托管 / 全日托管），
理由是"一门课可以面授，也可以网课+答疑"。但机构清单里的这一支（「其他类型 → 不分班型项目」
下的 网课 / 网课+答疑 / 网课+一对一针对性答疑 / 小学托管 / 初中托管 / 学期全日托管 /
假期全日托管）**本来就已经在 `subjects` 里**（`kind: "项目"`）—— 我把同一件事记了两遍，
还凭空造出"小学语文 × 网课"这种机构并不卖的组合。而矩阵是一张**要机构照着勾**的表，
多出来的格子会让人以为那些组合是存在的。

v26 的更正：**维度只剩四个**（学段 / 学科与项目 / 内容模块 / 班型）。

- `Catalog` 去掉 `deliveries`；`CatalogOffer` 去掉 `deliveryId`；组合的身份变成三个 id；
- 矩阵的列就是班型（5 列），格子数从 3400 变成 **665**（133 行 × 5 列，实测）；
- 迁移 v25 → v26：抹掉 `deliveries`，把只在交付形态上不同的组合**合并成一条**
  （**有一条开放就算开放** —— 那是更强的表态；备注取非空的，时间取较晚的，确定性、可重复执行）；
  收尾归一还会抹掉"自称 v26 却带着 deliveries"的文件里的那一维；
- 那些项目**一条都没丢**（自检第 34 节专门钉住：5 条还在 `subjects` 里、类别是「项目」、
  挂在「不分班型项目」下、在「其他类型」那一栏，且**不**出现在矩阵的列里）。

自检新增第 34 节（**20 条**）。逐页验收仍是 105/105（开放矩阵那一节改成按班型勾）。

**这次的教训**（写在这里免得再犯）：维度法最危险的地方不是少一个维度，而是**多一个**
—— 多出来的那一维会在矩阵里变成一格格"看起来可以卖"的组合。机构给的清单要**照着它的分组读**，
不要替它发明"可以怎么组合"。

### D1：日历与节假日合并成一页 + 假期时段口径（v27/v28）

机构提了两件事，合成一批做完：

1. 「**日历和节假日这两个页面的功能应该放在一起**」；
2. 假期怎么算：「**假期要上课，寒暑假开始和结束的日期每次需要手动输入，在寒暑假期间排课会不一样**」，
   随后补清：「**作息和现在的周末上课时间完全一样**」「寒暑假**按学段**（不过实际上时间是差不多的）」
   「**先把时段分清楚，后续的排课再另外安排**」。

**合并**（v28）：`/admin/holidays` 不再是独立页面，整块搬进「日历」页的「假期与作息」页签
（`components/admin/HolidayTablePanel.tsx`）；导航只剩「日历」一条；
`HOLIDAY_ACTION_ACCESS["holidays.read"]` 与 `/admin/calendar` 的页面权限对齐。
自检里原来钉着那一页的 6 条断言**一条没删**，只是改指向新文件，另加两条
（"没有第二个人口"、"日历页确实挂了这一块"）。

**时段口径**（`lib/backend/calendar-plan.ts`，纯函数、唯一口径）：

| 这一天是什么 | 按哪一组时段 |
| --- | --- |
| 学期内的工作日 | 工作日组 |
| 学期内的周末 | 周末组 |
| **法定假日** | **周末组**（机构照常上课，白天能排） |
| **调休上班日** | **工作日组**（学生要上学，白天排不了） |
| **寒暑假段内** | **周末组**（作息与周末相同；每天都行） |

优先级 **寒暑假 > 调休 > 法定假日 > 周末/工作日** —— 关键是春节的假日与调休**落在寒假里**，
那时学校已放假，因此寒暑假优先（否则"寒假里的调休周六"会被判成只能排晚上）。

**两组时段只有一份来源**：从「网站内容 → 时间安排」读（`windowsFromSchedule`），
不另建配置 —— 网站上公布的时间与判定用的口径必须同一份，否则家长看到的时间会与排课用的漂开。

**数据**（v27）：`vacations`（寒暑假段：名称 + 类别 + **学段** + 起 + 止，**空表起步**，
机构每年手动录，系统不推算）；`vacations.list` / `vacations.save`（+2 方法 → 112）。
周视图每天标 `休 / 班 / 寒 / 暑` 并写出适用时段（「工作日组 17:30–21:30（2 段）」）。
页面上明确写着**这一版只把口径摆出来、不改排课**。

自检新增第 35 节（**42 条**）：优先级（含春节那一对反直觉的用例）、按学段生效范围、
时段解析（含"晚辅导 / 全日托不算排课时段"、改名与"双休日"写法、读不到时的 missing）、
寒暑假校验（写反 / 非法日期 / 2026-02-30 / 没勾学段 / 悬空学段 / 重叠只提示）、
**"排课一行没动"**（断言 `recurrence.ts` 里没有 `dayPlanFor`）、迁移与读写。
逐页验收新增 7 条（**112/112**）。

### E11：加三个内部字段 —— 教室的「校区」、教师的「全职 / 兼职」与「来源」（v39 / 数据库 v30）

机构原话：「**在教室页面里添加一个校区字段吧，教师界面也添加一个全职/兼职以及教师来源**」。
一句话里三个字段，口径在动工前就定死了（下面这张表就是那一份口径，实现里不许自己改）：

| 实体 | 新字段 | 类型与取值 | 界面上 |
| --- | --- | --- | --- |
| `Classroom` | `campus` | 字符串，**自由文本**；**v31 起写入时必填**（机构「校区必须填」，见 E11 四续） | 表单里「校区」一格（**`required`**），带 `<datalist>` 列出**已在用的校区**；卡片上有值时显示 |
| `Teacher` | `employment` | 只能是 `"全职"` / `"兼职"` / `""`（**空串＝未填**） | 表单里「全职 / 兼职」下拉，**含「未填」一项**；列表里有值时显示成小标 |
| `Teacher` | `source` | 字符串，**自由文本**、可空 | 表单里「来源」一格（hint 写明是**招聘渠道**），带 `<datalist>` 列出已在用的值 |

**迁移（v29 → v30）：一律补空串，不猜内容。**

这是这一版最该守住的一条（与 v12 → v13 给教师补资料那一步同一条纪律）。三个字段在这件事上
其实都**能猜出一个"看起来对"的值**，而那正是不能猜的理由：

| 字段 | 手边那个"看起来对"的猜法 | 为什么不能猜 |
| --- | --- | --- |
| `employment` | 默认成「全职」 | 库里那几位教师是照**网站教师页**建的（`origin: "网站"`），机构**从没登记过**谁是全职谁是兼职。猜一个等于把一个"没填"记成一条**用工事实** —— 而它会立刻被拿去用（排课量、成本、对外说"我们有多少全职老师"） |
| `campus` | 从教室名里截一个（库里写着「沐阳教育·教室1」「全慧教育·落地房四楼小」） | 名字里是**看得出**校区，但"看得出"不等于"机构登过记"。按名字截一批进库，等机构真要按校区分权 / 分账 / 分统计时，手上是一批**没人确认过**的值，而它们看着都像真的 |
| `source` | 猜「朋友介绍」 | 招聘渠道根本没有可推的来源信息，纯属编 |

空串在这三个字段上的含义都是明确的（＝未填），页面上显示成"没填"，人一条条补即可。
**补字段宁可留一堆空，也不要留一堆猜错的非空值** —— 空的会被看见，错的看着像真的。

迁移之外还有**收尾归一**兜一次这三个字段（`db.coursePartitions` 那种兜法）：一份"自称 v30 却缺字段"
的文件（手改过的导出、只跑了一半的恢复、以及**导入**）会让教师页那个下拉读到 `undefined`、
教室卡片上印出「校区：undefined」，而且**不报错**。收尾归一**不抛错**：那里遇到非法取值
（手改文件里写着「临时工」）归成空串＝未填 —— 抛错等于**整库读不出来**，比"显示成未填"坏得多。

**校验放在服务层，非法值报错拒绝**（机构那一条："不许静默改成空串"）：
`employment` 只认 全职 / 兼职 / 空串，其它值（「临时工」、`true`、数字…）当场抛错、**什么都不写**
（钩子抛在 `persist` 之前，连记录版本号都不推进）。归一与校验挂在**唯一那道写入闸**上
（`versionedCollection` 的 `normalize` 钩子，`create` / `update` 各过一次）—— 因此页面、
批量导入、`/api/call` 三条路不会各有一套口径。
**"字段没给"与"给错了"分开判**：入参里没有 `employment` 这个键时按"未提供"处理、补空串＝未填
（老脚本、老客户端、`/api/call` 的裸调用不会因为多了个字段就全被拒），
而**给了非法值**必须顶回去 —— 否则"我明明填了兼职"会变成一次没有痕迹的数据改动
（操作日志里只写着"改了几列"）。

**「来源」与已有的 `origin` 是两个东西**（这一版最容易做混的地方）：

| | `Teacher.origin`（v13 起就有） | `Teacher.source`（v30 新加） |
| --- | --- | --- |
| 回答什么 | **这条档案从哪来** —— 技术来源 | **这个人是机构从哪招来的** —— 人事口径 |
| 取值 | `网站` / `后台` | 自由文本：招聘网站 / 朋友介绍 / 内部推荐 / 校招 / 其他 |
| 谁能改 | 不可编辑（由导入 / 建档那一侧决定） | 人在教师表单里填 |
| 界面上 | 列表里一个只读小标 | 表单里的「来源」输入框 + 列表里的「来源 …」 |

两者在中文里都叫得像"来源"，因此界面上的文案刻意分开：新字段叫「**来源**」，
那个技术小标从「来自网站」改成「**网站导入**」（被导入的是**档案**，不是人）。
**改的只是文案，`origin` 字段本身一个字没动**（类型、取值、写入路径、导出与断言都没碰）。

**批量导入**跟着长了两列（教师）和一列（教室）：「全职兼职」（`aliases` 也认「全职/兼职」
「全职 / 兼职」「用工性质」）与「来源」，教室多一列「校区」；`csvTemplate` / `jsonTemplate`
由列定义生成，因此模板自动带上。为这一版给导入的枚举列加了一个显式声明 `FieldSpec.empty`
（**留空取什么**）：默认取第一个候选值（「类型」那种"留空＝没说"的列要的就是这个，
宁可多一个可排课的教师），而「全职兼职」声明 `empty: ""` —— 留空＝未填，
`?? ""` 那种写法分不清"列没声明"与"列声明了空串"，所以必须是显式的一格。

**不许泄漏到公网**（硬要求，四条断言盯着）：三个字段都是**内部信息** ——
`lib/backend/public-site.ts` 的字段白名单里没有它们（那里的设计是"新增字段必须显式写一行，
忘了就是没给出去"），`data/site/content.md` 与导出的 `data/site/*.md` 里也没有。
理由不是"懒得加"，而是**招聘渠道属于人事信息**：这份仓库是公开的、网站是 GitHub Pages 上的
静态站，用工性质与招聘渠道出现在那里既没有用途、也不该被看到。
自检第 44 节查四道：公开快照的**键名**、快照**正文里的值**（夹具给前两位教师写了「全职」「朋友介绍」，
它们只可能来自内部字段，是现成的金丝雀）、白名单**源码**（去掉注释再查 ——
注释里引用了一个叫 `backend-source.ts` 的模块，裸词匹配会假红）、
导出出来的六个文件（同样是键 + 值两道；这里判据要挑 `campus:` 这种**字段写法**，
因为 `content.md` 里本来就有 `campus_title: 校区信息` 这个**页面文案键**，禁裸词会误报）。

**这一版做了什么**（改动清单）：

- `lib/backend/types.ts`：三个字段 + `TEACHER_EMPLOYMENTS` / `TeacherEmployment`（候选值只此一份）；
- `lib/backend/version.ts`：`CURRENT_VERSION` 29 → 30 与 `VERSION_NOTES[30]`；
- `lib/backend/api.ts`：v29 → v30 迁移分支、收尾归一、`normalizeTeacherRecord` /
  `normalizeClassroom`（v31 起挂到写入闸上的那个壳叫 `normalizeClassroomStrict`，见 E11 四续）/
  `teacherIssues` / `normalizeTeacherStrict`、
  `versionedCollection` 新增 `normalize` 钩子（钩子改不了 `id` / `version`）；
- `lib/backend/import.ts`：`ENTITY_SPECS` 三列 + `FieldSpec.empty` + `finalize` 补空串；
- 两个页面：教室页加「校区」（`<datalist>` 的候选值从当前列表收集，
  **不另存一份"校区清单"**——校区的真源就是教室里那一列）、教师页加下拉与「来源」输入框，
  列表上显示用工性质小标与「来源 …」，技术小标改文案；
- `lib/backend/seed.ts`：示例教师 / 教室补上这三个字段（**明写"这是示例值，不是机构真实人事信息"**）；
- 文档：使用手册 §5/§6（字段说明）、后台API约定新增 §6.7（字段表 + 校验 + 迁移 + 两个"来源"的区别 +
  不公开的理由）、技术架构 §5.1（白名单）与 §6.4（迁移纪律：能猜也不许猜）。

**自检与验收**：`scripts/check.mts` 新增第 44 节（**56 条**）：常量取值域、v29 → v30 补空串、
收尾归一（缺字段 + 非法取值两种）、合法值存读回、非法值被拒且"什么都没写"、
缺字段的老调用方仍可用、导入三列与模板、界面源码四处、以及上面那四条"不泄漏"断言。
`scripts/accept-check.mts` 新增 5 条真实读写（教室 2 条 + 教师 3 条，含"整份提交不丢字段"
与"非法值被拒且库里没变"）—— 合计 **125/125**。

**一处真实库上的观察**（写下来给后来的人看）：这次迁移在真实库（`server/data/nexgenedu.db`）
上把 v29 升到 v30，只多了三个空串字段。有意思的是，升级完之后**机构本人在后台上把这些字段真用了一遍**
（操作日志 18:39–18:46，共 7 条）：采苓先填了「兼职」又改回「未填」（顺手证明**「未填」这一档能选回来**，
不然人一旦点错就回不去了）、曹轶豪填「兼职」、陈林维祎填「全职 + 机构股东」、
林笑丹填「全职」、`教室1` 的校区填「沐阳教育」。也就是说这条新字段的**整份提交**路径
是在真实数据上跑通的；而比对脚本里那两处"差异"（教师表、日志）**全部归因于这几次编辑**，
不是迁移造成的。顺带一个可复现的旁证：这些内部值填进库之后，
`GET /api/public/site` 的快照**除了 `generatedAt`（取数时刻）之外逐字节没变** ——
用工性质、招聘渠道与校区确实一个都没进公开数据。

#### E11 续：机构补了一条口径 —— 「输入分开、显示不变」（数据库 v31）

加了「校区」之后，机构立刻指出了这条口径（原话）：

> 「**沐阳教育·教室1**，现在这个显示格式就是校区·教室名，现在添加了校区字段，
> 也就意味着我需要这个**卡片显示格式不变**，但**输入的时候校区和教室名称要单独输入**」

也就是说「**·**」这个间隔号在教室这件事上是**有含义的**：它是校区与教室名的连接符 —— 于是
v30 那一步"不猜"的判断（只补空串、不按名字截校区）**没有白做**：真实库里 6 间教室的名字
一个字没动，现在只按机构确认过的含义拆一次就够了。

**这一半做了四件事**（数据库 v30 → v31，`VERSION_NOTES[31]`）：

| | 做什么 |
| --- | --- |
| 1. **显示** | 新增 `lib/backend/classrooms.ts` 的 `classroomLabel(room)`：`campus` 非空 → `` `${campus}·${name}` ``（中文间隔号、**不加空格**），空 → 纯教室名。把**每一处**给用户看教室名的地方都改成用它：排课清单、批量排课与调课建议、课表与占用（含导出 `.ics` 的「地点」）、日历、今日概览、教室页卡片与删除确认、统计的教室利用率、咨询可行性说明、批量排课的跳过原因、搜索的匹配串与标题、教室数据集之外的两处导出 |
| 2. **迁移** | v30 → v31：`campus` 为空且名字含「·」的教室按**第一个**「·」拆成两格（真实库 6 间 → 沐阳教育 / 全慧教育 + 教室1 / 教室2 / 教室3 / 自习室 / 落地房四楼小 / 落地房四楼大）；拆了几条**写进操作日志**（点名是哪几间），这是"库里的教室名怎么变了"的唯一线索 |
| 3. **输入** | 教室表单变成**两个独立输入框**：名称那格改叫「**教室名称**」（placeholder `教室1 / 落地房四楼小`，**不再**引导填带「·」的完整写法），校区那一格照旧带 `<datalist>`（候选值从当前列表收集，现在会是 沐阳教育 / 全慧教育）。列表与卡片仍按 `classroomLabel` 显示，因此看到的还是「沐阳教育·教室1」 |
| 4. **归一与导入共用** | 「校区空 + 名字含「·」就拆」这条规则住在 `normalizeClassroom`（内含 `splitCampusFields`），**四个调用点共用**：v31 迁移、`migrate()` 的收尾归一、批量导入、服务层写入 |

**为什么敢把拆分放进"读时 / 写时都经过的归一"里**：因为它**不改变用户看到的东西** ——
拆完 `classroomLabel` 拼回来与原文**一字不差**。这只是把同一串信息换一种存法，
与"把 A 改成 B"那种静默改动有本质区别。反过来定（只在迁移里拆）会出现更糟的形态：
表单一提交就把合并名又写回去，于是迁移修过的记录与表单建的记录**是两种形状**，
校区的候选值也会随"这间房是怎么来的"而不同 —— 那正是这个项目一贯要消灭的"两套口径"。

**真实数据逼出来的一条额外规则**（如果不做就是"看起来像界面坏了"）：机构在 18:44 先在表单里
给 `沐阳教育·教室1` 填了校区，又把名字改成了「教室1」（v30 时它是 `campus="沐阳教育"` +
`name="教室1"`，而其余 5 间还是 `campus=""` + 合并名）。于是出现了第三、第四种数据：
**校区那一格有值、名字里又粘着同一段校区**（旧表或旧导出再导一次就会出现）。
不管的话显示会拼成「沐阳教育·沐阳教育·教室1」。因此 `splitCampusFields` 是三行表：
拆 / **去掉重复前缀**（只动名字，校区那格不动）/ 原样（幂等）。两件事在操作日志里**分开说**，
免得后人以为"去前缀"也丢了一份信息。

**"只剥一次"是刻意的，别改成循环**：剥离不改变显示（`沐阳教育` + `沐阳教育·教室9` 与
`沐阳教育` + `教室9` 拼出来一样）；但若名字里粘了两遍校区（粘两回才会出现），剥一次显示不变，
**循环剥到干净就会把显示改短**（变成「沐阳教育·教室9」）——那就从"去掉重复"变成"替用户改名字"了。
自检第 45 节有一条专门钉住这件事（`粘了两遍校区：只剥一次（剥完显示与原文一字不差）`），
防止有人把它"优化"成循环。

**导入还有一处必须早做**：「校区空 + 名称含「·」」的拆分放在 `parseImport` 的**行归一**里，
而不是留到落库的 `finalize` —— 因为**判重键是 `name`**。拆晚了，一份"名称列里写着
「沐阳教育·教室1」"的旧名单会被当成一间**新**教室，重导一次库里就多一间重复的房
（而"把旧表再导一次"正是机构最常做的事）。夹具里那条断言就是照这件事写的。
教室那张导出的 CSV 也跟着变成「校区」「名称」两列（名称**不拼**校区），与导入同形、可往返。

**显示的"唯一一处"由源码级断言钉住**（自检第 45 节，**35 条**）：清单显式列出
**17 个**会显示教室名的文件，每个都必须出现 `classroomLabel(`；再加一道网拦
`room.name` / `classroom.name` / `classrooms.find(...)?.name` 这类"直接读名字来显示"的写法。
**允许的例外逐条写在断言旁边**（helper 自己、导入的判重键、v31 迁移日志里点名的旧名字、
教室数据集的导出列、课表里那个"早就拼好的 `row.name`"）—— 例外不写清楚，
后来的人会以为断言漏了，然后把它删掉。

**自检与验收**：第 45 节用**一份「沐阳教育·教室1」的 v30 老库走真实 `migrate()`**，断言拆成
`campus=沐阳教育` / `name=教室1` 且**再跑一次逐字节不变**（幂等）、已分开填的一字不动、
按第一个「·」拆（剩下的「·」留在教室名里）、日志两种情形分开记、导入不新增重复房、
表单两个输入框（源码级）、helper 在 `campus` 为空时回退成纯名。
验收新增 3 条真实读写（校区 + 纯名 → 落库两格且**显示拼回原样**；合并写法 → 拆开且显示不变；
没填校区 → 显示就是纯名）—— 合计 **128/128**。

#### E11 再续：没填的也要看得出来（第三条口径，只改界面）

机构原话：「**教师信息我自己在后台填**，更希望**没填的时候也看得出来**
（比如卡片上显示一个灰色的「用工未填」，提醒你去补）」。

这条把 v30 那个"**没填就不显示**"的策略**反过来**了 —— 当时的判断（"空小标既占地方又看不出是
没填还是显示坏了"）在"机构会自己填"这个前提下不成立：**没填的那些就是待办**，藏起来等于
没人知道还缺多少人的信息。改的只有界面，**数据库版本没动**（没有新迁移）：

- 卡片上两个人事字段变成**两态**：填了 → 白底描边小标（用工性质）/ 普通弱化文字（来源）；
  没填 → **虚线边框 + 灰底 + 更浅的字色**的灰标「**用工未填**」「**来源未填**」，带
  `title="点姓名展开就能填"`。两态样式**必须一眼分得开**：同一档的话，那个灰标会被读成
  "这个人的用工性质叫『用工未填』" —— 一条**假的人事信息**比不显示更糟（机构正是拿这两个字段
  算排课量与成本的）。
- 顺序：**AI → 教龄 → 用工性质 → 来源 → 网站导入**。缺信息不该抢在身份与资历前面。
- 样式与提示各只写一份（`HR_FILLED_CLASS` / `HR_TODO_CLASS` / `HR_TODO_HINT`）：
  两个字段各写一套，机构会以为其中一个"没生效"或"更重要"。
- **一个值都没替机构填**：迁移与 `seed.ts` 的示例值之外，任何教师记录都没有被写值
  （v29 → v30 补的是空串、v30 → v31 只碰教室表 —— 真实库上教师表的哈希**至今一字未变**）。
- 自检第 44 节补 7 条（原来那条"有值才显示"改成两态各钉一条）：空值那一支渲染的是
  「用工未填」/「来源未填」（**不是 `null`／不渲染** —— 正好能拦住 `{x !== "" && …}` 那种写法）、
  填了的时候渲染的是值本身、两个灰标共用一个类与一条提示、灰标样式与已填的分得开、
  带"去哪儿补"的提示、以及小标顺序。
  使用手册 §5 与后台API约定 §6.7 各补了说明。

#### E11 三续：把「网站导入」那个技术小标删掉（作废上一条的改名）

机构原话：「**教师标签里的『网站导入』能删掉吗？**」—— 于是上一轮那个"把它改名成「网站导入」"
的做法**作废**，直接不显示。

**为什么可以删**：`origin`（网站 / 后台）说的是"**这条档案当初从哪来**"，而 v36 把那个
"从网站拉一份教师档案"的入口整块删掉之后，**以后不会再有新的档案是「网站」来源** ——
这个标只会出现在当年那几条老记录上（真实库里 5 位教师有 4 位是 `网站`：采苓 · 试课诊断、
有恒 · 学习跟踪、陈林维祎、林笑丹；曹轶豪是 `后台`），留着只是噪声，而且它天然容易与
人事口径的「来源（招聘渠道）」撞名 —— 删掉之后，卡片上"来源"这个词只剩人事那一个意思。

**为什么字段保留、一个字都不动**：`origin` 记的是一段**历史事实**。删字段要动数据库形状
（迁移、`seed.ts`、`import.ts`、导出的教师映射、自检断言全都在用它），而"不假造历史"
比"界面上少一个标"重要得多。它照旧由迁移与导入维护、导出时照旧带出去，只是不渲染。
`types.ts` 里 `TeacherOrigin` 的注释把这两件事都写清了（留字段的理由 + 为什么不显示）。

**两条断言一正一反**（第 44 节）：① 教师卡片上**不再渲染** `origin`（那段 JSX 与「网站导入」
文案都不在了，顺带也钉住「来自网站」这个旧文案）；② `origin` **仍在类型 / 迁移 / 导入里**
（`origin: TeacherOrigin` 还在、`import.ts` 还在赋值、`api.ts` 的迁移还在补默认值）——
防止后来的人"顺手把字段也删了"。这也是为什么这两条要写在一起：删标与留字段必须同时成立。

**顺带修了一处断言的判据**：§「从网站导入删干净了没有」那一节原本是对**原文**查
`includes("从网站导入")`，而教师卡片那段注释里正解释着"v36 把那个入口删掉之后为什么可以不再
显示 origin" —— 写清楚历史的人反而会把断言弄红。现在**先剥注释再查**（与 §22 / §44 / §45
同一个做法），要拦的是"页面上还留着一个点了报 404 的入口"，不是"注释里提到了这个词"。

#### E11 四续：校区必填（v39 之后收紧，机构原话「**校区必须填**」）

上面那条"校区空 + 名字含「·」就拆"的规则留下了一个**已知含义**（当时的做法是把它写进文档、
由机构认可）：名字里真带「·」而校区空着的房间会被当成合并写法拆开 —— 显示不变，
但校区会**多出一个值**（例如把「自检·限时教室」拆成校区「自检」）。

交付时把这个问题原样问了机构一遍（"要不要加个开关"），机构的答复是一句：
**「校区必须填」**。

**为什么这比加开关好**（写下来，免得以后有人反过来觉得"少做了个开关"）：

- 开关是把**歧义**留给每个填表的人去判断 —— 而"这串名字里的「·」到底是校区分隔符还是房间名的一部分"
  恰恰是**填表的人也不知道**的事（他知道自己写的是哪个，但下一个人看到的仍是一串字符）。
  于是开关的默认值无论怎么定，都会有一半的记录被那样拆或那样留，**同一个问题在库里以两种形状共存**。
- 必填是把那个**状态**直接消灭：校区空着就存不进去，于是"名字里带「·」而校区空着"这一格
  在库里**根本不会存在**，歧义也就不需要任何人去判断。
- 而且它是**可用性**上更省事的一边：校区那一格本来就有 `<datalist>` 提示已在用的校区名
  （机构自己的叫法，通常点一下就行），比让人理解一个开关的语义便宜得多。

**判据落在"拆完之后"**（`hasCampus`：只看 `campus` 那一格 trim 之后是否非空）：

| 交上来的一行 | 结果 |
| --- | --- |
| `campus=""` / `campus="  "`、`name="教室1"` | **拒绝**（拆不出校区） |
| `campus=""`、`name="沐阳教育·教室1"` | 通过：先拆成两格（**先拆后判**） |

第三行是刻意留的口子，与**批量导入**同一条口径（那里的必填判也放在行归一之后）。
理由是同一个：**收紧要收的是"校区空着"，不是废掉旧写法** —— 判在拆分之前的话，
一份"名称列里写着「沐阳教育·教室1」"的旧名单、一个只知道合并写法的旧调用方会被**整份拒掉**，
而它们本来是能建、能导的（"把旧表再导一次"正是机构最常做的事）。

**三条路都判**（少一处就会出现"换个入口就能建出一间没校区的房"）：

| 在哪 | 实现 | 错误原话 |
| --- | --- | --- |
| 服务层 `create` / `update` | `normalizeClassroomStrict` —— `versionedCollection` 的 `normalize` 钩子，**先拆后判** | `校区必填 —— 它决定教室在列表与排课里显示成「校区·教室名」，也用来按校区筛。` |
| 批量导入 | `import.ts` 的 `FieldSpec.derivableFrom: "name"`：声明成必填列，但**不进"整份不导入"的表头闸**，改成行归一之后逐行判 | `缺少必填列：校区（…）` |
| 教室表单 | `required` + 提交前那句 JS 校验 | 同服务层那一句 |

**⚠️ 别把服务层与教师那个钩子的顺序搞混**：教师是**先校验后归一**（归一会把非法取值压成空串，
先归一就再也判不出非法值了），教室是**先拆后判**（归一会把校区**补上**，先判会把旧写法一起判死）。
两个 `normalize` 钩子的内部顺序**相反**，理由各写在 `normalizeTeacherStrict` /
`normalizeClassroomStrict` 的说明里；`api.ts` 那两个集合的定义处也写了一句对照。

**老数据里"拆不出校区"的记录怎么处理**（这一条是这次唯一的"妥协"，也是对的妥协）：

一份更老的库（v30 之前）里可能有**名字里没有「·」**的教室（例如「自习区」）——
拆不出校区，机构也从没登记过。那种记录：

- **读取与迁移都不报错**：在那里抛错等于**整库读不出来**，比"有一间房待补校区"坏得多
  （迁移的纪律一直是"不猜、不拒"，只补空串）。自检第 46 节有一份这样的夹具，
  断言它能 `importDatabase` 升级、能 `list()` / `get()` 读出来、显示就是纯教室名（不多一个「·」）；
- 教室页卡片上挂一个**「校区未填」待补灰标**（虚线灰底小标，与教师卡片上「用工未填」**同一个样式**，
  `title="点编辑补校区"`）。为什么必须挂：那种记录在列表上**看起来一切正常**
  （标题里只有房间名，和"真的没有校区"长得一样），不挂标的话，
  保存时被必填拦住会让人以为"我什么都没改，它凭什么不让我存"；
- **保存的时候才被必填拦住**（改容量之类也一样 —— 表单是整份提交，校区还是空的就过不去）。
  也就是说：要动那种记录的任何字段，先把校区补上。

**数据库版本没有动**（没有新迁移）：这次只加了三道**写入时**的判据 + 界面上的一个灰标，
`Classroom` 的形状与迁移纪律一个字没改。自检第 46 节新增**32 条**断言
（服务层拒空 / 带校区能建能改能读回来 / 导入"先拆后过" / 缺列报「缺少必填列：校区」 /
老数据读取与迁移不报错、保存才被拦 / 表单必填与待补灰标的源码级断言 / 三处同一条口径）；
§44 里那条"校区也允许清空（＝未填）"**反过来**了（它正是这次要作废的口径）；
验收给教室补了"空校区被拒"与"只写名称列的旧名单先拆后过"两条真实读写 —— 合计 **129/129**。

**顺带修掉的两处旧账**：`classrooms/page.tsx` 与 PROJECT.md 里把服务层那个钩子叫成
`normalizeClassroomRecord`（v31 之后它其实叫 `normalizeClassroomStrict`）—— 名字写错的话，
照着文档去搜代码的人会搜不到。

### E10：删掉「科目系数」这一维 + 「班级系数」改叫「人数系数」（v38 / 数据库 v29）

机构原话：「**科目系数可以删除，班级系数改成人数系数**」。两句合起来是一件事的两半：
**科目那一维整个不存在了**（不是"先按 1 记着、以后再定系数"，是连这一维都不要了），
而系数那一处只是**换了个叫法** —— 它讲的本来就是**人数**，不是"班级"。

**新的报价口径**（价格维度从 4 个变成 3 个）：

```
课单价（元 / 节） = 基础价（元 / 小时） × 人数系数 × 时长乘数
                   （报 1 节时再加 10% 手续费）
```

三个维度：**基础价（元 / 小时）、人数系数、时长乘数**；外加**手续费**与**试课**两条规则
（后两条是"规则"，不是乘上去的维度）。**「科目」这一维整个没有** ——
价目表里不再有 `#### 科目:` 那一行，报价公式里也没有那个乘法。

**「人数系数」就是原来的「班级系数」，含义一个字没变**：

| 班型（名字不变） | 人数系数 |
| --- | --- |
| 一对一 / 一对二 / 一对三 / 一对多（4-8） | 1.0 / 0.7 / 0.6 / 0.5 |
| 班课（9-20） | 不按系数：按「**教师课时总费用 ÷ 班级人数**」分摊（这条说法不变） |

改的只是**叫法**：文档里、页面上、提示语里的**当前口径**一律写「**人数系数**」，
旧名只在"它以前叫什么"这类历史说明里出现（否则后来的人看到旧名找不到对应）。
**代码标识符一律没改**（`classTypes` / `PricingClassType` / `coefficient` 都保留 —— 改名会牵动
契约形状与老库数据，不值当），改的是注释与人话。
**「班型」这个名字也不变**（一对一 / 一对二 / 一对三 / 一对多（4-8）/ 班课（9-20））。
网站报价页在班课模式下那个「**班级人数**」输入框**保留原名**：它问的确实是班级人数，
只有"系数"那一处改叫人数系数。

**删掉的东西**（科目那一维的整条链，一处不留）：

| 位置 | 删了什么 |
| --- | --- |
| 配置类型 | `PricingConfig.subjects` 字段与 `PricingSubject` 类型 |
| 校验 | 配置校验里"科目必须指向存在的阶段、名字得是该学段真有的学科"那一条（`validatePricingConfig` 里删掉它；后台API约定 §5 的对应口径也从三条变两条） |
| 报价明细 | 家长看到的试算明细里那一行（以前会列出科目与它的系数） |
| 公式 | `课时价 = 基础价 × 科目系数 × …` 里的那一项；`pricing.quote` / `pricing.teacherFee` 的**选择项里也没有科目了** |
| 后台 | 「报价」页那一整块「科目系数」编辑区；试算器里的「科目」下拉 |
| 网站报价器 | 「选科目」这一步。步骤从 6 步变 5 步：原来的「学段 → 课程 → **科目** → 班型 → 每节课时长 → 节数」，现在是**学段 → 课程 → 人数（班型） → 每节课时长 → 节数** |
| 内容数据层 | `SubjectGroup` 类型与 `subjectGroups`（`data/site/pricing.md` 的 `#### 科目:` 行不再解析；内容文件里还留着那一行的也不报错，只是**忽略**） |
| 公开数据 | `PublicSite.pricing.subjects`（上网的那一份里也不再带科目） |
| 内容文件 | `data/site/pricing.md` 里的 `#### 科目: …` 行（导出的 Markdown 也不再写它） |

**为什么删掉不改变任何已算出的价**（这四条一起成立，才敢删）：

1. **那份表里的系数全都是 1**：`data/site/pricing.md` 里一个 `科目 ×系数` 的写法都没有
   （旧内容里也没有），×1 乘不乘结果一样；
2. **试课费**本来就按课程**基础价原价**收，不带任何系数 —— 科目有没有都轮不到它；
3. 教师课时费的「标准单价」口径以前是"基础价 × 科目系数"，系数为 1 时它就是**基础价本身**，
   删掉那个 ×1 之后课时费一分没变；
4. **迁移只删字段、不做任何换算**（不算钱、不重算历史报价）。

**迁移（v28 → v29）做了什么**：

- `CURRENT_VERSION` **28 → 29**，`VERSION_NOTES[29]` 写着这一轮改了什么；
- `migrate()` 里新增 v28 分支：**`delete db.pricing.subjects`** ——
  用 `delete` 而不是置成 `undefined`：留一个 `subjects: undefined` 的键，
  "这份配置里到底还有没有科目"就变成要看序列化实现才知道的问题
  （`JSON.stringify` 会把它丢掉，而内存里那份对象还带着这个键）；
- 迁移链**收尾归一**那里再兜一次同样的 `delete`（声称的版本号不是证据 ——
  手改过的导出、只跑了一半的恢复都可能长成这样）；
- 迁移前服务端照例先备份（`nexgenedu-migrate-<时间戳>.db`）。

> **别与另一处 v29 混**：使用手册 / 内容维护手册里把「课程库 + 课程类型 + 开放矩阵合并成一页」
> 那一轮也叫 v29，那说的是**页面合并**；这里的 v29 是**数据库结构版本**。

**顺带把两种「课程单价」口径钉清**（教师分成那个开关，`priceBasis`）：

- `course`（标准单价，默认）= **基础价**（以前是"基础价 × 科目系数"，系数为 1，所以数字一样）；
- `seat`（班型课时价）= **基础价 × 人数系数**，即家长每生实付的课时价。这条含义没变。

**文档同步**（这一轮改的全是文字口径）：使用手册 §12（§12.1 的维度表从四个改成三个、
§12.2 删掉「选科目」那一步、教师分成与 blank 那几处）、内容维护手册 §7（整个删掉
「科目与科目系数」一节、原「班级系数」一节（现 §7.2）改名「人数系数」并顺移节号、
`pricing.md` 文件说明表、教师分成口径、blank 那行）、后台API约定 §5（计价模型表、
`subjects[]` 那段、两种单价口径）、技术架构（§7 报价与教师分成、§5.1 的 blank 表、
班型那一行）。PROJECT.md 自己这里的旧口径也一并改了，E9 里那些**历史事实**
（"改之前有科目系数 19 行"）保留原样并标成旧口径。

### E9：以课程清单为准，把网站的报价整过来（v37）

机构：「**现在以课程清单为准，整理一下网站的报价**（公式依旧不变，主要修改的是课程名称
以及分类这些）、学生信息记录等各个需要调整的部分」。

**先审计出来的事实**（这条错位不是"名字不好看"，是两套结构根本对不上）：

| 报价页（改之前） | 课程库 |
| --- | --- |
| 6 个组：小学 / 初中阶段 / 高中阶段 / 出国考试 / 专业英语 / 成人·兴趣 | 5 个学段：小学 / 初中 / 高中 / 大学 / 其他类型 |
| 20 门"课程"，按**年级与备考档位**分档：七年级课本 220、八年级课本 260、九年级课本 300、中考冲刺 350、高中必修 300、雅思口语 800… | 32 门课，按**学科**：初中数学、高中数学、雅思… |
| 科目系数 19 行（**旧口径**：这一维在 E10 里已经整个删掉），挂在上面那 6 个旧组名下 | 43 个学科，按 5 个学段排 |
| **20 门课一条 `courseId` 都没关联上** | **32 门课一门都没被定价** |

最后一行是最要命的：v25 那套"报价跟着课程库走"（改名跟随 / 停开跟随 / 失效标记）
**完全空转** —— 名字粒度不同，谁也没认领谁，家长看到的是一份与真实课程无关的价目表。

**机构定的四件事**（我列了选项请他们选，因为价格只有他们能定）：

1. **分组**：按 **5 个学段**（小学 / 初中 / 高中 / 大学 / 其他类型）；
2. **旧价怎么落**：**沿用同组旧价的入门档**（小学 150 / 初中 220 / 高中 300 / 雅思 700），
   之后在后台报价页逐门改；
3. **报价里有、课程库里没有的那些**：**不要删，建成课程库里的课**；
4. **学生信息记录**：下一阶段再调整（这一轮不动）。

**做出来的东西**：

- **十二门"只在后台用"的课**（`lib/backend/extra-courses.ts`）：小学奥数 260、小学英语竞赛 260、
  小升初 200、中考冲刺 350、提前招专项 400、高考冲刺 400、特殊计划专项 500、医学 / 机械 / 贸易、
  成人旅游、出行 100、跨国交友。课程清单 **32 → 44 门**。
  它们**不新增网站卡片**：网站上每张卡片都要在课程正文里配一段小节（自检有「每张卡片都有页面数据」
  钉着），凭空多出十来张没有正文的卡片，课程页会变成一排点进去空白的条目 ——
  那不是"以课程清单为准"，那是把网站弄花。要上网随时说一句（填「卡片」与网址两个字段）。
  **空库起步（`initial.ts`）、示例库（`seed.ts`）、老库升级（v11 → v12 迁移）三处共用这一份定义** ——
  只改一处的话，新装的库又会回到"报价 44 门、课程库 32 门"那个错位。
- **报价配置重建**（走真实后端的 `pricing.update`，不是手改文件）：4 个组（大学这个学段现在没有课，
  建不出组 = 校验会拒空组 + 家长会看到空选项）、44 门课全部带上 `courseId`、科目系数表按
  新学段重建（小学 4 / 初中 6 / 高中 15；「其他类型」那一组不分科目，与旧配置里
  出国考试/专业英语/成人兴趣的处理一致）——**这张表后来在 E10 里被整体删掉了**
  （当时每一个科目的系数都恰好是 1.0，所以删掉没有改过任何价）。
  公式、人数系数（当时还叫「班级系数」）、时长、计费规则、教师分成、
  试课、其他项目**一个字没动**。
- **内容文件同步**：`pricing.exportMarkdown` 导出 → 替换 `data/site/pricing.md`
  「`## 学习阶段`」及其后的部分（页面上写的就是这个流程），`npm run sync-content` 重新生成
  `data/site/pricing.ts`。改完 `/quote` 上就是新价目表（已用真实页面核对）。

**顺带修掉的两个真错误**（都是加断言时当场现形的）：

| 位置 | 错在哪 | 后果 |
| --- | --- | --- |
| v11 → v12 迁移 | 课程是按**名字**从内容文件灌进来的，**没有 id** | 从 v11 升上来的库，课程一门都改不动 / 删不掉（id 是台账里改名、删除、挪分区、报课、排课的引用键）。原先自检只数了条数，所以一直绿着；现在 v18 那一步补 id（`course-site-<卡片路径>`，与新装系统同一套） |
| `syncPricingWithCourses` | 只要 `syncLibraryLinks` 有变更就把配置来源标成「后台修改」 | 补一条 `courseId` 不是机构改过价 —— 标过之后报价页那句"这份配置来自站点内容"成了假话，机构会以为有人动过价。现在**只补关联时不改来源**（关联仍然写日志） |

**自检新增第 41 节（13 条）**，断的是"结构上必须成立"的那一类，而不是把当前清单再抄一遍：
报价的课程名集合 = 课程库的课程名集合；空库与示例库同一份清单；报价的每个阶段都真有该学段的课；
课程库每门课的学段都能在报价里找到组；报价每门课都有价或明确「暂未开放」；
那十二门课都在报价里、都不上网、维度都挂好；网站卡片数没变（还是 32 张）；
整份配置能被 `syncLibraryLinks` 一次认全（不留未关联的行）。

**还需要机构定的**：法语 / 德语 / 西班牙语原来没有价，我按**高中的入门档 300** 填的
（它们是开放课程，不能空着）—— 觉得不对在报价页直接改；「学生信息记录」按机构的话留到下一阶段。

**门禁**：tsc / eslint / check（含第 41 节）/ check:both / check:auth / accept 116/116 /
build（out/ 82 个 HTML）/ check:404 / check:links 全绿。

### E8：教师 / 教室的「从网站导入」也删掉（v36）—— 三个入口全部清完

v32 删掉课程那两个入口之后，我回头问了机构一句：**教师 / 教室面板里还有一个
「从网站导入」（`imports.fromSite`），要不要一起删？** 机构答：「**要一起删掉**」。

于是"从 Markdown 反推库"这条路**整块没有了**（课程 v32、教师与场地 v36）：

| 删掉的东西 | 说明 |
| --- | --- |
| `imports.fromSite` | 契约里的方法（支持实体只有 teachers / classrooms），走的是逐行导入 |
| `lib/backend/import.ts` 的 `siteImportRecords()` / `SiteImportSource` | 读网站内容、产出"与 CSV 导入同一种形状"的记录（**这一步是它设计得好的地方**：落库走同一套判定） |
| `lib/backend/site-import.ts` 整个文件 | 59 行：v32 之后只剩 `siteTeachers()`，而它**已经没有任何调用方**（自检那段改用夹具了）—— 留着就是死代码加一句已经不对的文件头注释 |
| 批量导入面板里的"来源选择" | 面板现在只有一条路：机构自己的 CSV / JSON 文件 |
| 自检里那一节 | 改成断言"**入口删干净了**"（第 12 节那一组，11 条） |

**保住的东西**（删的时候最容易连坐）：批量导入（CSV / JSON）、下载模板、预览与行级报错、
冲突三策略、导入前自动留快照 —— 以及那套"**体检 → 写入**"的两阶段流程
（`runTwoPhaseImport` 的三条断言就在同一节里）。这些都是机构自己的数据进库的正路。

**要说清的口径后果**：新装系统里**教师与教室是空的**。`initial.ts` 的"空库起步"那张表
一直把学生 / 教师 / 教室 / 排课 / 收款列在"机构自己要一条条录"那一栏，课程库与报价配置
是例外（网站上的公开信息，建库时灌好）。那个按钮以前是"从网站拉一份名单"的**唯一入口**，
删掉它就等于：**装好之后教师与教室要录一次**（网站上那 8 位教师含 2 位 AI 智能体、
3 个场地名，十几分钟的事）。

我没有替机构把这 8 个人自动灌进初始状态 —— "初始状态是空库"是**写进设计文档的取舍**
（早期"空库自动灌示例学生"踩过一次：员工以为那是自己录的）。因此把它**写成一条断言**
（`新装系统里教师与教室是空的`）：哪天要改成自动灌一份，必须先改那一行，而不是悄悄多出 8 个人。
**这一条我在交付说明里明说了**，机构要改口径随时说一句就行。

**顺带清掉的过时注释**（都是"功能删了、注释还在说它存在"）：
`api.ts` 里 `npm run server:import-site`（**这个脚本根本不存在**，真名是 `server:import`，
而且那是灌 JSON 备份的）、`site-copy.ts` / `data/site.ts` / `data/pages.ts` / `data/featured.ts`
把"从网站导入"列为一条现存路径、`seed.ts` 的"与从网站导入的结果一致"、
`courses.ts` 三处"下次从网站同步又会把它拉回来"（入口没了，但**那条性质还在**：
网站来源的课 id 与卡片路径绑定，建库 / 恢复备份都会照内容文件把它再放回来 —— 现在这么写）。

方法数 110 → **109**。自检新增一组 **11 条**断言（契约 / 服务层 / 页面 / 那个模块已删 /
批量导入还在 / 两阶段流程还在 / 注释不再指向不存在的脚本 / 空库那两条口径）。
文档同步：使用手册（§13.4 整节重写为"教师与场地：新装系统里是空的，要录一次"、
§15.2 从"点从网站导入没反应"改成"点批量导入没反应"、§7.0.x 那句"可以从网站一键导入"）、
后台API约定、技术架构、README 与后端开发方案里的方法数、本文件。

### E7：修掉一个真 bug —— 「课程」页点课程类型 / 开放矩阵没反应

机构报的现象，我复现了根因：**合并三个页面的那一版（v29）我自己引入的**。

选页签的 effect 依赖写成了 `[tabs]`，而 `tabs = ADMIN_COURSE_TABS.filter(...)`
**每次渲染都是新数组** —— 于是那个 effect 每次渲染都跑一遍，把刚点下去的页签
立刻重置回第一个；而且它与"把页签写进地址"的 effect 组成了来回改状态的循环。
用户看到的正是"点了没反应"。

三处修法（每一处都有对应的断言）：

1. `tabs` 改成 `useMemo`（**这不是优化，是正确性**），依赖只认 `roles`；
2. 选页签的 effect **只在挂载时跑一次**（`useRef` 守卫 + 空依赖），
   `roles` 之后变化不再把用户正在看的页签拽回去；
3. "选哪一个页签"的逻辑抽成纯函数 `pickInitialCourseTab()`（放在 `lib/auth/roles.ts`，
   页面只允许导出 default，多导出会被 `next build` 的类型校验拒掉）——
   逻辑抽出来之后可以被逐条断言：锚点优先、锚点不允许时退到第一个（教师拿到 `#matrix`
   不会看到空壳）、一个都进不去时返回 `null`（页面给说明而不是空白）。
   另外补一条：角色变化后当前页签**真的**不允许时才退，避免来回抖。

自检新增第 40 节（12 条）：7 条纯逻辑 + 5 条页面结构（页签列表必须 useMemo、
选页签只跑一次、用那个纯函数、角色变化兜底、面板 hidden 保活）。
**这一节的存在意义就是这类 bug 下次会被断言挡住** —— 上一次它躲过了 8.8k 行自检，
因为那些断言都在看"源码里有没有某句话"，而这次的问题在"effect 什么时候跑"。

### E6：自检与验收脚本纳入类型检查（清掉最后一块"看不见的地方"）

`tsconfig.json` 的 include 一直是 `**/*.ts` + `**/*.tsx` + `server/**/*.mts` ——
**通配不匹配 `.mts`**，所以 `scripts/*.mts`（自检 8.8k 行 + 逐页验收 1k 行）从来没被 `tsc` 碰过。
当时的取舍写在注释里："自检出错是响亮失败，后端出错是静默改数据，所以先保后端"，
还记着"115 处历史类型债（check.mts 93、accept-check.mts 21）"。

那些债的**根因其实很集中**，清完只花了一轮：缺 9 个类型 import（`Lesson` / `Student` /
`Classroom` / `Teacher` / `Inquiry` / `LessonRecord` / `HomeworkRecord` / `Assessment` / `Database`
—— 类型注解会被 strip，所以裸用也照样跑）；夹具少几个新字段（`version`、`makeupForLessonId`、
`leaveRequestedAt`、`unitPrice/agreedAmount/paidNow/method`、课程的 `stageIds/subjectIds/moduleIds`）；
以及十几处 strict 下的 null 收窄。

**清账时抓到四处真错误，全都是"看起来在断言、其实没有"**：

| 位置 | 错在哪 | 后果 |
| --- | --- | --- |
| `accept-check.mts` 「标记已上动作」 | `markCompleted(...).skipped === false` —— `skipped` 是数组，永远不等于 false；而 `check()` 在没有 `expect` 时**只看抛不抛错** | 那条断言什么都没验（现在是"提前请假 → 一个都没扣，且原因写着请假"，并且它当场抓出我对 `skipped` 语义的误解） |
| `accept-check.mts` 课堂记录 | `interaction: "好"` —— 合法值只有 主动 / 一般 / 被动 | 一直往库里写了一个非法枚举值 |
| `check.mts` 开放矩阵那一节 | `offersOfDimension(rows, "delivery", …)` —— v26 删掉"交付形态"之后留下的 | 查不到的字段返回空数组 → 断言**空转通过** |
| `scripts/diff-site-content.mts` | 读 `publicSite.coursePartitions`，真实字段叫 `partitions` | 脚本一跑就抛（`npm run site:diff` 之前是坏的） |

另外清掉两处"静默忽略"：`walkApi(api, "", realMethods)`（函数只收两个参数）与
`check.mts` 里重复 import 的 `HolidayDay`。

现在 `npm run typecheck` = `tsc --noEmit`，覆盖 `app/` / `components/` / `lib/` / `server/` / `scripts/`
的**全部** `.ts` / `.tsx` / `.mts`；自检新增第 38.5 节两条断言盯住 include 不再回退。

### E5：课程台账按维度分组（v33）—— E2-B 落地，分区视图保留

E3 把课程挂到了维度上（能看、能筛、能挂），这一步把**分组**也换成维度：

- 清单上方多一个「**分组方式**」开关，**默认「按维度（学段 → 学科）」**（机构口径：以维度法为主）；
- 「**按分区（网站栏目）**」保留 —— 分区**仍然是网站的展示结构**（课程页按栏目分组、
  每个栏目在网站上都有入口），而分区改名 / 排序 / 删除 / 批量移课那几个动作**挂在分区表头上**，
  因此这一种视图不能删。等 E2-C-B（网站也由维度生成）做完才能撤；
- 读不到课程类型时**强制分区视图**（否则所有课都归到"未挂维度"里，看起来像清单坏了）；
- 三条分组口径：多学段的课在每个学段下都出现（按学段看才完整）；**多学科卡片**（高考外语）
  归到「多学科卡片」那一桶**只出现一次**；没挂维度的课**不进**维度分组；
- 两种分组**复用同一张课程卡片**（`renderCourseCard`），卡片上的维度标签、报价状态、
  两层开放提示在两种视图里完全一致。

自检新增第 39 节（**9 条**）。

**顺手修掉一处过时注释并钉住它**（第 35.5 节 9 条）：`lib/site/backend-source.ts` 的文件头
一直写着"首页文案 / 关于 / 联系 / FAQ / 课表 / 特色课程 / 品牌与联系方式**不在库里**，
永远来自 `data/site/*.md`" —— 那是 v19 之前的实情，v19–v22 搬进库之后这句话就错了，
而**没人会发现**（注释不影响运行，页面照常工作，直到有人照着它去改代码）。
现在文件头改成"已经没有'永远来自模版'的页面了"，并新增 9 条断言盯住"那八块的取数都走同一套三态判定"。
第一版断言按"函数体里必须出现 `siteContentSource()`"来查，把走 `copySourceFor()` 的五块
误判成不合规 —— 报的不是代码错了，是**判据没覆盖那种写法**；现在两组写法各断言一次。

### E4：删掉「从网站同步课程 / 从网站导入内容」两个入口（v32）

机构：「**从网站导入课程和从网站同步课程的功能可以删掉了，现在都以后端为主了**」。

删掉的是**运行期**这两个入口（后台「课程」页工具栏上那两个按钮）：

| 删掉的东西 | 说明 |
| --- | --- |
| `courses.syncFromSite` | 把内容文件里新增的课程卡片拉进库（只增不改） |
| `site.importFromContent` | 把教师资料 / 课程卡片字段 / 课程正文 / 报价文案搬进库（体检 → 写入，默认只补空） |
| `lib/backend/site-import.ts` 的导入实现 | 347 行 → 59 行：只剩 `siteTeachers()`（自检拿它当"内容文件里那几位教师"的基准） |
| `mergeSiteCourses()` | 随同步入口一起没了（"只增不改"那个语义只服务它） |
| 自检里那一整节 | 285 行（约 20 条断言）—— 断的是已删掉的功能，留着就是空转 |

**新装系统的初始数据仍然来自内容文件**（`initial.ts` → `materializeSiteCourses()`）：
那是"空库起步"，不是运行期的导入 —— 空库没有课，排课与报课的下拉就什么都选不到。
被删掉的是"**库已经在了，还从 Markdown 反推**"这条路。老库要用 JSON 导入/恢复
（「数据与备份」页），不再从 Markdown 补。

**「批量导入」（从 CSV / JSON 文件导入）保留** —— 那是另一件事（机构自己的文件）。
教师 / 教室面板里的「从网站导入」（`imports.fromSite`）当时**没动**：机构只点了课程那两个。
> **后记（v36）**：删完课程那两个之后我回头问了这一句，机构答「**要一起删掉**」——
> 于是教师 / 教室那一路也整块删了，见下面 E8。

方法数 112 → **110**；自检里新增一组断言钉住"删干净了"：契约里没有这两个方法、
服务层上没有它们、页面上不再调它们、而「批量导入」还在；页面上也写明了那两个入口已删
（免得下一个人以为功能丢了）。文档同步：使用手册（§0 上手四步、§7.0.2、§7.0.5、§13、§15.2）、
内容维护手册、技术架构、后台API约定、后端开发方案。

### E3：课程挂到维度上（v28）—— 「不靠枚举」的第一步（E2-A），网站不动

机构：「**课程可以完全按照我后面说的不靠枚举的方式为主安排**」+ 三条口径：
**两层开放状态都留、各管一层**；**先做 A**（网站展示暂时不动，B 单独设计）；其余听我的。
我据此定：**高考外语不拆**（它是一张卡片覆盖五个语种，挂 `subjectIds` 列表即可，
拆卡片会改网站，那属于 B）。

**数据**（v28）：`Course` 加 `stageIds` / `subjectIds` / `moduleIds`（全部按 id 引用；
`subjectIds` 是列表，因为"打包卖的课"是真实形状）。

**怎么挂上**（`suggestCourseDimensions`）：显式对应表 → 学段前缀 + 学科名 → 学科名本身；
**对不上留空**，台账里列出来让人手选（不猜）。

**顺手补掉 5 个"枚举尾巴"**：机构在卖的 5 张卡片（高中技术 / 编程与信息素养 / 成人英语口语 /
成人零基础外语 / 出国语言备考）在维度表里原本**没有对应行** —— 那它们就永远挂不上维度，
而机构的口径是"以维度法为主"。于是补进种子（技术 = 高中下的学科、其余 = 其他类型下的学科/项目，
暂放顶层、机构要归组自己在「课程类型」页拖一下）：种子规模 43 学科项目 / 100 内容模块，
**32 张卡片全部能挂上**（自检里有一条断言钉着"每一张卡片都能挂上"）。

**自查发现并修掉一个真漏**：第一版只在迁移里挂课程，于是**空库起步 / 示例数据 / 从网站同步（当时还有）**
建出来的课全是"没挂维度"—— 而新装的系统根本没有老库可迁移，等于这个功能对新装系统完全不起作用。
现在判据只有一处：**谁手里有维度表，谁负责挂**（`materializeSiteCourses(partitions, site, catalog)`
与 `mergeSiteCourses` 的调用点都把维度表传进去），自检里三条断言钉住（示例数据 32/32、
带维度表挂上、不带就留空、同步回来的课也挂上）。

**页面**（这一版是"能看、能筛、能挂"，分组仍是分区）：

- 每张卡片显示这门课的维度标签（`小学 · 语文`；高考外语会显示五个学科标签）；
- 清单上方「按维度筛：学段 / 学科」+「只看未挂维度」；搜索也认维度名；
- 一块黄色提示「有 N 门课还没挂到课程类型上」+「只看这几门」→ 逐个挂；
- 表单里「所属维度」三个选择器（学科/模块按 id 引用，模块只列所选学科下面的）；
- **两层开放互相提示**（`opennessHint`）："矩阵里开放着 N 条组合" / "这门课是开放，
  但矩阵里一条组合都没设过" / "组合全关着，与开放矛盾" —— 不拦，只说。

**没做的**（下一步 E2-B，需要单独一轮）：台账**按维度分组**（要把分区表头的管理动作搬进分区面板，
因为网站展示仍按分区）、以及 E2-C-**B**（网站展示也由维度生成 + 旧路径映射）。

自检新增第 38 节（**42 条**）：按名字挂的四类规则、32 张卡片全挂上、六类引用校验、
两层开放的四种提示组合、迁移（32/32 挂上）、新库与同步路径、API 闸门、页面接线。
逐页验收新增 5 条（**118/118**）。

### E2：日历加月视图（v30）

机构：「**日历再加个月视图**」。周视图与月视图是**同一件事的两个缩放级别**（同一条数据、
同一个"点开某天看明细"面板），因此它不是一个新页签 —— 而是看课那一块左上角的**周 / 月切换**
（页签留给"不同的事"：看课 / 假期与作息）。切换时锚点不动：从"9月23日那一周"切到月，看到的就是 9 月。

- 月历形状：`monthGrid()`（`lib/backend/format.ts`）—— **周 × 7、从周一开始**（与 `weekDays` 同一口径，
  两处不一致会把同一节课画在不同的列上），含前后补齐的天（2026-02 是周日开头 → 补齐到 35 格；
  2027-02 正好周一开始 → 就是 28 格，不做多余补齐）。隔壁月的天淡显。
- **卡片与周视图完全一样**（机构 v31 更正：「月视图应该和周视图的卡片一样，只不过是按照日历的
  排布顺序」）：日卡片抽成 `DayCard`，两个视图共用 —— 因此不会出现"改了一处、另一个视图还是老样子"。
  第一版做的是"另一种精简格子"（只有日号 + 节数），被机构否掉了。
  摆法是"一周一行、周一到周日七列"；**上个月 / 下个月的卡片淡一档**（`dimmed`，机构要求）；
  窄屏不把七列压扁，而是整块横向滚动（与「开放矩阵」页同一个做法）。
- **读的区间跟着视图走**：周读 7 天，月读**整张格子**（含补齐的天）—— 不补齐的话那几格会显示"0 节"，
  而那里其实有课；"看起来空着"比报错更难查。
- 翻月用 `shiftMonths()`（锚到当月 1 号）：直接给日期加一个月的话，1 月 31 日会变成 3 月 3 日
  （`Date` 把不存在的 2 月 31 日往后规整），于是"下一月"跳过了 2 月 —— 这类 bug 只在月末几天出现。

自检新增第 37 节（**17 条**）：格子形状（7 的倍数、周一开始、含整月与补齐的天、两个 2 月的不同形状）、
翻月（1/31 的下一月是 2/1、12 月跨年、上一月同样锚 1 号）、页面接线（周/月切换、共用 selectedKey、
range 与格子首尾一致、用 shiftMonths、"不在本月"提示）。逐页验收新增 1 条（**113/113**：
月视图区间在一个多月跨度上照样查得到课）。

### E1：课程库 + 课程类型 + 开放矩阵合并成一页（v29）

机构：「**课程库、课程类型、开放矩阵这里面很多功能也完全可以合并到同一个页面里**」，
并定了两条：**网址也只留一个**、**课程以"不靠枚举"的维度法为主安排**（后者见 E2 待办）。

三块本来就是"课程"这件事的三层，日常还来回跳：加一门课要"课程库 →（发现没有这个学科/班型）→
课程类型 →（回来）→ 开放矩阵勾开放"。现在是一页三个页签：

| 页签 | 原页面 | 保存语义（**必须各自保留**） | 角色（与合并前逐字一致） |
| --- | --- | --- | --- |
| 课程台账 | 课程库 | **逐条**保存 + 乐观锁（记录 + 网站正文 + 报价一次交） | 技术 / 招生 / 教师（只读） |
| 课程类型 | 课程类型 | **整份替换** | 技术 / 财务 / 招生 |
| 开放矩阵 | 开放矩阵 | **整份替换** | 技术 / 财务 / 招生 |

**最容易犯的错是把三块合成一个保存按钮**：那会把"改一门课的名字"变成"提交整份维度表"，
乐观锁与"改了三处只有两处落库"两个问题一起回来。因此页面壳**自己没有保存按钮**，
自检第 36 节用一条断言钉住这件事（以及"三个面板各有一个保存动作"）。

**实现**：三块各拆成 `components/admin/*Panel.tsx`（沿用仓库既有的 `*Panel` 约定），
页面只做页签壳；页签**去过就留着**（`hidden` 保活，切回来草稿还在 —— 卸载等于把没保存的
改动悄悄丢掉）；锚点 `#ledger / #dimensions / #matrix` 可直接分享，且**先过权限**
（教师拿到 `#matrix` 链接不会看到空壳）。路由权限取三个页面的**并集**，
页签按 `ADMIN_COURSE_TABS` 把关（机构说"先不管权限"，于是**不放开也不收紧**）。

**顺带**：自检里"带加载态的后台页面……"那一组原本只扫 `app/admin/**/page.tsx`，
拆分之后**扫描集合扩到 `components/admin/*Panel.tsx`** —— 否则拆分就等于把一整组断言
（裸 load 只许一处、刷新必须安静、不许有定时器）悄悄放走了。指向旧三个页面的
11 + 2 + 1 条断言一条没删，只改指向；新增第 36 节（13 条）钉入口唯一、保存语义独立、
权限逐字一致、草稿保活、旧路由确实删掉。

**旧网址**：`/admin/catalog`、`/admin/offers` 连同目录一起删掉（机构口径"网址只留一个"，
不留转发页）。

**C1–C4 合起来就是那次重构的完整目标**：课程类型从"枚举式课程清单"变成"四个维度 + 稀疏组合 + 引用维度"，
每一阶段独立提交、全部门禁绿、文档同步更新。


### B1+B2+B3：页面文案块进库（v22）—— 五块共用一套模型与一个读取接口

这一批把最后五块"只在模版里"的文案搬进库：**品牌与联系方式 / 首页 / 关于我们 /
联系我们 / 课程时间安排**（`siteContent.copy`）。至此 `data/site/*.md` 的角色变成
"没连后端时的骨架来源"与迁移初值 —— 前台网站**能连后端就没有一处来自模版**。

**关键设计：五块共用一套模型 + 一个读取接口**

它们形状完全一样（短字段 + 分组），因此共用一个 `SiteCopyBlock` 与一个 `CopySource`：

```
模版（*.md 解析结果） ─┐
                       ├─→ CopySource ─→ brandFrom / homeFrom / aboutFrom / contactFrom / scheduleFrom
库（siteContent.copy） ─┘                 （**一份映射，两种来源**）
```

为什么值得这么改：两处各写一遍字段映射（`pageString(page, "phone")` vs `field(block, "phone")`）
必然会漂，而**漂了不会报错**（两边都返回空串）。共用映射之后，"两条来源产出同一份页面数据"
是**结构上成立的**；自检里六条等价断言把它钉住。

**开发中被这套断言/逐页 diff 抓到的三个真问题**（都已修）：

1. **`trial_points` 的分隔符**：旧代码对它是 `split("|")`，而 `keywords` 是解析器给的数组。
   我第一版把两者混成"一律按顿号/逗号切"，首页试课要点的 ✓ 位置因此变了 ——
   只有逐页 diff 才看得出来，现在 `list()` 分两条路各自照抄原来的读法；
2. **时间安排取错了文件**：它单独在 `schedule.md`，我第一版按 `content.md` 的页面名去取，
   结果"分组全没了"（自检里"分组数 / 分组名 / 每组的时段"当场报红）；
3. **分组白名单**：「全站」页里除了品牌短字段还有「课程栏目」（那是课程库的分区数据），
   不过滤就会把 32 条课程卡片当成品牌文案搬进来。现在每块声明自己该有哪些分组
   （`SITE_COPY_GROUPS`），时间安排那种"列举型"块用 `"all"`。

**顺带（当时）**：那条「从网站导入内容」的路（v32 已删）也覆盖这五块（空的补、`overwrite` 时整体替换）——
这既是"内容文件 → 库"这条既有路径的补全，也是"迁移搬歪了"的补救入口（真实库就是用它重铺的）。
另外把 `SiteCopyGroup` 的"分组说明"从 `note` 改名为 `description`：它是**会渲染的公开文案**，
而公开数据里有一条断言"不许出现 `note` 这个键"（那个键名专指内部备注）。

自检新增第 29 节（23 条）：迁移（五块都在 / 字段与分组数与内容文件一致 / 品牌那块没有分组 /
id 齐全）、**模版与库产出同一份数据**（六个取数函数逐字节比对）、blank 口径（短字段与分组标题在、
条目空；品牌整份保留）、四类校验、后台编辑器挂载。逐页验收新增 5 条（85/85）。

**实测**：真实库升到 v22 后，五块的字段/分组/条目数分别为
brand 14/0、home 38/3/13、about 7/4/16、contact 7/1/6、schedule 4/4/12；
生产构建后端态下首页、关于、联系、时间安排逐页渲染正确（页脚电话、试课要点都在）。

### 口径修正：`blank` 不是"整页空白"，是「骨架在、条目空」+ B3 常见问题进库（v21）

机构看了上一版之后连说两句，两句合起来才是完整口径：

1. 「在后端未连接的情况下前端需要用到后端数据的部分应该是空白的」→ 我把**整块**都弄空了，
   于是 `/teachers` 与 `/cases` 打开是**全空**（连标题都没有）；
2. 「教师界面和学生案例不能全空，得有分区标题呀！！！」→ 标题与**分区标题**必须留着。

现在的定义：**`blank` = 模版骨架 + 空条目**。
骨架 = 页面眉题 / 标题 / 说明、**分区标题**（FAQ 的分组标题、课程页的「特色课程」「课程总览」）、
页脚提示、报价页的按钮与标签文案；条目 = 教师列表 / 课程栏目与卡片 / 学科正文 / 报价阶段 /
案例列表 / 特色课程树 / FAQ 问答。每个空块的页面上再说一句话（「暂无展示中的教师」
「案例整理中」「这一组暂时没有问题」…）—— 只有标题也会让人以为坏了，这一句是给机构看的。

**这一轮同时把常见问题搬进库（v21）**（机构要求"常见问题也要进后端，以后端内容为主，
前端只根据后端"）：`siteContent.faqPage` = 标题区 + 提示 + 分组（分组标题 + 问答），
迁移照例从 `data/site/faq.md` 灌初值（43 条问答），后台「网站内容」页可增删改与调分组顺序。

**校验**：分组标题非空 / 不重名（它就是页面上的分区标题）；问题与答案都不为空；
分组可以零条问答（组标题照常显示）。

**顺带修掉一条自己的假判据**：公开数据的边界断言里有一条
`!JSON.stringify(publicSite).includes("系数")` —— 它是个很粗的代理判据：报价页本来就公开
「人数系数」这类**价格输入**（只是恰好用英文键名 `coefficient` 装）。
FAQ 进公开数据后，问答里写着"基础价 × 人数系数"，这条断言立刻**误报**。
现在改成查三个只属于教师分成的字段名（`teacherShare` / `basePercent` / `stepPercent` / `priceBasis`）。

自检：第 25 节改成"条目全空 + 骨架必须在"（逐个块比对模版骨架的标题 / 分区标题）；
新增第 28 节（22 条）：v21 迁移（组数 / 问题总数 / 分组标题逐条 / id）、保存与三类校验、
"骨架在、条目空"（FAQ / 案例 / 教师 / 特色课程）、四个页面的空状态文案、后台编辑区。
逐页验收新增 4 条（80/80）。反向验证：把迁移改成"不灌初值" → 若干条报红。

### B4：特色课程进库（v20，「特色课程也该来源于后端」）

机构指出「特色课程应该也来源于后端呀，为什么现在前端还有」—— 它原先确实只在
`data/site/featured.md` 里（B4 还没做），因此后端没起时照样显示。这一版把它搬进
`siteContent.featuredPage`，与案例同一套做法（整块编辑、整块保存）。

**几个刻意的决定**：

- **嵌套结构，不拆成"一行一个课程 + parentId"**：特色课程本来就是一棵**有层级的内容树**
  （一级 → 二级 → 三级，每级都有独立页面），而且它整块一起编辑 —— 没有"单独改某一行"的场景。
  课程分区当初拆成行，是因为它**是**结构数据（一门课要引用某一区、还要排序）；
  这里的层级只属于它自己，嵌套结构本身就是那棵树；
- **`slug` 与课程名分开存**：网址用的是 slug，**改名不会让网址失效**（这是 v20 之后的一个真实改进：
  以前没写「路径」字段的课程，改名会让它的页面地址跟着变）；
- **最多三级**：网站的导航与页面结构只做到三级，第四级"存得下、没有任何入口"，因此直接拒绝；
- **删除护栏两条**：有子课程的不能删（下面那些三级课程会失去入口）；
  **名字还被某门课当班型用的不能删**（课程库里的 `forms` 存的就是特色课程的二级课程名 ——
  删了它，那些课的班型会变成一个查不到的名字，而网站与排课都不会报错）；
- **迁移照例从内容文件灌初值**（14 门课程、三级结构、字段与正文全在）：与 v19 同理，
  空着等于 14 个页面一起消失。

**顺带解开一处真实耦合**：后台的「可开班型」候选原先读的是**网站内容文件**
（`lib/backend/options.ts` 的 `getFormOptions()`），于是机构在后台加了班型，后台自己的下拉里看不到。
现在那五处（课程库 / 学生建档 / 报课面板 / 单节课 / 批量排课）都走
`components/admin/useFormOptions.ts` —— 先用模版垫一帧，再用**库里那棵树**替换
（模块级共享一次请求，五个组件只请求一次）。`options.ts` 里那份改名为
`getFormOptionsFromTemplate()` 并注明它只是"第一帧的种子"。

**另外**：那一块为空时页面**整块不渲染**（首页课程区、课程页的「特色课程」「课程总览」）——
空着是对的，但渲染一个空标题只会让人以为坏了。

自检新增第 27 节（22 条）：迁移（门数 / 名字 / slug / 字段 / 正文 / id）、三态、
四类校验（名字空 / 同级重名 / 路径重复 / 超过三级）、两条删除护栏 + 不误伤、
班型候选取自库里那棵树、五处表单都换了 hook、「网站内容」页挂了编辑器。
反向验证：把迁移改成"不灌初值" → 6 条断言报红。逐页验收新增 5 条（76/76）。

实测（真实库 v19 → v20）：14 门课程、一级「课内辅导」、二级的字段与正文都在；
生产构建两种态各一次：后端态 `/courses/featured/in-class/one-on-one/` 正常渲染（含「适合对象」），
空白态 `/courses` 与 `/courses/featured` **都没有特色课程**（首页那一整块也不渲染）。

### 口径定稿：那五块「连上后端用库、连不上就空白」

机构的要求分两步：先说「不连后端时只显示模版」（于是删掉了"三层来源 + 逐块回落"那套混乱），
再说「**需要用到后端数据的部分应该是空白的**」—— 这一句把口径定死了，最终形态是：

| 类别 | 内容 | 来源 |
| --- | --- | --- |
| **库里才有的"那五块"** | 教师页 · 课程卡片 · 课程正文 · 报价 · 学生案例 | 连上后端 → 用库；**没连上 → 空白** |
| **只在模版里的** | 首页文案 · 关于 · 联系 · FAQ · 时间安排 · 特色课程 · 品牌与联系方式 | 永远来自 `data/site/*.md` |

取值三种、判据一处（`siteContentSource()`）：`backend` / `blank`（默认，连不上）/ `template`
（显式 `SITE_CONTENT_SOURCE=template`）。生成文件里多写了一个 `backendSiteSource`，
这样"模版那一态"仍然是**能显式要到**的（本地对照、或确实要发一份模版站），
而"悄悄用模版"这条路径不存在。

**为什么默认是空白而不是模版**：机构要的是"没连上就别拿别的东西顶上" —— 否则页面上看到的
到底是库里的还是文件里的分不清，而那正是这一版要消灭的。代价写在文档里：
**没连后端的那些构建里，教师页 / 课程页 / 报价页 / 案例页会是空的**；
GitHub Pages 的工作流因此**显式**设成 `SITE_CONTENT_SOURCE=template`（Pages 是公开门面，
空教师页 / 空课程页比"模版旧内容"更糟），注释里写明"删掉那行就与默认口径完全一致"。

**自检被这件事推动着做了一处结构性的修改**：`check.mts` 里**一大批**断言校验的是
`data/site/*.md` 的内容与结构（栏目、卡片、课程页、教师页、报价、案例），
它们必须与"上次构站连没连后端"无关，否则本机开着后端跑会红、CI 上跑会绿。
现在自检开头把来源**显式注入成 `template`**（`__useSiteContentSourceForTesting`），
拿那五块真实分支（`blank` / `backend`）的断言集中在 §25 里单独验。
顺带修掉一个由此暴露出的真 bug：`getCoursesPageFromTemplate()` 内部有一处
`columns: getCourseColumns()` —— 那是"按当前来源取数"的入口，空白时会返回空数组，
于是**「只读模版」这个出口自己会变空**，`lib/backend/site-content.ts` 那批
「内容文件 → 库」的路径（初始化 / 迁移 / 从网站导入）就导不到任何卡片了。

反向验证：把默认值改成 `template`（即"连不上时偷偷用模版"）→ 断言当场报红
（第一版几条断言全是**显式注入**来源的，抓不到这个变异，因此补了一条
「没注入时来源就是生成文件里那一份」的断言）。

### 计划：把**剩下的网站文案也搬进库**（做到"能连后端就没有一处来自模版"）

两态口径（上一节）解决的是"混着用"，但后端的公开数据目前只有**四块**：
教师页 / 课程栏目卡片 / 课程正文 / 报价。前台还有一批页面**只有模版**：

| 块 | 现在的来源 | 用在哪 |
| --- | --- | --- |
| 品牌与联系方式（站名 / 电话 / 地址 / 营业时间 / 备案） | `content.md` | 全站 header / footer、关于、联系、404 |
| 首页各区块（hero / 优势 / 统计 / 课程区 / 案例区 / 教师区 / FAQ 区 / CTA） | `content.md` | 首页 |
| 关于、联系 | `content.md` | `/about`、`/contact` |
| 案例 / 常见问题 / 时间安排 | `cases.md` / `faq.md` / `schedule.md` | `/cases`、`/faq`、`/schedule` |
| 特色课程（一级 → 二级 → 三级课程树） | `featured.md` | `/courses/featured/**`，**后台「班型」候选也读它** |

分批搬（每批独立提交、独立验收，都走全部门禁）：

- **B0（打样）✅ 已完成**：**学生案例**（机构明确要求"学生案例以后端为主"，因此它排在第一个）
  —— `SiteContent.casesPage` + v19 迁移（从内容文件灌初值，理由见下）+ 后台新页
  「网站内容」（技术管理员 + 招生老师）+ `site.saveBlocks` 写入口 + 契约/权限 + 自检 + 文档；
- **B1**：首页各区块；**B2**：关于 + 联系；**B3**：案例 + 常见问题 + 时间安排
  （三个都是"列表型"，共用一套列表编辑器）；**B4**：特色课程（树形，且要解开它与后台
  「班型」候选的耦合：`getFormOptions()` 现在直接读内容文件，搬完之后要从库里取）；
- **B5 收尾**：`data/site/*.md` 的角色在文档里定死为"**没连后端时的整站模版**"，
  并加一条自检断言：网站侧**所有**取数函数都是两态 `if`，不存在"只读模版"的漏网之鱼。

迁移为什么要**从内容文件灌初值**（与 v15 那次"只补结构、不读外部文件"相反）：
v15 那次空着是安全的（课程正文可以点一次「从网站导入内容」补，页面那侧的判定也还在）；
这一次是**八块已经发布出去的文案**，空着等于升级完网站首页、关于、联系、FAQ、案例、
时间安排、特色课程全部变空 —— 那是事故，不是"诚实的空"。做法与 `createEmptyDatabase()`
里"课程库与报价配置来自网站内容"是同一条既有纪律：**内容文件是初值的来源**。

### B0：学生案例进库（v19，机构要求「学生案例以后端为主」）

案例原先只在 `data/site/cases.md`：改一条分数要打开文件、改完还要重新构站，招生老师在后台
根本看不到 —— 而案例是**最常更新**的内容（新学生进来、老案例过时）。这一版把它搬进
`siteContent.casesPage`，后台新增「网站内容」页（`/admin/content`）可增删改。

**几个刻意的决定**：

- **案例是一条记录，不是一段 Markdown**：`SiteCase` = 标题 + 一组「字段名 / 值」+ 过程描述。
  整段塞进一个正文框的话，"改一下分数"就要在长文里找数字；而字段做成**可增删的键值对**
  而不是固定六项，是因为内容文件里本来就是可增删的（艺术类案例会写「考级等级」），
  固定成六项就得为每个新字段加一次迁移；
- **迁移这一次要读内容文件灌初值**（与 v15 那次"只补结构、不读外部 Markdown"相反）：
  v15 空着是安全的（课程正文可以点一次「从网站导入内容」补），而这一次是**已经发布出去的案例**
  —— 空着等于升级完 `/cases` 与首页那块全部变空，那是事故不是"诚实的空"。
  待搬的首页 / 关于 / 联系 / FAQ / 课表 / 特色课程同理，都按这个口径办；
- **写入口与课程正文分开**（`site.saveBlocks` vs `site.saveContent`）：两块内容由两个页面维护、
  责任与权限都不同（案例给**招生老师**，课程正文仍是技术管理员），合成一个方法只能整份放权；
  而且两个页面的草稿都带着整份 `siteContent`，谁先保存谁就会把对方读到那一刻的旧值写回去
  —— 现在各写各的块，`saveContent` 原样保留案例、`saveBlocks` 原样保留其余三块；
- **允许一条案例都没有**（内容文件里就写着"不希望公开就把分组整段删掉，页面会自动适配"），
  页面显示空状态而不是报错。

自检新增第 26 节（23 条）：迁移真的灌了初值（条数 / 标题顺序 / 字段与过程描述逐项比对）、
两态（无快照用模版、有快照但案例为空就显示空）、`saveBlocks` 只动案例这一块（课程正文 /
教师页 / 报价文案一字未改）、四类校验、`saveContent` 不会冲掉案例、页面与权限（招生老师能改、
教师不能）、以及一条**踩过的坑**的护栏（见下）。逐页验收新增 6 条（71/71）。

**反向验证抓到的两个真问题**：

1. 把迁移改成"不灌初值" → 4 条断言报红；
2. 把 `saveBlocks` 改成整份覆盖 `siteContent` → 2 条报红；
3. **一条断言本身是假的**：我最初写的"api.ts 导出的每个类型都 import 过"用非贪婪正则找
   `export type {`，结果它从文件里那个单行块（`export type { X } from "./export";`）开始
   一路吃到大半个文件 —— 于是"删掉 import"这个变异**照样全绿**。改成"从后往前定位紧随
   `export {` 的那个 type 块"之后才真正生效。
   这条断言守的是一个很阴的坑：往 `export type { … }` 里加名字**忘了 import** 时，
   从 `@/lib/backend/api` 导入它会**静默变成 `any`**（不报错），页面里
   `cases.map((item) => …)` 的 `item` 变成隐式 any 才暴露出来 —— 排查方向会被带偏。

### 网站内容来源收紧为**两态**（连上全用库 / 连不上全用模版，删掉"仓库快照"）

机构的要求：前台网站在**不连后端**时（没起后端、部署在 GitHub Pages）只显示模版数据；
**连上后端**就全部按后台/库里的教师、课程等数据。也就是**两个状态，没有中间态**。

改之前是三层来源（后端 → 仓库快照 → 模版）并且**逐块回落**，于是有两个说不清的现象：

1. **同一页一半来自库、一半来自文件**：教师页是库里的、课程卡片却是文件里的，
   而页面上完全看不出来（`backendX() ?? getXFromTemplate()` 让每一块各自决定）；
2. **「数据不全」伪装成「没连上」**：后端好好的，只因为课程正文还没导入，
   整站就悄悄换成了模版 —— 机构以为在用库里的数据。

改法（三件事一起）：

- **删掉中间层**：`scripts/site-snapshot.mjs`、`data/site/site-snapshot.json`、
  `npm run site:snapshot`、`SITE_CONTENT_SOURCE=snapshot` 全部删掉；
  照旧敲 `=snapshot` 会**明确报错**（"这个模式已经删掉"），不会悄悄走模版；
- **判据只有一条**：`backendSnapshot()` 是不是 `null`。取数层的每个函数写成
  `if (snapshot !== null) return backendX(snapshot); return getXFromTemplate();`
  —— 源码级断言盯着"不许再出现 `backendX() ?? 模版`"；
- **空就空着**：块内为空（教师一位都没勾展示、课程库没卡片、正文没导入、报价没配）
  **不再回落模版**，页面如实显示为空（连标题也来自库，不从文件抄一份）。
  构站日志逐块报数，空块单独 ⚠ 列出来并说清去哪补："这一块为什么是空的"从此只能从日志得到答案。

构站脚本的体检也收了口径：**只看结构**（缺 `teachers` / `courses` / `partitions` /
`siteContent.coursePage` / `pricing.stages` 才算"连不上"），内容多少不参与判定。

实测（三种情形都真跑过）：

| 情形 | 结果 |
| --- | --- |
| 连上后端（一次性空库） | `本次网站数据来源：**后端** … 0 位网站教师 / 32 张卡片 / 9 个分区 / 21 个学科 / 61 个小节 / 6 个报价阶段 / 4 门选修课` + ⚠ 空块点名（教师页） |
| 连不上后端（空端口） | `本次网站数据来源：**模版** —— 连不上 …：fetch failed` |
| 旧的 `SITE_CONTENT_SOURCE=snapshot` | 退出码 1，并说明"`snapshot` 模式已经删掉：网站内容只有两态" |

自检新增第 25 节（13 条）：无快照时四块与"只读模版"出口逐项一致；注入一份"各块全空"的
快照时四块**一律按后端（空）**、连标题都不回模版；取数层不许出现 `?? 模版`；构站脚本只剩
两态、旧模式报错、空块在日志里点名；`site-snapshot.json` 与 `site:snapshot` 都不在了。
反向验证：把取数层改回 `?? 模版` → 三条断言当场报红。

### 后台外壳：滚动时顶栏与侧栏不动（sticky，不是 fixed）

机构反馈「后台页面上下滑动的时候左边的导航不应该随着页面移动」。改法只有几行类名，
但**做法选错会连带弄坏一整条已经修好的东西**，因此把取舍记在这里：

- **为什么是 `sticky` 而不是 `fixed`**：fixed 把元素移出文档流 → 文档高度变矮、内容要另加左边距，
  而 `useScrollGuard`（就地动作的滚动守护）、`ScrollMemory`（整页重载后还原位置）、
  「点编辑不跳顶部」整条链路都依赖"文档高度 / 窗口滚动位置"（§15.3）。sticky 元素仍在文档流里，
  对这些机制**完全透明** —— 这一点是这次选型的全部理由；
- **顶栏必须一起钉**：侧栏的偏移量就是顶栏高度，顶栏滚走会在侧栏上方留一条缝；
- **`lg:self-start` 是命门**：flex 行默认把子项拉伸到整列高，而整列高的元素**钉不住**
  （它跟着页面一起滚，看起来像"样式没生效"）。这一页原先的注释写着"桌面端为固定侧栏"，
  而代码里既没有 sticky 也没有 self-start —— 注释描述的是一个从未实现的状态；
- **钉住的另一半是自己能滚**：侧栏加了 `max-h-[calc(100dvh - 顶栏高)]` + `overflow-y-auto`，
  否则被钉住之后导航最后几项落在屏幕外、怎么滚页面都够不到；
- **顶栏高度只有一处定义**：外壳上的 CSS 变量，顶栏用它定高、侧栏用它算偏移与最大高度；
- **分隔线移到内容区那一侧**：侧栏不再被拉伸，画在它身上的右边框会只画到导航最后一项就断掉。

自检第 24 节（10 条）钉住这四件事 + 两条"别用错做法"（不许 fixed、不许写死 `h-14`）。
**反向验证抓到一个真问题**：第一版断言直接搜字面量，而注释里正写着这些类名 ——
删掉 `lg:self-start` 后照样全绿；改成"先去掉注释再查"之后，删 self-start 与 sticky→fixed
两种变异都会当场报红（这正是 §19/§23 同一个坑的第三次出现，因此这次把它写进注释里）。

### 课程分区成为数据（v18：栏目 → 子栏目可增删改名排序）

机构问的是「课程库清单里『小学课内』『初中课内』这些小标题能不能加、能不能减」。
答案是**以前不能**——分区那时候不是数据，而是从每门课的 `category` / `subgroup`
两个字符串**聚合**出来的，于是机构撞上三个治不好的毛病：

1. **改名要逐门课改**：把「小学课内」改成「小学学科」得把那一区的课一门门打开改，漏一门就裂成两个分区；
2. **分区不能空着、不能排序**：一个名字只有"恰好有课"时才存在，"先建好栏目、再往里放课"做不到；
3. **同一件事有两个写法**：`category` + `subgroup` 拼出层级，网站那边又是另一套（栏目 → 子标题），
   对不上时只能靠人眼比对。

这一版把分区做成**一行记录**（`coursePartitions`：名字 / 上级 / 同级顺序），课程用 `partitionId`
引用它。后台课程库新增「课程分区」面板与两级清单（分区标题旁：改名 / ↑↓ / 删除 / 把本区课程移到…），
新建课程时选分区；网站课程页的栏目与子栏目顺序由分区自己的 `order` 决定。

**几个刻意的决定与其理由**：

- **两级是硬限制**：网站课程页渲染的就是两级，第三级"存得下但看不到"，
  因此 `validatePartition()` 直接拒绝并说明原因 —— 允许第三级只会让人建出一批看不见的分区；
- **删除有护栏**（有课 / 有子栏目就拒绝）：删掉一个有课的分区不会报错，只会让那些课静默变成
  「分区已失效」、网站上一整块消失，而没有任何提示 —— 比"删不掉"糟糕得多；
- **迁移照抄升级前的显示顺序**：升级前网站的栏目顺序 = "该栏目下最小的卡片 order"，
  因此 v17 → v18 的分支先按 `(order, 原位置)` 排一遍再按出现顺序编号，而不是按名字排序。
  这条有反向验证：把迁移改成按名字排序 → 自检两条断言当场报红（栏目顺序、子栏目顺序）。
  真实库上验过：迁移前后网站的栏目顺序与子栏目**逐项一致**（小学课内 → 初中课内 → 高中课内 →
  外语 → 课外兴趣 → 成人课程；高中课内下 必考科目 → 外语 → 七选三），32 门课全部落到正确的分区；
- **搬完就拆桥**：迁移把 `category` / `subgroup` 两个字段从课程行上**删掉**，
  自检断言它们不许回流（"同一件事写两处"正是这一版要修的东西）；
- **一处写法**：分组是 `groupByPartition()`（后台清单与网站共用），显示名是 `partitionPathLabel()`
  （导出 / 下拉 / 清单 / 数据页共用）；两条都各有断言盯着"不许各写一份"；
- **顺带清掉一处多余**：`import.ts` 里那份自己实现的 id 生成（`makeId`）删了，
  改用 `lib/backend/ids.ts` 的 `nextId`（id 只有一处实现）；
- **顺带补上一处漏掉的权限门控**：课程库页原先对普通教师也渲染「新增 / 编辑 / 删除 / 切换状态」
  按钮（点了才 403）。现在按 `canCallMethod` 逐个方法判断，没权限就不渲染并说明归谁。

自检（`scripts/check.mts` 新增第 23 节，26 条）覆盖：v17 老库迁移（分区、层级、顺序、字段删除）、
改名一处改处处变（含科目下拉）、同级重名 / 第三级 / 环 / 上级不存在四类拒绝、排序生效、
删除护栏三条、批量移课（含"再移一次不算数"）、未归类、引用不存在的分区被拒、
以及"分组与写法各只有一处实现"。逐页验收（`npm run accept`）加了 8 条按机构真实顺序走的分区流程。

### 节假日与调休（`/admin/holidays` + `data/holidays/<年>.json`，不占数据库版本）

机构要"知道哪天是法定假日、哪天是调休上班日"（家长会问；假期是旺季、白天能排课，
而调休上班的那几个周末学生要上学、白天的课得挪到晚上）。这一版的取舍是
**只做一张可查的表，不动排课**：

- **为什么不做成"停课日历"**：把假日接进排课要先把"假期到底是上课还是停课"定下来。
  机构给的方向是**放假日按假期作息**（假期白天能排课）、**调休上班日按工作日作息**
  （教室配的"周一–周五"时段那天生效）—— 这需要先让教室的作息能按日期覆盖，是另一件事。
  半截做了更危险：排课会**静默地**少排或排错，而"少排了一节课"没有任何提示。
  因此 `lib/backend/recurrence.ts` 的"按星期几往后数"与 `isWithinAvailability` 这一版**一行没改**，
  四处"调休与节假日请手动处理"的口径（`recurrence.ts`、`LessonSeriesForm.tsx`、使用手册、后台API约定）
  **依然成立**，页面上也显眼地写着"这张表只供查看，不会自动改排课"。
- **为什么数据不进数据库**：这是**参考资料**（一年更新一两次、与业务数据没有外键关系），
  放进数据库要跟着走迁移、契约、权限表、导出导入、备份 —— 都是为一张"查得着的表"付的代价。
  放在 `data/holidays/<年>.json`：进 git（哪一年哪天改过一眼看见）、丢了重新抓一次即可、
  手改也方便（读盘时会校验，坏数据明确报错而不是静默当成"这天没假期"）。
  **方法数因此没有变化**（仍是 115 个）：抓取走服务端自己的两条路由，与账号管理同一个理由。
- **为什么坚持两个来源交叉校验**：这种数据错了会直接影响排课与家长沟通，而它一年只看一两次、
  没人会逐日核对。所以 Apple 的「中国大陆节假日」日历（主源，带机器可读标记）与国务院公告口径的 JSON
  必须**逐日比对**：调休上班日完全一致、国务院列的放假日 Apple 必须有、Apple 多出来的**必须都是周六周日**
  （实测 2024 年多出 6/08、6/09 就是"Apple 把假期块里的周末一起标了"，属口径差异）。
  对不上就**拒绝写入**并列差异 —— 宁可这次没导进来（界面写着缺哪一年），也不要静默用错数据。
- **实测留下的两条工程事实**：① `raw.githubusercontent.com` 在同一台机器上 6 次里 4 次超时，
  因此国务院那个源配了三个镜像按顺序试、并把失败原因逐一说出来；② 一天可能同时属于两个节日
  （2025-10-06 是国庆+中秋），逐日表必须**按（日期，类型）去重**、名字用「、」连起来，
  否则"放假多少天"会多数一天、连休块还会互相重叠。

### 课表与占用（`/admin/timetable`）

排课之后最常被问的两件事，共用一个周视图、按页签切换：

- **教师课表**：某位老师这一周什么时候有课（哪天满、哪天空）；
- **教室占用**：某间教室这一周的占用与空档（接新学生时先找空档）。

底部还有一行「本周总览」：按教师或按教室列出本周课次与小时数，
用来回答「谁最忙」「哪间教室最紧」。

**空档卡片**（`lib/backend/timetable.ts`）：没排课、但还能排一节课的时间会显示成
虚线卡片，共三段 —— **课前**（上课时间开始 → 第一节课）、**课间**（两节课之间）、
**课后**（最后一节课 → 上课时间结束），每天栏头给出当天空档合计。
课表上写出来的是「谁什么时候上课」，而接新学生、安排补课、答应家长临时加课靠的
恰恰是**没写出来的那段空隙**，所以把它算出来显示。课表页与今日概览共用同一份计算。

口径都是刻意定的：

- **头尾按「上课时间」算**：窗口由 `options.ts` 的 `getClassHoursWindow()` 从内容的
  「上课时间」字段解析（默认 8:00–22:00），**不写死在代码里**；解析不出来就退回
  「只算课间空档」，宁可少显示也不猜错；课排在窗口之外时不产生空档；
- **已取消的课不算占用**（与统计口径一致）：取消后左右两段连成一整段；
- **用「到目前为止最晚的结束时间」往后比**：时间重叠时不会算出负数或假空档；
- **一节课都没有的那天不产生空档**：整天空着由页面空状态表达，否则会画出一张
  14 小时的卡片，与空状态重复。

纯函数，页面只负责按时间线渲染（`buildDayTimeline`），文案（`gapText`）也在模块里，
避免两个页面各写一套说法。

场地的三层信息：**用途**（上课用教室 / 自习室）、**容量**（自习室即座位数）、
**可用时段**（一周中哪几天 + 哪个时间段；**留空表示不限**）。

可用时段是一条可增删的「几天 + 一段」，覆盖「周一至周五 17:00–21:00」这种常见写法；
它不只是展示：排课时会检查这节课是否**完整落在**某行时段内，落在外面会提示
「所选教室在该时段不开放」并禁止保存。判定规则集中在 `lib/backend/availability.ts`：
没设时段＝不限、跨天行（结束早于开始）视为无效、星期必须匹配。

数据结构改动必须配迁移：`Classroom` 从 v1（只有名称/容量/备注）升到 v2 时，
`migrate()` 会给老数据补上 `kind` 与 `availability` 并落盘 —— 否则那份旧数据会缺字段，
界面上直接出现 undefined。自检里有一条用例专门灌入 v1 数据再读回来验证迁移。
（**历史**：这条要求的原话针对的是"**别人那份**旧数据会不会缺字段" ——
那时数据分散在每个使用者的本地存储里，"别人的那份"就是另一份库。
接后端之后库只有一份、位置也变了，但要求一点没变：要迁移的是**库里那份老数据**，
缺字段的下场同样是 undefined。）

排课的两个关键动作都放在服务层里，页面不自己算：

- **排课校验**（`lessons.findConflicts`）一次给出五类问题，页面逐条说明并禁止保存：

  | 校验 | 含义 |
  | --- | --- |
  | 教师 / 教室 / 学生冲突 | 时间重叠；**相邻时段不算冲突**、已取消的课不占时间、编辑自己不算冲突 |
  | `classroomClosed` | 落在教室可用时段之外 |
  | `overCapacity` | 学生数超过场地容量（8 人的教室不能排 10 个学生） |
  | `teacherSubjectMismatch` | 科目不在该教师的「可带科目」里（未登记科目时不报） |

  这里原先写的是"注意真实服务端**必须再校验一次** —— 客户端检查只是体验，不是保证"。
  **那条要求到现在仍然没有兑现，别以为接了后端就自动有了**：服务端跑的是同一份
  `api.ts`，所以校验逻辑确实在服务端手里，但**落库路径（`lessons.create` / `update`、
  安排补课）自己不复核**，仍然依赖表单在保存前先调一次 `findConflicts`。
  因此"检查只是体验"这句话今天依然成立 —— 它是接后端之后仍挂着的一条待办。
- **标记已上**（`lessons.markCompleted`）：状态改为「已上」并按课时扣减，
  **幂等** —— 重复点不会重复扣课时（这是同类系统最常见的数据事故）。

### 假登录：只是门，不是锁（**已迁移到服务端会话**）

> 这一节记录的是**当时**的形态与理由，保留下来是因为它解释了一个决策：
> 为什么那个阶段可以接受"口令写在前端"。**现在不要按它做。**
> 现状见下一节。历史内容：`admin` / 一个固定口令写在 `lib/auth/session.ts` 里，
> 静态站点没有服务端、仓库又是公开的，因此它只能挡住误点进来的访客；
> 当时数据本来也在访问者自己的浏览器里，所以风险有限。

### 登录：从假登录到服务端会话（现状）

数据搬进**服务端数据库**（钱、课时、家长联系方式）之后，"口令写在前端"就不再可接受了：
公开仓库里的口令等于没有口令。于是：

- 口令与校验都在服务端（`server/auth.mts`）：口令取自 `NEXGENEDU_ADMIN_PASSWORD`，
  否则**首次启动随机生成**并写入 `server/data/admin-credential.json`（0600），
  启动日志打印一次；**数据库里不存凭证** —— 否则"恢复三个月前的备份"会把口令一起回退；
- 前端只拿**令牌**（`lib/auth/token.ts`），`/api/call` 与老 REST 接口未登录一律 401；
- 鉴权是**结构性**的：`/api/` 下除 `/api/login`、`/api/logout`、`/api/session` 外
  一律要登录（一处闸门）。逐个接口加鉴权一定会漏 —— 第一版就漏了读接口；
- 操作日志的**操作人由会话决定**，前端说了不算（原先前端调 `setOperator`，
  而它经远端代理会静默变成 Promise，操作人一直是默认值）；
- 服务端默认只绑 `127.0.0.1`（`NEXGENEDU_HOST` 可改）：本机使用的系统没必要监听所有网卡。
  **历史**：早先是 `server.listen(PORT)`，那会监听所有网卡 —— 同一个 WiFi 下的任何设备
  都能连上；真要给别的设备用，得显式设 `NEXGENEDU_HOST=0.0.0.0`，那是一个有意识的决定，
  而不是默认行为；
- 令牌只放在**服务端内存**里（不落盘）：闲置有效期默认 12 小时
  （`NEXGENEDU_SESSION_MINUTES` 可配）、**每次使用都会续期**，
  并且**后端一重启就全部失效，需要重新登录**。这是刻意的取舍：
  把令牌持久化会立刻引入"过期与回收"那一整套逻辑，而本机使用下重新登录的成本很低；
- 由此 `lib/auth/session.ts` 的 `getSession` / `isLoggedIn` / `login` / `logout`
  **全部是异步的**：「已登录」现在只有服务端说了算（本地存着令牌不等于令牌还有效），
  同步返回一个本地状态会让人"看着进去了、点什么都失败"，比多等一次请求糟糕得多；
- **没有后端时不假装能登录**：线上静态站没有可连的服务端，登录直接不可用，
  界面明说原因与下一步（`loginUnavailableReason()` / `RequireAuth` 的「后台需要本机后端」），
  而不是退回"口令写在前端的假登录"或让人反复试口令 —— 那样只会制造
  "我在这儿录的数据去哪了"的困惑；
- 后台整棵子树仍包在 `components/admin/RequireAuth.tsx` 里、未登录不渲染界面，
  但那只是**体验层**的门；真正的边界在服务端。

### 管理后台数据变更方案（Phase 5 决策）

> 这是 Phase 5 时的决策记录，**已经不能用来理解今天的存储**：下面那张表是当时的形态。
> 保留它是因为"为什么当初要把数据分成两类"这个判断值得留下，但按它去理解现状会走错方向。

后台的排课、课程完成、课时扣减等「写操作」需要区分两类数据：

| 类别 | 来源 | 存储位置（**当时**） |
| --- | --- | --- |
| 基础档案：学生 / 教师 / 教室 | `data/*.md` | 只读，不修改 |
| 运行时数据：课程安排、课时流水 | 由数据层初始化 | 用户修改部分写入 `localStorage` |

- 原因：Markdown 在前端运行时不可写，后台若只读就无法演示排课；而基础档案无需在 UI 中编辑。
  （**这条已经过时**：现在学生、教师、教室都在后台里建档 / 编辑 / 删除，
  它们和排课、课时流水一样都是服务端库里的业务数据；`data/site/*.md` 只管**宣传网站**的内容。）
- 数据层对外暴露统一的 Provider 接口，`MarkdownDataProvider` 与（未来的）`PostgresDataProvider`
  实现同一套函数签名。（**设想没有这么落地**：接后端时没有 Provider、也没有换 PostgreSQL，
  而是给 `KeyValueStore` 加了一个 SQLite 实现 —— 见「教务后台的后端架构」。）
- 运行时数据通过 `createRuntimeStore(storageKey)` 工厂封装，为未来替换为 API 调用预留唯一改动点。
  （**同样没有按这个设想做**：这段代码里没有 `createRuntimeStore` 这个工厂。
  实际改动只有两处 —— 加一层代理（`lib/backend/remote.ts`）与加一个存储实现
  （`server/kv-store.mts`），业务代码与页面代码都没动。）
- **`localStorage` 仅为开发阶段的临时方案，不是数据库**：不承担并发、事务、多端一致性职责，
  校验逻辑（如排课冲突）必须在数据层重新执行一次，不能只依赖 UI。
  （**这条判断是对的，也已经兑现**：临时方案退役了，现在是服务端 SQLite。
  "校验不能只靠 UI"照旧成立 —— 而且比当时更值得警惕：服务端跑的是同一份实现、
  校验函数确实在服务端手里，但**落库路径自己不复核**，所以今天仍然只有页面那一次检查，
  详见后面「课表与占用」里排课校验那一条。）

### 后端开发方案（`docs/后端开发方案.md`）

接真实服务端的路线写在契约里（`contract.ts` + `docs/后台API约定.md`：方法清单、
服务端必须复核的那些校验、迁移六步），**落地方式**另有一份。这份方案的要点：

开发阶段用「本机 Node + SQLite 单文件」，不上 Docker —— 这台开发机的环境约束
（`~/.npm` 只读）让容器化收益为负，而 SQLite 单文件让备份=复制文件、迁移=搬文件。
业务规则是纯函数，服务端可以原样搬，不用重写。触发条件满足（多人并发 / 要常驻线上 /
要对外提供数据）时再换 Postgres 与容器，那一步**不需要改接口**。
数据库就是 **SQLite 单文件 + `better-sqlite3` 驱动**（WAL、一个请求一个事务、
`VACUUM INTO` 在线备份，即不停服务也能备份）；
服务端采用**路线 B**：给 `api.ts` 换一个 SQLite 支持的 `KeyValueStore`，再用
`POST /api/call { method, args }` 把方法暴露出去。

**这份方案已经落地**（当时的写法是"计划这么做"，现在是"就是这么做的"）：

- 服务端跑的就是 `lib/backend/api.ts` 这**同一份源码**，不是在服务端重写一遍 ——
  业务口径只有一份（代价与后续迁移见该文档 §5.3）；**历史**：这里原先说的是
  "服务端跑的就是**前端那一份** `api.ts`"，本意是同一份源码，但接后端之后前台走的是代理、
  本地实现只在没配后端时兜底，那句按字面读已经不准确，所以改成"同一份源码"；
- SQLite 存的是**整库一份 JSON 快照**（`kv` 表），不是按行存表：因此没有按行查询，
  并发写入以"整库替换"为单位；
- 备份与恢复也按方案落了地：每天自动一份、保留份数可配、恢复演练可重复执行
  （见「服务端备份」一节）。

**还没落地的一件事**（重要，别按已完成理解）：网站内容进库做**只读镜像**。
表已经建好了（`site_content`），但**同步脚本还没有写、表里是 0 行**；
网站内容的真源目前仍然只有 `data/site/*.md` 这一个。当初定的方向不变：
真源仍是内容文件，库里将来存的是只读镜像（单向、幂等、能看出过期），
以此保住免费静态托管与现有发布流程；将来若要改成"数据库做真源"，
只需把同步方向反过来，业务表与接口都不动。

## 9. 本机环境约束（重要）

开发机存在以下网络 / 权限限制，影响构建方式，**不是代码问题**：

1. **`~/.npm` 不可写**（沙箱限制在项目目录内）。
   → 所有 npm 命令使用项目本地 cache：`npm install --cache ./.npm-cache`。
   该目录已加入 `.gitignore`。
2. **Google Fonts 不可达**。
   → 不使用 `next/font/google`，改用系统字体栈，构建可完全离线完成。
3. **`github.com` 连接不稳定**（首次探测超时，推送时偶发 `Connection reset by peer`）。
   → 若 `git push` 失败，改用 HTTP/1.1 重试（已实测有效）：

   ```bash
   git -c http.version=HTTP/1.1 push origin main
   ```

## 10. Git

- 默认分支：`main`
- 远端：`origin` → `https://github.com/JeremyThierryChan/NexGenEdu.git`
- 首次提交已推送（`f7ed6fd`），本地 `main` 与 `origin/main` 一致。
- 后续每个 Phase 完成后 commit 并推送：

```bash
git push
```
