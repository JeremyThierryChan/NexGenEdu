# NexGenEdu · 辅导机构轻量化管理系统 V1

## 1. 项目目标

为辅导机构提供一个「先把每天真正会用到的功能做好」的轻量化系统。V1 聚焦五件事：

**学生 · 教师 · 教室 · 课程 · 课时**

终极验收目标：**管理员打开系统，10 秒内知道今天谁上课、在哪里上课、老师是谁，以及每个学生还剩多少课时。**

本项目不是 ERP，当前阶段不是 SaaS。

## 2. 当前范围

### 本阶段做（立项时写的是「纯前端」，现在要分两半读）

- 对外宣传网站：首页 / 课程 / 教师 / 关于我们 / 联系我们 —— **仍然是文件驱动**，
  Markdown 作为内容源经数据访问层读取；
- 教务后台 UI 框架：Dashboard / 学生 / 教师 / 教室 / 课程 / 日历 —— 界面还是这一套，
  但数据已经不在页面这一侧；
- 排课、冲突检测、课时扣减、快捷搜索 —— 算法没变，**跑的地方变了**。

> 这一节写于立项时，当时"前端"就是全部。接上真实后端那一步之后，后台的业务数据
> 搬到了**本机 Node + SQLite**（见第 8 节「教务后台的后端架构」与「后端开发方案」），
> 因此这段补一句说明，免得读者仍按"纯前端"去理解现在的系统 —— 那会得出
> "换台电脑就看不到数据""清缓存会丢"这类**已经不成立**的结论。

### 本阶段明确不做

数据库（PostgreSQL / MySQL / MongoDB / Redis）、API Server、ORM、真实登录与 RBAC、支付、
微信登录、短信、AI 功能、微服务、多校区 / 多机构 / SaaS、财务与工资、CRM、题库与组卷、
复杂报表、家长 / 学生 / 教师账号。

这份清单里有**两项已经做了**，其余仍然不做：

- **API Server 已经做了**：本机 Node 服务端提供 `/api/call` 等接口（不是上面列的
  PostgreSQL / MySQL / MongoDB / Redis，而是 SQLite 单文件）；**ORM 仍然不做** ——
  服务端只用了一个 KeyValueStore，没有引入 ORM 这一层；
- **真实登录已经做了**：后台改成服务端会话（见「登录：从假登录到服务端会话」），
  但 **RBAC 仍然不做** —— 现在只有一个账号、没有权限区分，这是刻意的范围判断；
- 支付 / 微信登录 / 短信 / AI / 微服务 / 多校区 / 家长与学生教师账号：仍然不做，
  V1 要的是"每天真正会用到的功能"，不是功能齐全。

## 3. 技术栈

| 项目 | 选择 | 说明 |
| --- | --- | --- |
| 框架 | Next.js 15.5.25（App Router） | 界面与静态导出；**本机使用时另有一个 Node 后端**（`server/`），线上静态站不带它 |
| 语言 | TypeScript 5.9（`strict` + `noUncheckedIndexedAccess`） | 不使用 `any`；服务端直接跑 `.mts`（`--experimental-strip-types`），不另建一套编译产物 |
| 样式 | Tailwind CSS 4.3（CSS-first `@theme`） | 设计令牌集中在 `app/globals.css` |
| UI | 自建小组件库（`components/ui`） | 不引入重型组件库 |
| 数据 | 网站内容：Markdown + 数据访问层；后台数据：服务端 SQLite | 前者是构建期内容源，后者才是业务数据（钱、课时、家长联系方式） |
| 后端 | 本机 Node（只用标准库 http）+ `better-sqlite3` | 不引框架、不上 Docker，理由见「后端开发方案」一节 |
| 包管理 | npm（项目本地 cache：`.npm-cache/`） | 受本机权限限制，见第 9 节 |
| 字体 | 系统字体栈 | 不依赖外部字体 CDN，构建可离线完成 |

**架构主线（保证未来迁移成本低）**

现在有两条数据通路，界面都不感知数据存在哪里：

```
① 宣传网站（构建期读取，线上是静态文件）
   data/site/*.md  →  lib/data（数据访问层）  →  React / Next.js UI

② 教务后台（本机使用，数据在服务端）
   后台页面  →  lib/backend/remote.ts  ──POST /api/call──→  server/index.mts
                                                                   ↓ 跑的是同一份实现
                                                          lib/backend/api.ts
                                                                   ↓ KeyValueStore 接口
                                                          server/kv-store.mts → SQLite

未来（两边都不改界面 / 函数签名）：
   PostgreSQL  →  API  →  lib/backend/api.ts（同一套签名）  →  UI 不变
```

UI 不感知数据来自 Markdown 文件、服务端 SQLite 还是将来的 PostgreSQL。
（接后端之前，这句里写的选项是 localStorage —— 那条路已经走完，见第 8 节。）

## 4. 目录结构

```
app/
├── layout.tsx            # 根布局：html/body + 全局样式
├── globals.css           # 设计令牌（品牌色 / 字体 / 圆角）与基础样式
├── (site)/               # A. 对外宣传网站
│   ├── layout.tsx        #   顶部导航 + 页脚
│   ├── page.tsx          #   首页 /
│   ├── about/            #   /about
│   ├── courses/          #   /courses
│   ├── teachers/         #   /teachers
│   ├── contact/          #   /contact
│   ├── cases/            #   /cases 学生案例
│   ├── faq/              #   /faq 常见问题
│   ├── schedule/         #   /schedule 课程时间安排
│   ├── featured/         #   /featured 特色课程（含各课程独立页）
│   └── quote/            #   /quote 智能报价（下拉选择 + 报价计算）
└── admin/                # B. 教务后台（页面齐全；数据走 lib/backend/api.ts）

components/
├── ui/                   # Button / Card / Badge / Container / Section / PageHeading
├── layout/               # Logo / Header / Footer / AdminSidebar
├── site/                 # 宣传网站区块组件（PageHeader / FeatureCard / EmptyState）
├── courses/              # CourseCard
├── teachers/             # TeacherCard
├── pricing/              # EstimateForm（报价表单）
└── admin/                # 后台业务组件（学生详情 / 排课表单 / 报价面板 / 登录壳 …）

lib/
├── data/
│   ├── site.ts           # 数据访问层：页面唯一的数据入口
│   └── content.ts        # 单文件内容源解析（## 页面 / ### 分组 / #### 条目）
├── backend/              # 后台服务层：api.ts 是业务口径的唯一实现
│   ├── api.ts            #   全部后台方法；本机使用时被 remote.ts 换成对后端的代理
│   ├── remote.ts         #   POST /api/call 代理 + 令牌（仅 NEXT_PUBLIC_API_BASE 有值时启用）
│   ├── storage.ts        #   KeyValueStore 抽象（浏览器内存 / localStorage）
│   ├── contract.ts       #   接口契约（按「服务端怎么做」分组，自检双向比对）
│   ├── initial.ts        #   初始状态：业务表全空 + 网站课程 + 报价配置
│   ├── seed.ts           #   测试 / 演示夹具（只有 NEXGENEDU_ALLOW_SEED=1 才灌）
│   └── pricing / finance / attendance / stats / followup / timetable / export …
├── auth/                 # 登录：token.ts（令牌存取）+ session.ts（登录/登出/会话）
├── markdown.ts           # frontmatter / Markdown 极简解析与渲染
├── types/site.ts         # 内容类型定义
├── pricing/quote.ts      # 报价公式（改报价规则只改这个文件）
├── site/nav.ts           # 导航与页头 CTA 的结构配置
├── scheduling/           # 排课与冲突检测（Phase 5）
└── utils/cn.ts

data/site/
├── content.md            # 网站内容唯一来源（品牌 / 5 个页面文案与数据）
├── pricing.md            # 报价页的可选项与基础价格
├── faq.md                # 常见问题页 /faq
├── cases.md              # 学生案例页 /cases
├── featured.md           # 特色课程页与各课程独立页
├── schedule.md           # 课程时间安排页 /schedule
└── *.ts                  # 由 scripts/sync-content.mjs 从上面的 .md 自动生成

scripts/
├── dev.mjs               # 开发入口：监听 .md 改动自动同步 + 启动 next dev
├── sync-content.mjs      # 把 .md 编译为 .ts 内容模块
└── check*.mts / check-*.mjs
                          # 自检：业务口径、真实 HTTP、认证、链接、404 产物、逐页验收

server/                   # 本机后端：只在 npm run server 时运行，不进线上静态站
├── index.mts             # HTTP 入口：/health、登录、会话闸门、POST /api/call、自动备份
├── auth.mts              # 口令与令牌（口令来自环境变量或首次启动随机生成，不落库）
├── db.mts / kv-store.mts # SQLite 连接与 KeyValueStore（kv 表存整份 JSON 快照）
├── backup.mts / backup-cli.mts   # 每日自动备份 + 命令行（npm run server:backup）
├── migrate.mts / migrations/     # 建表与升级
├── data/                 # nexgenedu.db（不进 git）
└── backups/              # 每日备份文件（不进 git）
```

## 4.1 内容数据层（主体在 1 个文件，另有 4 个页面各自成文）

**所有面向访客的文案、数字、联系方式都在 `data/site/content.md` 一个文件里。**
改完保存，刷新页面立即生效（dev 模式下该文件是构建依赖，保存即触发重新编译）。

文件按页面分段，共 6 段：

| 段落 | 覆盖范围 | 影响页面 |
| --- | --- | --- |
| `## 页面: 全站` | 品牌名、标语、描述、关键词、联系方式、版权 | 全站标题 / 页头 / 页脚 |
| `## 页面: 首页` | 首屏、首屏数据、教学特色、课程卡片、教室格位、底部 CTA | `/` |
| `## 页面: 课程` | 课程页文案 + 每门课程介绍 | `/courses` |
| `## 页面: 教师` | 教师页文案 + 每位教师的科目/职务/教龄/简介/介绍 | `/teachers`、首页教师卡片 |
| `## 页面: 关于` | 教学理念、校区数据、校区介绍 | `/about` |
| `## 页面: 联系我们` | 联系方式清单、到校路线、预约按钮 | `/contact` |

> **补充（写这一节时"只有这一个文件"是真的，后来不再是了）**：宣传网站扩展之后，
> 常见问题（`faq.md`）、学生案例（`cases.md`）、特色课程（`featured.md`）、
> 时间安排（`schedule.md`）各自成文 —— 它们也是 `data/site/*.md`，同样由
> `sync-content.mjs` 生成同名 `.ts`，只是各自有更贴题的结构（问题 / 案例 / 课程层级 / 时段表）。
> 上面那张表只覆盖 `content.md` 的 6 段；改这几页的内容要去对应的文件，别在 `content.md` 里找。

### 解析器：标题层级即数据结构

`lib/data/content.ts` 的 `buildTree()` 按 Markdown 标题的井号数**如实建树**，
不做「哪一层是分组、哪一层是条目」的推断（早期版本靠层级加减推断，
在 `## 分组 → ### 阶段 → #### 条目` 这类多层结构下反复出错）。
数据层按名字取节点，需要更深层级就继续读 `children`。

约定：

| 写法 | 含义 |
| --- | --- |
| `## 页面: 首页` | 页面分段 |
| `## 分组` / `### 分组` | 分组（页面级） |
| `#### 名称 \| 值` | 条目（值用半角冒号时也可写作 `名称: 值`） |
| `#### 学段：一句话` | 内容小节（正文写在下方）；全角冒号是区分信号 |

### 自检脚本

`npm run check` 会校验数据层条目数量、首页标签与课程页小节的**双向**对应、
以及报价公式的关键用例（断言数量见运行输出）。

**为什么需要它**：解析器层级出错时构建依然成功、首页也正常，
只有课程正文是空的 —— 这类问题靠肉眼检查发现太晚。

### 首页 / 课程页的「栏目 → 子标题 → 卡片」结构

### 课程的三层结构

```
/courses                    课程总览（6 个栏目 → 卡片）
  /courses/primary          学段（栏目）页 —— 这一阶段各科目的简介
    /courses/primary-chinese 课程卡片页 —— 这门课的具体内容（阶段小节）
```

- **班型的反向关联**：在班型页（`/courses/featured/in-class/one-on-one` 等）列出
  **开设这个班型的科目**，按「阶段（栏目）→ 子栏目」分组，用与课程总览一致的
  卡片网格展示（32 门课竖着排成一列会很长、不好找），栏目标题指向该阶段页。关系是**反向查出来的**（`getCardsForForm()`），
  数据只在卡片行的 `· 班型:` 里写一次，改卡片两处同时变；
  自检逐项比对正反两个方向，多一个少一个都会失败。
  两个不是「按人数分」的班型，语义按业务固定（自检会拦住改动）：

  | 名称 | 语义 | 关联规则 |
  | --- | --- | --- |
  | 晚托管 | 单独的服务，**不分科目**，按学段分小学晚托 / 初中晚托 | 不挂任何学科；页面正文写明「不按科目分班」 |
  | 周中预习课 | **只有初中有** | 只挂在初中课内的 5 门科目上 |

  此外「预习课」按时间分成两种，都是**二级课程**：

  | 名称 | 含义 | 下级 |
  | --- | --- | --- |
  | 周中预习课 | 学期中提前于校内进度上课（只有初中有） | 无 |
  | 假期预习课 | 假期提前预习下一学段，衔接初一 / 高一 | 精品小升初 / 精品初升高 / 基础小升初 / 基础初升高 |

  假期预习课原先把 4 门课拆在「一对多小班课」「9 人以上大班课」下（按人数分），
  现改为按**用途**成组 —— 代价是这 4 门课的网址从
  `…/mini-class/junior-prep` 变为 `…/holiday-preview/junior-prep`。
- **栏目页**（`primary` / `junior` / `senior` / `languages` / `interests` / `adults`）
  回答「先定学段，再看科目」：列出该阶段每个科目 + 一句简介（**从现有正文派生**，
  不重复写字：学科导语 → 选修课介绍 → 第一阶段正文的第一段），点标题进科目页。
  栏目路径写死在 `COLUMN_PATHS`（栏目名是结构，自检固定了六个），
  自检还会校验栏目路径不重复、且与卡片路径不重叠 —— 两层共用 `/courses/<一段>`，
  重叠会让某一层被遮住。
- **课程卡片页**额外回答两个家长问题：
  - **这门课开设的班型**：来自卡片行里的 `· 班型:`，写的是「特色课程」里的班型名
    （一对一定制课 / 一对二·三小组课 / 一对多小班课 / 9 人以上大班课），
    自检会核对每个班型名都真实存在于特色课程；没填的课程不显示这一块。
    当前 32 张卡片都填了这四种按人数分的班型。
  - **这门课谁来上**：只按教师页的「科目」字段匹配（教师的某个科目出现在
    卡片名或阶段名里），匹配不到就明说「授课教师会按学生的年级与薄弱环节安排」，
    **不硬塞老师**。

    > **当前是「部分关联」，这是刻意的**：教师人数还少，暂时有 12 门课关联不到老师。
    > 关联逻辑完全由数据驱动 —— 在 `content.md` 的教师段补一位老师、把 `科目`
    > 写成课程里用的叫法（语文 / 物理 / 雅思…），对应课程页立刻出现这位老师，
    > **不需要改代码**。`npm run check` 会打印「暂未关联到教师的课程」清单，
    > 补人时照着清单看进度；因此自检**不要求**每门课都有老师。

### 课程卡片页：一张卡片一个页面

点课程总览里的卡片进入 `/courses/<路径>`（路径写在卡片的 `· 路径:` 里，必须是 ASCII）。

**卡片上的标签不是独立页面，而是同一页面内的「阶段」**：

```
/courses/senior-physics   高中物理 → 学考、选考 两段（页内锚点互跳）
/courses/ielts            雅思 → 总览 + 口语 / 听力 / 阅读 / 写作
/courses/gaokao-languages 高考外语 → 六种语言各一段
/courses/primary-chinese  小学语文 → 自己一段 + 关联 初中语文 / 高中语文
```

页面上给出与其他阶段的关联：**同一学科的其他学段**（小学语文 → 初中语文 / 高中语文）
与**同栏目（同子栏目）的其他课程**（高中物理 → 化学 / 生物 / …），
前者让家长看清进阶路径，后者提供横向对比。

卡片自己有总览小节时（如「雅思｜按目标分数提分」），它不对应任何标签，
但内容不能丢，因此作为页面开头的「课程说明」渲染 —— 见 `CoursePageData.overview`。

**两个页面的课程区职责不同**：

| 页面 | 课程区内容 | 回答的问题 |
| --- | --- | --- |
| 首页 | **按班型**（一对一定制课 / 一对二·三小组课 / 一对多小班课 / 9 人以上大班课 / 晚托管 / 周中预习课），数据取自「特色课程」，用 `CourseTree` 渲染 | 「你们怎么上课」 |
| 课程页 | 「课程总览」按 **栏目 → 子标题 → 卡片** 列出全部学科与课程 | 「你们教哪些课」 |

首页的六个区块顺序固定为：**教学特色 → 负责的教师团队 → 学生案例 → 试课体验 →
按班型选课 → 教室环境**（首屏与结尾的免费测评行动号召在外层）。

栏目结构定义在 `content.md` 的
**「页面: 全站 → `### 课程栏目`」**（`getCourseColumns()`），避免两处各写一遍走偏。

**核心约定：一张卡片 = 一门课程。课程内部没有细分时不要写标签，整张卡片就是入口。**

```
小学课内   小学语文 / 小学数学 / 小学英语 / 小学科学                  （4 张，无标签）
初中课内   初中语文 / 初中数学 / 初中英语 / 初中科学 / 初中社会        （5 张，无标签）
高中课内   ├ 必考科目  高中语文 / 高中数学                          （2 张，无标签）
           ├ 外语      高考外语 → 高考英语、高考日语、高考俄语、
           │                   高考德语、高考法语、高考西班牙语      （1 张，6 个标签）
           └ 七选三    高中物理 → 学考、选考（化学/生物/政治/历史/地理/技术相同）
                                                                     （7 张，各 2 个标签）
外语       雅思（无标签）
           日语 → N5、N4、N3 · 俄语/法语/德语/意大利语/西班牙语 → A1、A2、B1、B2
课外兴趣   书法与硬笔字 / 围棋入门 / 编程与信息素养 / 阅读与写作兴趣班（4 张，无标签）
成人课程   成人英语口语 / 成人零基础外语 / 出国语言备考 / 职场与商务英语（4 张，无标签）
```

「暂未开放」有两处来源，**必须一致**（`npm run check` 会拦住不一致）：

| 写在哪 | 显示在哪 |
| --- | --- |
| 卡片行里 `· 状态: 暂未开放` | 课程总览卡片的右上角标记 |
| 课程分组里 `- · 状态: 暂未开放` | 课程详情区该课程标题旁的标记 |

课外兴趣类课程（3D建模、编程与信息素养）与学科一样是 `### 课程名` + `#### 小节｜一句话`，
因为它们的内部确有细分（按工具 / 按语言分）。选修组（`- · 状态` 的叶子课程）只用于
没有细分、仅一张卡片的课程（当前是 4 门成人课程）。

格式为 `#### 卡片名 | 栏目: X · 子栏目: Y · 标签: A、B→目标小节`：

- `子栏目` 只在栏目需要再分组时写（目前只有高中课内的 必考科目 / 外语 / 七选三）。
- `标签` 只在课程内部确有细分时才写；不写 `→` 时标签名即目标小节名。
  标签文字与目标分开，例如 `学考→高中物理学考`（7 科都用「学考」「选考」两个词，
  但锚点必须各自唯一）。
- **卡片标题点哪里**：这门课在详情区有自己的说明小节时指向它；没有时（例如
  「高中物理」被拆成学考/选考两节、没有总览小节）退回到它的第一个标签。
  规则实现在 `getCourseColumns()` 里，依据是 `courseSectionNames()` 收集的真实锚点集合。
- 卡片与标签都指向课程页**详情区的小节**（`id` 锚点），小节名的唯一来源是
  `content.md` 的「页面: 课程」段。
- `npm run check` **只校验性质，不校验具体名单**：六个栏目固定、高中课内固定三个子标题、
  每个子标题下都有卡片、卡片名不重复、卡片内标签不重复、七选三每卡都带学考+选考、
  外语栏目每张语言卡都有级别标签；再加上卡片/标签与课程页小节的**双向**锚点校验
  （不能跳空，也不能有「从哪都进不去」的孤立小节）。

  > 这里刻意**不写死**课程名、卡片数、教师人数。`content.md` 是手工维护的，
  > 增删课程与教师都是正常编辑；把名单写进断言会让「改内容」和「自检通过」互相打架，
  > 而 `check` 会卡住部署，等于正常编辑也推不上去。
- 卡片标题刻意不用标题标签：层级已经是 h1 → h2 区块 → h3 栏目 → h4 子标题，
  再往下用 h5 只会让大纲更碎，链接本身已经可读。

### 文件格式

````markdown
## 页面: 首页              ← 页面分段
---
title: 让学习真正发生      ← 该页面的短字段（frontmatter）
---

### 首屏数据               ← 分组
格式为「标签 | 数值」。这段说明不会显示在页面上。

#### 班级规模 | 4–8 人      ← 条目：竖线分隔（列表项）
#### 师生比 | 1 : 6
````

- **短字段**：段落开头的 `--- ... ---` 里，`key: value` 一行一个；数组用 `- 项`。
- **分组**：`### 分组名`；分组标题下的说明文字只写给人看，不显示在页面上。
- **条目**：`#### 名称 | 内容`（列表项）或 `#### 字段: 值`（教师字段）。
  名称取最左侧分隔符：竖线优先，因此「1 : 6」这类含冒号的数值不会被误拆。
- **长文本**：直接写在分组标题下，支持 `**粗体**`、`- 列表`。
- 解析实现在 `lib/markdown.ts` 与 `lib/data/content.ts`，只支持上述子集，零依赖。

### 为什么 .md 还要生成 .ts

内容以 Markdown 维护（便于手改），但 Next 无法可靠地直接把 `.md` 当字符串导入：
其内置处理会把 `·`（U+00B7）序列化成非法的 `\xb7` 转义，产物直接语法报错。
因此 `scripts/sync-content.mjs` 会把 `data/site/*.md` 生成同名的 `.ts` 模块
（`export const contentSource = \`...\``），再由数据层 import。

这一步已自动化，正常使用无需关心：

- `npm run dev` → 启动时同步一次，并监听 `.md` 改动自动同步（刷新页面即生效）
- `npm run build` → 构建前同步一次
- `npm run sync-content` → 手工同步（备用）

### 报价页（/quote）

**报价模型**（数据在 `data/site/pricing.md`，公式在 `lib/pricing/quote.ts`）：

```
1. 课时价   = 基础价 × 科目系数 × 班级系数       （一对一 / 一对二 / 一对三 / 一对多）
   课时价   = 教师课时总费用 ÷ 班级人数           （班课）
2. 时长调整 = 课时价 × 时长乘数                    （1 小时 1.0 / 1.5 小时 1.5 / 2 小时 2.0）
3. 手续费   = 1 节 +10%，其余不加收
   最终单价 = 时长调整后价格 × (1 + 手续费百分比)
4. 正课总价 = 最终单价 × 节数
5. 试课：试课后报课满 10 节则免费；否则按课程原价收 1 节试课费
   总价 = 正课总价 + 试课费
```

| 分组 | 内容 |
| --- | --- |
| 学习阶段 | 6 个阶段 → 课程 → 基础价（元 / 课时），共 22 门 |
| 科目 | 按阶段列科目；系数写成「<科目>系数: 1.2」，未配置按 1.0 |
| 班级类型 | 一对一 1.0 / 一对二 0.7 / 一对三 0.6 / 一对多 0.5 / 班课（按人数分摊） |
| 课时选择 | 1 小时（×1.0）、1.5 小时（×1.5）、2 小时（×2.0）；参与计算但不展示换算 |
| 课程与科目 | `## 学习阶段` → `### 阶段` → `#### 课程: 名称: 价格` / `#### 科目: 科目1、科目2` |
| 报课节数 | **由用户在页面上手动输入**，不写在数据文件里 |
| 手续费规则 | 1 节 +10%，其余不加收（写在 `quote.ts`） |
| 试课 | 试课后报课满 10 节免费；未满 10 节按课程原价收 1 节（写在 `quote.ts`） |
| 其他项目 | 课后晚辅导、考试大师网课等独立产品，不参与课时公式 |

实现要点：

- 「暂未开放」写在字段值里即表示不可选：下拉框中显示但禁选（专业英语全部、
  成人/兴趣的跨国交友、高中政治）。
- 阶段与科目联动：切换阶段会清空课程与科目选择，避免出现不匹配组合。
- **科目可选**：出国考试 / 专业英语 / 成人兴趣没有科目分组，此时不显示科目下拉，
  科目系数按 1 计。
- 班课模式（coefficient 值不是数字，例如「按人数分摊」）会额外显示
  「班级人数」与「教师课时总费用」两个输入框，并按 `费用 ÷ 人数` 计算。
- 所有价格与档位均可增删，页面会自动适配。

**展示口径**：结果只给两项 —— **课单价（元 / 节）** 与 **总价**。
刻意不展示「每课时」与时长换算过程：家长关心的是每节课多少钱、一共多少钱，
中间换算属内部逻辑，摆出来反而增加理解成本。时长仍参与计算，只是不作为展示项。

⚠️ 待确认项：

1. **科目系数**目前全部按 1.0，等实际系数确定后在 pricing.md 给对应科目补一行「<科目名>系数」。
2. **手续费语义**按「加价」实现（1 节 = 课时价 × 1.1）。若应为折扣需改 `quote.ts`。
3. **试课计入的节数**：正课节数按用户填写值计算，试课另计（不免正课节数）。
4. **时长乘数**按线性实现（1.5 小时 = 1.5 倍）。若实际不是简单相乘，
   改 `pricing.md` 里「课时选择」的「乘数」即可，代码不用动。
5. 「考试大师网课 + 针对性答疑课」价格未定，当前显示「暂未定」。

### 页面读取方式

页面只调用 `lib/data/site.ts` 的函数（`getSiteBrand` / `getHomeContent` /
`getCoursesPage` / `getTeachersPage` / `getAboutContent` / `getContactContent`），
不直接读文件、不解析 Markdown。未来替换为数据库实现时页面无需修改。

## 5. 数据模型（Phase 2 落地）

五个核心实体，字段设计直接对齐未来 PostgreSQL Schema：

| 实体 | 关键字段 |
| --- | --- |
| `Student` | `id` `name` `grade` `phone` `status` `creditBalance` `createdAt` `updatedAt` |
| `Teacher` | `id` `name` `subjects[]` `phone` `status` `createdAt` `updatedAt` |
| `Classroom` | `id` `name` `capacity` `status` `createdAt` `updatedAt` |
| `Lesson` | `id` `studentId` `teacherId` `classroomId` `date` `startTime` `endTime` `status` `creditCost` `note` |
| `CreditTransaction` | `id` `studentId` `lessonId?` `amount` `type` `balanceAfter` `createdAt` |

设计约定：

- 课时余额不以 `credit_balance` 为唯一真实来源，同时记录 `CreditTransaction` 流水，便于未来对账。
- 所有实体带稳定 `id` 与 `createdAt` / `updatedAt`。
- 关系通过 `studentId` / `teacherId` / `classroomId` 外键字段表达。

## 6. 开发进度

| Phase | 内容 | 状态 |
| --- | --- | --- |
| Phase 1 | 项目基础：Next.js + TS + Tailwind + Layout + 基础 UI + 宣传网站骨架 | ✅ 已完成 |
| Phase 2 | Markdown 数据层：解析器 + 数据访问层 | ✅ 已完成 |
| Phase 3 | 宣传网站内容接入 Markdown | ✅ 已完成 |
| Phase 4 | 后台：Dashboard / 学生 / 教师 / 教室 / 课程 / 日历 | ✅ 已完成（当时是伪后端版本，页面齐全） |
| Phase 5 | 核心业务：排课 / 冲突检测 / 课程状态 / 课时扣减 / 搜索 / 收费 / 请假补课 / 统计 | ✅ 已完成（当时是伪后端版本；口径沿用至今） |
| Phase 6 | 响应式与 UI Polish | ⏳ 各页面已按断点适配，细节打磨与页面渲染逐页人工确认待补 |
| Phase 7 | 部署：GitHub Actions → GitHub Pages | ✅ 已完成 |
| — | 宣传网站扩展：学生案例 / 常见问题 / 时间安排 / 特色课程独立页 | ✅ 已完成 |
| — | 咨询线索与排课可行性（数据库 v9） | ✅ 已完成 |
| — | 报价进后端 + 教师分成规则（数据库 v10 / v11） | ✅ 已完成 |
| — | 文档体系：使用手册 / 内容维护手册 / 技术架构 / 部署与发布 | ✅ 已完成 |
| — | **接真实后端**：本机 Node + SQLite、服务端会话登录、每日自动备份与恢复演练 | ✅ 已完成（页面代码未改，见第 8 节「教务后台的后端架构」） |
| 下一阶段 | 网站内容进库做只读镜像（表已建好，同步脚本还没写）/ 页面渲染逐页人工确认 / 公网部署（机构已定暂不上服务器） | 计划中 |

### Phase 1 交付说明

- 完成 Next.js 15 + TS strict + Tailwind 4 脚手架，`lint` / `typecheck` / `build` 全部通过。
- 建立设计系统：品牌色（深墨蓝 + 暖琥珀）、状态色（成功 / 预警 / 危险）、中性色阶、圆角与字体。
- 建立布局与基础组件：`Header`（含移动端折叠菜单）、`Footer`、`Logo`、`Button`、`Card`、`Badge`、`Container`、`Section`。

### 「营业时间」与「上课时间」是两个字段

这两个概念很容易被写成同一个值，产生矛盾的说法（改之前站内有三种写法）。
现在分开定义，两处都从内容文件读：

| 字段 | 含义 | 当前值 | 显示位置 |
| --- | --- | --- | --- |
| `business_hours` | **营业时间**：工作人员在校区接待咨询 | 每日 9:00–21:00（**含节假日**） | 页脚（标为「接待时间」）、关于页、联系页 |
| `class_hours` | **上课时间**：教师与学生实际上课的时段 | 每日 8:00–22:00（**含节假日**） | 页脚、关于页、联系页、时间安排页 |

- 页脚标签刻意写成「**接待时间**」与「上课时间」：只写「营业时间」家长会以为
  8:00 就有人在；
- 时间安排页的 `notice` 把两者的差别说清（早课 8:00 开始、晚课 22:00 结束，
  但 9:00 前或 21:00 后到校区可能没人接待）；
- FAQ 里也有一条「你们几点上班？几点能上课？」；
- **教室利用率的估算基准用「上课时间」（14 小时/天）而不是营业时间**：
  教室能不能用取决于能不能上课，而不是前台有没有人。这条是这次澄清之后
  才发现的连带错误（原先按 13.5 小时估）。

自检会校验两个字段都有值、不是同一句话、上课时间的范围包住营业时间，
以及**节假日口径两处一致**（一处写「含节假日」另一处不写，家长会不知道信哪个）。

### 页脚布局

按「家长想做什么」分四栏，而不是把导航平铺成一列（以前 10 个入口混在一起，
家长找「报价」要先扫过「首页 / 学生案例 / 常见问题」）：

```
品牌 + 预约试听 / 查看报价 │ 选课与价格 │ 了解我们 │ 联系我们
──────────────────────────────────────────────────────────
© 版权                                        教务后台
```

- 导航分组在 `lib/site/nav.ts` 的 `FOOTER_GROUPS`（结构配置，不是文案）；
- **电话与邮箱是可点链接**（`tel:` / `mailto:`）：页脚常是家长在手机上最后看到
  联系方式的地方，只给一串没法点的文字等于没有。`tel:` 只保留数字与开头的 `+`，
  括号里的备注与连字符不能进去，否则手机拨不出去；
- **不放「首页」**：页头 Logo 就是回首页的入口；
- 「联系我们」的标题本身是链接（家长要找的是那一页，不只是这几个字）；
- 空值不渲染：地址、营业时间没填就不显示该行，而不是留一个空标签。
- （**当时**）宣传网站那几个页面全部可访问；还没接内容的区块一律用明确的空状态
  （`EmptyState`）而不是假数据 —— 这条原则照旧，"哪几个页面"和"哪些区块待接"
  以现在的 `app/(site)/` 与 `data/site/*.md` 为准，不再用当时的清点数说话。

## 7. 常用命令

```bash
# 前端与内容
npm run dev          # 开发服务器 http://localhost:3000
npm run lint         # ESLint
npm run typecheck    # tsc --noEmit
npm run build        # 生产构建（会先同步内容）
npm run sync-content # 手工同步 data/site/*.md（一般不需要）

# 本机后端（不运行它时后台用不了：登不进去，页面也只显示一句说明）
npm run server          # 启动后端 http://127.0.0.1:4000（首次启动会打印生成的口令）
npm run server:health   # 探活（公开的 /health 只回服务名与库文件名）
npm run server:migrate  # 建表 / 升级
npm run server:import   # 把导出的 JSON 导进服务端库（迁移与恢复用）
npm run server:backup   # 手动备份（-- --list 看现有备份，-- --force 强制再备一份）
npm run drill:restore   # 恢复演练：备份 → 验备份文件 → 删库 → 只靠备份恢复 → 核对账目

# 自检与验收（check:both / check:auth / accept / drill:restore 会自己起临时后端与临时库，
# 不碰 server/data 里的真实库与备份目录）
npm run check       # 内容结构 + 全部业务口径（跑本地内存实现）
npm run check:404   # 404 产物（实际跑一遍内联脚本）
npm run check:links # 站内链接与锚点
npm run check:all   # 上面三个一起跑
npm run check:both  # 同一套自检对「内存实现」与「真实 HTTP 后端」各跑一遍
npm run check:auth  # 服务端认证（未登录一律 401、口令不落库等）
npm run accept      # 逐页验收：自己起临时后端，逐页打开后台页面
```

日常改业务逻辑：`lint` → `typecheck` → `npm run check:all` → 更新本文件 → Git commit。
改到后端或登录相关的东西，再加跑 `check:both` 与 `check:auth`；
`check:both` 是"换后端没改口径"最硬的证据 —— 同一套断言必须两遍都过。

### 运行命令的环境要求

- **npm 必须带本地 cache**：`npm install --cache ./.npm-cache`（原因见第 9 节）。
  构建 / 运行（`dev` / `build` / `start`）**不需要**额外的 cache 参数，可直接 `npm run dev`。
- 若需在非交互环境验证页面，可用 `npx next start -p <端口>` 配合 `curl` 做冒烟测试。
- **想跟线上一致地验证，构建要带 basePath**：
  `NEXT_PUBLIC_BASE_PATH=/NexGenEdu npm run build`。
  `npm run check:404` 按 `NEXT_PUBLIC_BASE_PATH` 推断站内链接前缀，
  不带该变量做本地构建时前缀为空（这本身是正确的，不是失败）。
- **本机使用要显式指后端**：前端只在 `NEXT_PUBLIC_API_BASE` 有值时才把 `api` 换成
  远端代理（写在 `.env.local`，该文件不进 git）。不设它，后台就退回到"没有后端可连"
  的那种界面 —— 这是**刻意**的：判据是环境变量，而不是"能不能连上后端"，
  这样线上构建（不设该变量）永远保持静态站该有的样子。
- **自检与验收用的都是临时库**：脚本会把后端起在临时数据库上
  （`NEXGENEDU_DB` 指向临时文件、`NEXGENEDU_NO_BACKUP=1` 关掉自动备份），
  不会往 `server/data/nexgenedu.db` 或备份目录里写东西。

## 7.1 部署（GitHub Pages）

线上地址：**https://jeremythierrychan.github.io/NexGenEdu/**

| 项 | 值 |
| --- | --- |
| 方式 | GitHub Actions 自动部署 |
| 工作流 | `.github/workflows/deploy-pages.yml` |
| 触发 | push 到 `main`，或手动 `workflow_dispatch` |
| Pages 配置 | `build_type: workflow`（已通过 API 设置，`https_enforced: true`） |
| 产物 | 静态导出 `out/`（`output: "export"`） |

工作流步骤：`lint` → `sync-content` → `typecheck` → **`check`** → `build`（注入 basePath）
→ `.nojekyll` → **`check:404`** → **`check:links`** → 上传 → 部署。

`check` / `check:404` / `check:links` 会**卡住部署**：内容结构被改错（卡片跳空、栏目错位）
或 404 产物异常时构建直接失败，而不是把坏页面发到线上。
`check:404` 与 `check:links` 的前缀按 `NEXT_PUBLIC_BASE_PATH` 推断，因此部署步骤里也注入了同一个变量。

`check:links` 会把 out 下每个 HTML 的站内链接全部取出来验证：目标页面文件存在，
且带锚点时锚点 id 真的在目标页面里（防止「跳过去了但停在页首」）。
（**历史**：这份脚本里曾经有一份「显式放行」清单，放行后台尚未实现的几个入口 ——
它们点了会显示内部版 404，属于当时的刻意行为。那些页面后来都实现了，
清单现在是空的；留着一条不再需要的放行，等于给"点了 404"发一张通行证，所以必须删干净。）

**线上那份后台连不上后端，因此无法登录** —— 部署是静态导出，`NEXT_PUBLIC_API_BASE`
在线上没有值（也不该有：值会被打进前端包，等于把机构自己的后端地址公开出去）。
所以线上点进 `/admin` 会看到一句「后台需要本机后端」和两条本机启动命令，
而不是一个登录表单。这不是故障，是当前形态的必然结果：宣传站是给家长看的，
后台只在本机用。详见第 8 节「登录：从假登录到服务端会话」。

### 区块间距：Section 的 compact 档

`Section` 默认留白很大（`py-16 sm:py-20`、标题下方 `mb-10`），单个区块里好看，
但首页六个课程栏目依次排列时，上一个的下留白与下一个的上留白会**相加**
（约 128–160px），中间像断了一截，页面显得很空。

因此 `Section` 提供 `compact` 档：`py-6 sm:py-8`、标题下方 `mb-5`、说明文字降到 `text-sm`，
栏目间距从约 128–160px 收到 48–64px，仍靠 `border-t` 分隔，不会糊成一片。
**规则：连续排列的同类区块用 `compact`，独立的大区块保持默认。**

### 404 页面：一份产物，两版内容

静态托管（GitHub Pages）只认站点根目录的**一个** `404.html`，服务器没有机会
按路径返回不同内容。因此两版都渲染进同一个文件，由 `app/not-found.tsx` 里的
一段内联脚本读 `location.pathname` 打上 `html[data-nf-route="backoffice"]`，
再由 `app/globals.css` 里的两条规则决定显示哪一版。

| 触发场景 | 显示 | 内容 |
| --- | --- | --- |
| 网站路径（如 `/courses/` 打错） | 网站版 | 「这一页还没学到」+ 页码意象 + **完整站内目录**，让误入者能继续逛 |
| `/admin` 下的路径 | 教务后台版 | 工具化排版（等宽字、状态码、请求路径）+「该功能尚未完善，请联系管理员陈林维祎」+ 返回后台首页，**不铺站点导航、不显示页脚** |

几个刻意的处理：

- **默认显示网站版**：后台版带 `hidden`，只有脚本确认路径后才显示。JS 没跑或
  脚本出错时，用户看到的仍是一版正常的 404，而不是空白页。
- **脚本放在最前面且只写属性、不碰 DOM**：此刻两版内容还没解析出来；属性一打上
  CSS 立即生效，不会先闪一下网站版。脚本不依赖 React 水合，即使 JS 包加载失败也已执行。
- 后台的 404 当时大多是**功能还没做**（侧边栏的学生 / 教师 / 教室 / 课程安排 / 日历
  那时都还没实现），不是地址打错 —— 所以后台版的重点是「找谁」，而不是「继续浏览」。
  **现在那些页面都有了**，后台的 404 基本等于地址打错；但「找谁」这版文案留着，
  因为它对"某个深层功能还没做"的情况仍然成立，而这类情况还会再出现。
- 必须用 `!important`：Tailwind 的 preflight 用
  `[hidden]:where(...){display:none!important}` 压制 hidden 属性，
  要让后台版显示出来得用同等权重覆盖。
- `npm run check:404` 不只看字符串：它把产物里的内联脚本抠出来，用桩对象
  实际跑一遍后台路径与网站路径，确认只有后台路径被打上标记、标题被改写、请求路径被回填。

### 静态导出与子路径

- `next.config.ts` 设置 `output: "export"` + `trailingSlash: true`，导出目录形式的 URL
  （`/courses/` → `courses/index.html`），并关闭 `next/image` 优化（静态托管无优化服务）。
- 项目站点位于 `/<repo>/` 子路径，前缀通过 `NEXT_PUBLIC_BASE_PATH` 在**构建时**注入：
  `NEXT_PUBLIC_BASE_PATH=/NexGenEdu npm run build`。CI 中使用
  `${{ github.event.repository.name }}` 动态取得仓库名，fork 后无需修改。
- **本地开发绝对不要设置该变量**，否则 `localhost:3000` 会被重定向到 `/NexGenEdu`。
- 产物中的 `_next/` 目录以 `_` 开头，会被 GitHub Pages 的 Jekyll 处理忽略，
  因此工作流会写入 `out/.nojekyll`。

### 静态导出的架构含义

**宣传网站**的所有数据必须在构建时可得，所以内容仍然留在文件里（Markdown 数据层在构建期读取），
将来这部分接 PostgreSQL 时数据层签名也不变。

**教务后台**则已经从这条约束里出来了：后台的数据与服务端逻辑在本机 Node + SQLite 上跑，
前端在 `NEXT_PUBLIC_API_BASE` 有值时才把调用指向它（见第 8 节「教务后台的后端架构」）。
因此：

- **本机使用时，排课、课时扣减这些写操作已经可以跨设备共享了** —— 数据在服务端的库里，
  换台电脑连同一个后端看到的是同一份数据（接后端之前，数据存在访问者各自的浏览器存储里，
  换个设备就等于换一份）；
- **线上那份静态站不同**：构建时没有配后端地址，后台连不上、也登录不了，
  点进去显示「后台需要本机后端」（见 7.1 节）。线上只有给家长看的宣传页在工作；
- **静态导出与"数据必须构建期可得"这条约束仍然管着宣传页** —— 后台改了价、
  改了课程库，不会自己出现在线上，仍然要导出内容替换进 `data/site/*.md` 再重新构建。

## 8. 关键设计决策记录

- **不引入组件库**：UI 需求克制（卡片、按钮、徽章、表格），自建组件可保持体积与设计语言可控。
- **宣传网站与后台共用设计语言，但信息密度不同**：`(site)` 路由组大留白、大标题；`admin` 路由组紧凑、表格优先。
- **`app/(site)` 路由组**：让宣传网站与后台拥有各自独立的 layout，互不干扰。
- **`cn()` 不做 Tailwind 冲突消解**：保持零依赖与可预测性，约定调用方 `className` 最后拼接。
- **`next.config.ts` 显式设置 `outputFileTracingRoot`**：本机 HOME 下存在其它 lockfile，需固定项目根目录。

### 教务后台的后端架构（**已从「伪后端」接到真实后端**）

> 这一节分两半：**上半是当时**（服务端还没有，后台按「将来会有服务端」的样子写），
> 保留下来是因为它解释了这套分层为什么长成这样；**下半是现状**。看代码请以下半为准。

**当时（伪后端阶段）**：服务端还没有，但后台的所有页面都按「将来会有服务端」的样子写：

```
页面（app/admin/*，客户端组件）
   ↓ 只调用这一层，方法签名与 REST 一致
lib/backend/api.ts        list / get / create / update / remove / today / reset
   ↓ 读写一份 JSON 快照
lib/backend/storage.ts    KeyValueStore 抽象：浏览器里是 localStorage，Node 里是内存
   ↓ 首次访问灌入
lib/backend/seed.ts       示例数据（教师与教室取自站点真实数据，学生/排课为示例）
lib/backend/pricing.ts    报价：计价模型、公式与规则（价格是数据，不是常量）
```

当时写的三个设计要点，逐条对照现在的结果：

- **异步到底**：所有方法都是 `async` 并带轻微延迟，页面上不会出现「同步读 localStorage」
  这种接服务端时必须重写的写法 —— **这一条兑现了**。正因为它本来就没有同步写法，
  后来换成远端代理时页面一行都没改（代理返回的本来就是 Promise）；
- **存储可替换**：`KeyValueStore` 让同一套服务层既能在浏览器里跑（localStorage），
  也能在 Node 里跑（内存）—— **这一条也兑现了**，而且代价比预想的还小：
  接后端时只多写了一个 SQLite 版实现（`server/kv-store.mts`，整库当一份 JSON 存进 `kv` 表），
  业务代码没有改动。（`lib/backend/storage.ts` 那套本地实现仍在代码里 ——
  `npm run check:both` 的**第一遍**（内存后端）跑的就是它，第二遍才是真实 HTTP 后端。）
- **换真后端时**：只替换 `lib/backend/api.ts` 的实现（改成 `fetch`），`types.ts` 与
  全部页面代码不动 —— **结论对了一半，做法和这里预想的完全不同，值得记下来**：
  `api.ts` 并没有被改写成 fetch 版。那样等于把方法清单与参数再抄一遍，
  少一个方法、错一个字段都是**静默**出错。实际做法是加一层**代理**
  （`lib/backend/remote.ts`）：它遍历本地 `api` 的结构，把每个方法换成 `POST /api/call`，
  方法名、参数个数、返回类型全部与本地一致。**页面代码确实一行没改**，连类型都没改 ——
  `export type BackendApi = typeof api` 这条约束同时管住了代理（代理必须与本地同形）。

**现状（接后端之后）**：

```
页面（app/admin/*，客户端组件）—— 与伪后端阶段是同一份代码
   ↓ 只调用 api 这一层
lib/backend/api.ts（业务口径的唯一实现：课时、金额、冲突判定、报价都在这里）
   ↓ 本机使用时 api 被换成一个代理（只有 NEXT_PUBLIC_API_BASE 有值才换）
lib/backend/remote.ts     POST /api/call { method, args } + Bearer 令牌
   ↓ HTTP（默认 127.0.0.1，见「登录」一节）
server/index.mts          登录 / 会话闸门 / 调用分发 / 自动备份
   ↓ 同一个 KeyValueStore 接口，另一个实现
server/kv-store.mts       SQLite：kv 表存整份 JSON 快照（server/data/nexgenedu.db，不进 git）
```

最要紧的一点是**业务口径只有一份实现**：服务端跑的就是 `lib/backend/api.ts` 这个文件，
不是"照着它再写一遍"。所以"课时怎么扣、金额怎么算、冲突怎么判、报价怎么算"
不存在两个版本 —— 这正是 `npm run check:both` 在做的事：同一套断言对
「本地内存实现」与「真实 HTTP 后端」各跑一遍，两边必须一致。换后端没改口径，
靠的不是人记得，是这条自检。

**数据边界（现状）**：

| 场景 | 数据存在哪 | 换设备 / 清缓存会怎样 |
| --- | --- | --- |
| 本机使用（配了 `NEXT_PUBLIC_API_BASE`） | 服务端 SQLite（整库一份快照） | 不会丢；后端每天自动备份一份 |
| 线上静态站（没配后端） | **没有能存数据的地方**，后台也无法登录 | 后台根本用不了，点进去是一句「后台需要本机后端」的说明 |

（**历史**：伪后端阶段数据只存在访问者当前这一份浏览器存储里，换设备看不到、清缓存会丢、
多标签页并发写入以最后写入为准；那时界面上有「示例数据」提示条与一键重置入口。
接后端之后这两处都改了：`components/admin/DataNotice.tsx` 现在显示的是**连没连上后端**，
**一键重置也去掉了** —— 它在真实数据上是个随时可能误触的破坏性按钮，
而它想解决的"从头来过"用「数据与备份 → 导入空库」就够了，那条路还会先自动备份。）

仍然存在的边界（不是 bug，是当前范围）：**单用户单账号、没有并发控制、没有权限区分**；
存储是**快照式**的（整库当一份 JSON 存着，没有按行查询）。
将来要多人同时用、要按行查数据，才需要把 KeyValueStore 换成真正的表结构（见「后端开发方案」）。

### 后台模块现状

| 模块 | 路径 | 状态 |
| --- | --- | --- |
| 咨询 | `/admin/inquiries` | ✅ 家长咨询登记 + 排课可行性（含最接近方案与协调已有课） |
| 今日概览 | `/admin` | ✅ 今日课程 / 教室占用 / 课时预警 / 档案规模 |
| 学生 | `/admin/students` | ✅ 列表 + 搜索 + 建档 / 编辑 / 删除 + 详情三个页签（报课与课时 / 信息采集表 / 排课记录） |
| 教师 | `/admin/teachers` | ✅ 列表 + 在职开关 + 排课清单 |
| 教室 | `/admin/classrooms` | ✅ 用途（上课用教室 / 自习室）、容量、**可用时段**、占用情况 |
| 课程库 | `/admin/courses` | ✅ 课程台账（网站课程 + 后台新增）、可开班型、状态；排课与教师科目都取自这里 |
| 课程安排 | `/admin/lessons` | ✅ 按天排课 + 冲突检测 + 标记已上（按出勤扣课时）+ 待补课与安排补课 |
| 日历 | `/admin/calendar` | ✅ 按周查看排课密度与空档（只读，避免两处可改） |
| 课表与占用 | `/admin/timetable` | ✅ 教师课表 / 教室占用两个视角，按周切换 + 导出 ICS |
| 统计 | `/admin/stats` | ✅ 教室利用率与空档、时段分布、教师课时、退课与流失 |
| 待跟进 | `/admin/followups` | ✅ 自动挑出需联系家长的学生，附可直接复制的话术草稿 |
| 收费 | `/admin/finance` | ✅ 本月收款/退款/净收入、按方式拆分、欠费清单、退费口径说明 |
| 报价 | `/admin/pricing` | ✅ 基础价 / 系数 / 计费规则 / 教师分成可改 + 家长报测试算 + 教师课时费与人数对照表 + 导出上线 |
| 数据与备份 | `/admin/data` | ✅ 全量 JSON 导出/导入（含备份后悔药）、学生名单 CSV、课程 ICS、操作日志 |

共用件：`components/admin/AdminFields.tsx`（后台表单控件）、
`components/admin/DataNotice.tsx`（**连没连上后端**的状态提示条；接后端之前它显示的是
「示例数据」提示与一键重置）、`lib/backend/format.ts`（日期时间格式化）。

### 接口契约与转真后端（`lib/backend/contract.ts` + `docs/后台API约定.md`）

**接口契约**是接真后端之前最重要的产出之一 —— 它按「服务端怎么做」把全部方法分成七类，
而不是平铺一张表（平铺只会让实现者挨个猜「这个要不要事务」）：

| 分组 | 方法数 | 服务端的做法 |
| --- | --- | --- |
| 通用 CRUD | 45 | 9 个资源 × 5 个方法，一套 REST 即可；id 由服务端生成 |
| 关联查询 | 18 | 查询参数，不要前端全表过滤 |
| 业务动作 | 12 | **必须一个事务**（报课要同时落报课记录 + 收款 + 课时流水） |
| 咨询线索 | 8 | 判定是纯函数；落库前必须再复核一次（判定是「看」、落库是「改」） |
| 报价配置 | 5 | 价格是数据；报价只收「选择」不收价格；公式只有一份实现 |
| 看板与统计 | 8 | 只读，算法是纯函数可原样搬走；口径不要改 |
| 运维与审计 | 9 | 导入保持三道保险；按需导出照数据集清单实现；日志改为服务端不可篡改的审计表 |

`docs/后台API约定.md` 里另有两份清单：

- **服务端必须自己复核的 12 项校验**（冲突、容量、幂等、课时非负、金额不变式、
  退费重算、请假判定用服务端时间、**报价配置合法性**、**报价金额由服务端算**、
  鉴权、审计）—— 每一项都对应一个已经踩过的坑；
- **迁移六步**（冻结格式 → 实现接口 → 前端只改 `api.ts` → 搬数据 → 换登录 → 退出条件）——
  **前五步都走完了**：接口由服务端实现、数据搬进了服务端库、登录换成了服务端会话。
  第六步（退出条件）**大部分成立、有一处要打折不装作成立**：服务端确实通过了同一套自检
  口径（这正是 `npm run check:both` 在证明的），前端也不再直接读**业务数据**；
  但"前端不再有任何一处直接读 localStorage"这句按字面还没做到 ——
  **登录令牌仍然存在 localStorage 里**（`lib/auth/token.ts`）。那是登录态、不是业务数据，
  仍然要记在这里，免得下次有人拿这条当"已经彻底不用浏览器存储"的证据。

**文档不会过期**：`npm run check` 会双向比对「契约清单 ↔ `api.ts` 真实形状」，
并按边界匹配检查文档提到了每一个方法（用子串会被 `lessons.list` /
`lessons.listByDate` 这类前缀蒙过去）。加了方法不更新文档，自检就红。

接后端这件事的实际改动点与这里原先的预想不同：**页面代码一行没改、类型没改，
但被替换的不是 `api.ts` 本身，而是它外面的那一层代理**（`lib/backend/remote.ts`）——
原因见本节上面「教务后台的后端架构」。

还有一处要分清：**上面那张表的「服务端的做法」一列是按"要写一个 REST 服务端"写的设想**，
而实际落地的是**路线 B** —— 页面走一个 `POST /api/call` 入口、服务端直接跑同一份 `api.ts`。
REST 那一套并没有被丢掉：服务端里确实有一批 `/api/students`、`/api/pricing/…` 形式的接口，
但它们是**路线 A 的参考实现**（页面不用它们，留着是为了"真要写 REST 时该长什么样"有个样子，
它们和 `/api/call` 一样受登录闸门保护）。所以右列那些建议（一套 REST、
查询参数不要前端全表过滤、业务动作必须一个事务）应当读成**另一种做法的备选**，
而不是现状描述；分组本身（哪些是 CRUD、哪些是业务动作、哪些必须复核）仍然有用。

### 咨询可行性（`lib/backend/inquiry.ts` + `/admin/inquiries`，数据库 v9）

家长口头问「每周六上午十点、指定陈老师，能不能排」——这类问题要**当场**给答复，
答错了要么白答应（事后排不出课），要么白白推掉一个学生。因此判定写成可测的纯函数。

判定的三条要点：

1. **检查的是一串日期，不是一天**：每周一次 × 12 节 = 未来 12 个同一时段都得空着。
   只看第一次是这个功能最容易犯的错（自检专门有一条「系列中途撞课」的用例）；
2. **候选时段是「备选」而不是「全要」**：家长给两个时段是「哪个都行」，
   因此依次尝试，第一个能排下的就用它；
3. **不可行要说清「挡路的是谁」**：不是「不行」，而是「9月20日 17:30 陈老师在上
   另一节课（学生：张小明）」—— 因为接着要决定是让新学生换时段，
   还是去协调那位已有学生。

最接近的方案按「改动最小」排序：同一天改时间 → 邻日同时段 → 换老师 → 换教室；
每条都用同一套判定复核，给出来的方案一定是真能排的。

配套的三处服务端约束：

- `inquiries.accept` 落库前**再复核一次**：判定是「看」、落库是「改」，
  中间的时间差不能忽略；通过后一次性建出整串课（不是一节一节点）；
- `inquiries.abandon` 记原因，线索状态为待确认 / 已安排 / 已放弃；
- `lessons.suggestMoves` 给挡路的已有课算出可挪的时间（用于「协调已有学生」），
  且**界面必须显示受影响的已有学生**再让管理员决定动不动它，绝不静默改动别人的课。

跳过周（假期、考试周）在生成日期串时跳过并**往后顺延**，总节数不变 ——
与线下机构「报 12 节就是 12 节」的约定一致。

界面（`/admin/inquiries`）：

- **登记表**：学生信息 / 科目 / 任意时段（周几 + 时间）/ 每周或每两周 / 时长 / 节数 /
  起始日期 / 指定教师或「不限」/ 指定场地或「不限」/ 跳过哪些日期；
  时段旁边给一排**学校标准时段**的快捷按钮，那些值直接从「时间安排」页的内容里读
  （`getStandardSlots()`），不另抄一份；
- **可行性报告**：每个候选取一行，可行就显示推荐教师 + 场地 + 前几次日期并可
  「采用这个方案」（一次性建出整串课）；不可行就说清挡路的是谁；
- **两个方向都摆在眼前**：新学生换时段（采用替代方案），或已有课挪走
  （点「协调已有学生」→ 列出 `suggestMoves()` 算出的可挪时间）。
  **移动前先显示受影响的已有学生**，决定权在人，系统不替他决定；
- **线索列表**：待确认 / 已安排 / 已放弃三种状态，放弃时可写原因（记进备注）。

端到端主线（自检里串起来走了一遍）：**时段被已有课挡住 → 报告指出挡路的是谁 →
把已有课挪到系统算出的时间 → 再判定通过**。挪课会写操作日志 ——
`lessons.update` 是自定义覆盖过的方法（为了处理课时撤销），不会自动记日志，
因此它自己写；「不静默改动别人的课」这句话得有日志兜着。

### 全局搜索与操作日志

**全局搜索**（后台顶栏，`lib/backend/search.ts` 纯函数）：一个框搜五类对象 ——
学生 / 教师 / 教室 / 排课 / 课程，按类别分组，点进去直达。

- 匹配规则是**不区分大小写的子串**，刻意不做拼音/模糊匹配：后台里「张」和「章」
  是两个学生，**宁可搜不到也不要搜错人**；
- 结果带上参数直达：学生 → `/admin/students?studentId=…`（自动展开详情）、
  排课 → `/admin/lessons?date=…`（自动切到那一天）。两个页面读参数用
  `window.location` 而不是 `useSearchParams` —— 后者在静态导出下会触发
  CSR bailout，把整页踢出预渲染的 HTML；
- 空关键词返回空结果，不把全部数据倒出来。

**操作日志**（`/admin/data` 页，数据库 v8）：记录「谁在什么时候改了什么」。

- **写在服务层，不由页面负责**：各业务方法自己调用 `writeLog()`，
  通用集合（学生 / 教师 / 教室 / 排课 / 作业 / 测评）自动记录新建、修改、删除，
  自定义业务动作（报课 / 续费 / 退课 / 标记已上 / 课堂记录 / 填采集表 /
  导入 / 恢复 / 重置）各自写更具体的摘要。依赖调用方自觉写日志一定会漏；
- 修改日志会记下**改了哪些字段** —— 只说「修改了学生」等于没说；
- **条数上限 500**（旧的丢弃）：日志本身也占存储，无上限地涨下去，
  几年后一份 JSON 会大到导不出来；
- 操作人**由服务端按会话决定**。**历史**：伪后端阶段是"后台外壳在登录后调用
  `api.setOperator(name)` 把操作人写给服务层"，而 `setOperator` 是个**同步**方法 ——
  经远端代理一包就静默变成 Promise，写进去的永远是默认值（前端"说了"但没生效）。
  现在操作人取自服务端会话，前端说了不算，这类"看起来写了、其实没写"的坑也就没有了；
- 日志现在随整库存在**服务端**（`/admin/data` 页面照旧可看），
  但**仍然是快照的一部分、不是独立的只追加审计表**：原始计划里写的"接服务端后
  改为服务端不可篡改的审计表"只做了一半 —— 操作人不可由前端伪造了，
  可日志本身仍会随整库导入导出一起被覆盖。要做成真正的审计表，得等存储从
  "整库一份 JSON 快照"换成按行存储。

### 经营统计（`/admin/stats`）

三个数字回答的是**决策问题**，不是「给老板看的报表」（算法在 `lib/backend/stats.ts`，纯函数）：

| 统计 | 回答什么 | 口径要点 |
| --- | --- | --- |
| 教室利用率 | 这间教室值不值？该加教室，还是把课挪到别的时段？ | 可用时长优先按教室自己的「可用时段」；没设时段的按**上课时间**估算（每天 8:00–22:00 共 14 小时，不是营业时间）。**只算未取消的课** |
| 时段分布 | 哪个时段最挤、哪个时段空 | 按小时统计课次；最忙时段排满了 → 加时段比加教室更有效 |
| 教师课时 | 谁排满了、谁还空着 | 课次 / 时长 / 涉及学生数（去重）/ 平均每节人数 / 按科目拆分 |
| 退课与流失 | 学生在什么阶段退、原因集中在哪 | 按**退掉的课时**算而不是按条数；重点是「平均已上比例」——若集中在两三成，问题在入门期 |

「空档日」只统计**有可用时段却没排课**的日子：周六日不在某间教室的可用时段内，
不算它的空档 —— 否则每间教室每周都显示两天「空档」，看着像浪费。

### 请假与补课（数据库 v7）

口径：**一节课扣一次，扣的是「这次时间被占用」**（`lib/backend/attendance.ts`）。

| 情况 | 是否占用时间 | 扣课时 |
| --- | --- | --- |
| 到课 | 是 | 扣 1 |
| **提前 ≥ 24 小时请假** | 否（机构来得及另排） | **不扣** |
| 临时缺课 / 旷课 / 请假但未记录时间 | 是 | 扣 1 |
| 补课 | 是（重新占用教师与教室） | 扣 1 |

于是：**提前请假 + 补课 = 扣 1 节**（等于正常上课）；
**临时缺课 + 补课 = 扣 2 节**（缺课占一次、补课又占一次）——
这正是 24 小时规则的威慑力所在，也方便向家长解释。

- 课堂记录因此多了一个**请假时间**字段：只记「请假」是不够的，
  得知道提前了多久才能判断扣不扣；
- **没记录请假时间按临时缺课处理**（不默认成对机构有利的解释），
  界面提示「建议补录」，补录后课时自动调整；
- **对账而不是一次性扣减**：出勤是会变的（先标到课、后发现是提前请假），
  因此 `reconcileCharge()` 让「已扣」与「应扣」保持一致 ——
  该扣没扣就补扣、不该扣却扣了就按流水退回，**幂等**（重复保存不会重复扣）；
- **待补课清单**（课程安排页）：列出请假/旷课但还没补的学生，
  一键安排补课；补课以原课为模板建一节新课，走同一套冲突检查；
  补课被取消就重新回到清单，原课被取消则不再要求补课。

### 常见问题（`data/site/faq.md`）

八个分组、43 条问题（按家长找答案的路径排）：

```
试课与报名 → 课时与收费 → 班级与排课 → 请假与补课 →
老师与教学 → 学习过程与反馈 → 服务形式 → 特殊情况
```

写答案时有两条约定（写在文件顶部，提醒后来的人）：

1. **只写有依据的说法**：价格、规则、承诺都要能对上站内其它内容
   （例如「报满 10 节试课免费」对得上报价页，「提前 24 小时请假不扣课时」
   对得上后台的请假规则）；
2. **拿不准就写「以咨询确认为准」**，不要为了好看编一个数字。

`请假与补课` 这一组直接来自后台确认过的规则；`老师与教学` 里说明了
「AI 学习伙伴是辅助工具、授课仍由教师完成」，避免家长误会成机器人上课。

### 待跟进清单与家长话术（`/admin/followups`）

这些信号本来就躺在数据里（课时、欠费、作业、测评、出勤、排课），
但要靠人一样样去翻 —— 翻的结果通常是「想起来才看」。这一页的价值是**不用想**：
打开就是今天该联系谁，以及可以直接复制的话术。

六条规则与阈值集中在 `lib/backend/followup.ts` 的 `FOLLOWUP_RULES`：

| 规则 | 触发条件 | 严重度 |
| --- | --- | --- |
| 课时不足 | 剩余 ≤ 5 节（≤ 2 节为紧急） | 提醒 / 紧急 |
| 欠费 | 约定应缴 > 实收 | 紧急 |
| 作业异常 | 最近 5 次里未交 ≥ 2 或非按时 ≥ 3 | 提醒 |
| 测评下滑 | 同科目最近一次低于上一次（≥10 分为提醒） | 提醒 / 关注 |
| 出勤异常 | 最近 5 次里旷课 ≥ 1 或请假 ≥ 2 | 提醒 / 关注 |
| 久未排课 | 有课时但未来 7 天没课（已取消不算） | 关注 |

三条原则：**规则与阈值集中一处**（要调松紧只改这里）；
**话术是草稿**（数字已填好，发之前请自己看一遍，不给家长发机器味的话）；
**不猜**（没有测评、没有作业记录时不产生条目，而不是把「没有记录」当「表现不好」）。

规则引擎是纯函数，因此自检能逐条造数据验证：命中、不命中、以及边界
（课时刚好卡在阈值、已退课的报课不参与、已取消的课不算排课、结课学生跳过）。

### 话术专区（`/admin/scripts`，`lib/backend/scripts.ts`）

机构日常要说的话按 **11 个场景组、35 条**整理成可直接复制的草稿：首次咨询与接待、
报价与价格说明、试课与转化、排课与时间协调、请假与调课、续费与课时提醒、欠费与收款、
学习反馈与成绩波动、投诉与情绪处理、退费与流失挽回、教师相关。

**为什么做成代码里的模板，而不是像报价那样做成可编辑数据？**

话术里含**口径**（提前 24 小时请假不扣课时、临时缺课扣 1 节、缺课+补课=扣 2 节、
试课满 10 节免费否则按原价收 1 节、只报 1 节加收 10%、两种退费口径）。这些口径的
权威实现在别处（`attendance.ts` / `finance.ts` / `pricing.ts`），话术与它们**必须同步改**：
口径改了话术没改，就会出现「系统按新规则扣课时、老师按旧话术跟家长承诺」——
那是纠纷。所以当前阶段让话术**随版本走**：改口径的提交里一起改话术，
`npm run check` 会盯着关键数字（24 小时 / 10 节 / 按实付 / 1 节 +10% / 8:00–22:00），
以及「内部信息不得出现」这条红线。

等机构要自己改文案时，再照报价配置的做法（数据 + 后台可编辑 + 导出）把它变成数据 ——
那时机是「口径稳定、机构想改措辞」，而不是现在。

**一条红线：内部信息不进话术。** 教师分成比例、教师课时费、机构留存、其他学生的信息
一条都不能出现在话术里（这些话是发给家长的）。自检里有断言逐条扫描关键词，
不是靠自觉，是机器拦。

页面上还能**套用某个学生**：选一名学生（取 `api.students.list()`），把
`{学生}` `{科目}` `{剩余课时}` 按真实数据填好再复制；老师、时间、金额这类
因场景而异的值**刻意留空**并显眼提示「还需要自己补哪几项」——
猜错比空着更糟（把 `{老师}` 猜成某位老师发出去就是错的）。占位符缺值时**原样保留**，
不会悄悄变成空白。

### 收费与金额（数据库 v6）

钱的规则集中在 `lib/backend/finance.ts`（纯函数），因为「金额怎么算」是最容易
各处口径不一的地方：报课表单算一次、对账单算一次、退费再算一次，迟早对不上。

三个金额概念**刻意分开**（一次报课可能分期付款，也可能给优惠）：

| 概念 | 含义 |
| --- | --- |
| 标价 | 总课时 × 单价：挂牌价，用来展示「优惠了多少」 |
| 约定应缴 | 与家长谈定的总额：可低于标价（优惠），也可高于实收（分期未付） |
| 实收 | 实际到账累计，**由收款流水累加得出**，不手工维护 |

推出：优惠 = 标价 − 约定应缴；欠费 = 约定应缴 − 实收。

- **收款与课时是两本账**：一次报课可以课时一次给、钱分几次收；也有纯补款。
  因此收款记录独立成表（`payments`），报课的 `paidAmount` 由它累加。
  **不变式（自检逐条校验）**：每条报课的实收 = 其收款合计 − 退款合计。
- **退费是策略，不是写死的公式**：`REFUND_POLICIES` 里现在有两条 ——
  「按实付比例退」（默认，最好向家长解释）与「追回已上课时的标价」
  （防上完就退）。退课前先在界面上看到金额与公式再确认；要换成实际规则，
  改这一个数组即可，服务层与界面都从那里取。
- **欠费不按月切分**：它是一笔「还没收到的钱」，跨月存在，
  按月切会让 1 月欠的费在 2 月报表里凭空消失。
- 迁移（v5 → v6）给老报课记录补金额字段，**一律记 0 而不是猜**：
  猜一个单价会让欠费清单凭空冒出金额，比留空危险得多。

### 报价在后端（`lib/backend/pricing.ts` + `/admin/pricing`，数据库 v11）

报价原先只有宣传页上那个计算器：公式在页面侧（`lib/pricing/quote.ts`），
手续费 10% 与「满 10 节试课免费」还是**硬编码的常量**。结果是调一次价要改代码、
重新构建，家长打电话来时也只能切到宣传页点一遍。现在价格是数据、公式在后端一侧。

**计价模型**（每个系数各管一件事，互不重叠 —— 这是「基础价、系数是什么意思」的答案）：

| 维度 | 字段 | 含义 | 默认 |
| --- | --- | --- | --- |
| 基础价 | `basePrice` | **一对一、1 小时、报 2 节及以上**的价格（元 / 节），所有换算的基准 | 分阶段设定 |
| 科目系数 | `subject.coefficient` | 同一阶段内不同科目的师资 / 难度差异 | 1.0（不加价） |
| 班级系数 | `classType.coefficient` | 人越多每人越便宜 | 一对二 0.7、一对三 0.6、一对多 0.5 |
| 时长乘数 | `duration.multiplier` | 一节课上多久，1 小时为基准 | 1 / 1.5 / 2 小时 → 1.0 / 1.5 / 2.0 |
| 手续费 | `rules.singleLessonFeePercent` | 只报 1 节时加收（一次课不划算） | 10% |
| 试课 | `rules.freeTrialMinLessons` | 报满多少节后试课免费，否则按原价收 1 节 | 10 节 |

两个**不是乘系数**的例外：班课按「教师课时总费用 ÷ 班级人数」分摊；
试课费永远按课程**基础价原价**收（不带科目系数、班级系数、时长与手续费）。

**教师分成（课时费）**也在这一份配置里：课内按系数计价的班型（一对一定制课 /
一对二 / 一对三小组课 / 一对多小班课，1–8 人）适用

```
教师课时费 = 小时数 × (课程单价 / 小时) × (0.4 + (学生人数 − 1) × 0.1)
```

即「第一名学生 40%，每多一名学生加 10 个百分点」（8 人 110%）；9 人以上大班课不适用，
按「教师课时总费用 ÷ 班级人数」另议。公式里的「课程单价」默认取课程标准单价
（基础价 × 科目系数，不含班级折扣），也可切换为班型课时价 —— 后台一键切换，
切换不影响家长看到的报价。算法与人话说明在 `lib/backend/teacher-share.ts`，
做成「规则原文 + 人话三条 + 人数对照表」，后台报价页直接展示，
`api.pricing.teacherFee` 负责算（教师工资与报价一样，只收选择、不收金额）。

**代码分工**：

- `lib/backend/pricing.ts`：配置类型、公式（`calculateQuote`）、配置校验、
  按名字解析选择、Markdown 导出 —— 公式只有这一份实现；
- `lib/pricing/quote.ts`：只剩一层薄适配（取站点内容的规则转发给后端公式），
  页面（`EstimateForm`、`/quote`）的调用方式完全不变；
- `api.pricing.{get,update,reset,quote,teacherFee,exportMarkdown}`：后台服务接口，
  `quote` **只收「选了哪门课、哪个班型、多少节」，不收价格** —— 价格一律由服务端查；
- `data/site/pricing.md`：新增「计费规则」分组，科目支持 `物理 ×1.1` 写法（省略即按 1，老内容不用改）。

**后台与线上报价页之间的边界（重要，接后端之后依然成立）**：报价配置存在**服务端数据库**里，
而家长看到的报价页是**构建期读内容文件**的静态页面 —— 服务端库在构建时读不到、
线上也连不上，所以后台改完价**不会自动上线**：必须点「导出配置」，
把导出的片段替换进 `data/site/pricing.md`（「## 学习阶段」及其后的部分，页面文案字段不要动），
再重新构建发布。
（**历史**：这句话原先给的理由是"后台改的价格困在本地存储里，构建期根本读不到"。
**已经迁移**：结论没变，但原因换了 —— 现在卡住的是静态导出这个形态，
不是存储位置。把新旧理由都写下来，免得下次误以为"接了后端就自动上线了"。）
后台报价页顶部把这件事写在最显眼的位置，`api.pricing.get()` 返回的 `source`
也会区分「站点内容」与「后台修改（未导出上线）」。

**自检盯住的事**（这一组最容易出的事故是「宣传页一个价、后台另一个价」）：

- 同一方案在**前台公式与后台服务上必须算出同一个数**（7 个用例：含 1 节手续费、
  满门槛试课免费、班课人数分摊、2 小时、未开放课程、节数为 0 的报错）；
- 科目系数解析（`物理 ×1.1` / `化学 x1.05` / 省略按 1），缺「计费规则」分组时用默认规则；
- 坏配置进不了库（科目系数 0、手续费 120%、负的基础价、课程重名），且被拒后库里配置不变；
- 改价真的影响报价，并留下「哪门课从多少改到多少」的操作日志；
- 导出的 Markdown **回读后与后台配置完全一致** —— 导出的必须是能用的内容，
  而不是一段看起来像的文本；
- v9 老库升级后按站点内容补齐报价配置，且价格与升级前一致（升级不会让任何一节课变价）。

### 按需导出（`lib/backend/export.ts`，数据集 × 选中的行 × 格式）

原先只有三个固定出口（全部数据 JSON、学生名单 CSV、全部课程 ICS），于是「把这学期围棋课的
排课导出来发给老师」只能导全量再自己筛。现在导出是三段式：

| 维度 | 说明 |
| --- | --- |
| 数据集 | 学生 / 排课 / 教师 / 教室 / 课程 / 报课记录 / 收款记录 / 课堂记录（清单在 `EXPORT_DATASETS`，加实体只改这一处，界面与自检都从它取） |
| 选中的行 | `ids` 为空 = **全选**；传了 = 只导这几条 |
| 格式 | CSV / JSON 对所有数据集；**排课**另有 ICS（进手机日历）、**课程**另有 Markdown（粘回内容文件） |

几个刻意的决定：

- **CSV 列是「人看的」**：表头写中文，内容里把 id 换成名字（教室 id → 「301 教室」）——
  导出是给 Excel 和别的系统看的，一列 uuid 没有意义；
- **结果必须区分「导了 M 条」与「共 N 条」**：只报「已导出」会让人以为导全了；
- **不支持就报错**，不产生空文件（例如给「教师」导 ICS 会被拒绝并说明原因）；
- **只读**：导出不改任何数据（自检里跑完一轮导出会比对数据是否完全一致）；
- 复用了 `backup.ts` 已有的 CSV（带 BOM、转义）与 ICS（RFC 5545 转义 + 折行）实现，
  不另写一套 —— 两套实现迟早会在某个转义细节上分叉。

### 导出与备份（`/admin/data`）

这一组不只是「导出一下」，它承担三件事：

1. **留一份能拿在手上的数据** —— 备份现在由服务端负责（每天自动一份，见下一节），
   导出解决的是另一类需求：留档、交给别人、在别的机器上用、以及"库这一层出了问题"
   时手边还有一份能读的 JSON。**已经迁移**：接后端之前导出的意义是"唯一的保险"
   （那时数据只在用它的那一台机器上，换个设备就是另一份）；现在它不是唯一保险了，
   但也不是可有可无 —— 自动备份和库在同一个地方，"库连同备份一起没了"是它唯一挡不住的情况；
2. **迁移与搬库**：导出 JSON → 在服务端导入，这条路径**已经用过了**
   （`npm run server:import`），现在它同时是"从别处搬一份数据进来"和"恢复某一份旧留档"的入口；
3. **导入是唯一能一次性毁掉全部数据的操作**，因此配了三道保险：

| 保险 | 做法 |
| --- | --- |
| 结构校验 | 不是对象 / 缺 `version` / 版本比程序新 / 缺核心表 / 记录缺 `id` → **直接拒绝，不碰现有数据** |
| 导入前自动备份 | 当前数据先写一份快照（键名仍叫 `nexgenedu.admin.db.backup.v1`，现在存在服务端库里），界面上出现「恢复导入前的数据」 |
| 自动升级 | 旧版本文件先过 `migrate()` 再替换，提示会写明 `v2 → v5` 这类升级路径 |

刻意**不做业务校验**：报课科目为空之类的问题照常导进来、由界面暴露，
否则一条脏数据就会让人无法修复数据。结构不对才拒收。

导出格式：JSON（完整备份）/ CSV（学生名单，**带 BOM**，否则 Excel 打开中文乱码）/
ICS（日历）。ICS 里 UID 用 `lesson-<id>@nexgenedu`，重复导入不会产生副本；
时间用本地时间（不带 Z）；文本按 RFC 5545 转义并折行。

教师课表也可以在「课表与占用」页单独导出（选中某位老师 → 导出本周课表 ICS），
老师导进手机日历就不用回后台查课。

### 服务端备份（`server/backup.mts` + `server/backup-cli.mts`）

数据搬到服务端之后，"备份"从"记得导出"变成了**后端自己每天做一次**：

- **每天自动一份**：后端启动时会补齐当天缺的那一份，运行期间每小时再检查一次 ——
  机构不会每天都点一次按钮，所以这件事不能靠人记得；临时库（自检、演练、
  逐页验收）用 `NEXGENEDU_NO_BACKUP=1` 关掉，免得测试数据混进正式备份目录；
- **保留份数默认 90**（约三个月的回溯窗口，`NEXGENEDU_BACKUP_KEEP` 可改）：
  库是 KB 级，90 份也就十几 MB，宁多勿少 —— 真正要防的是**无上限**（目录无声涨到没人管）；
- **文件名是唯一的时间真相**：`nexgenedu-YYYY-MM-DD-HH-mm-ss.db`（本地时间），
  排序与"哪份最新"一律以**文件名里的时间戳**为准，**不用 mtime** ——
  这条是恢复演练逼出来的：复制、移动、rsync、从别处恢复都会改写 mtime，
  演练里正是按 mtime 排序把两份真备份当成"最旧的"清掉了；
- **只清理符合命名规则的文件**：目录里手放的 JSON 留档（例如某次的空库、某次的清理前快照）
  不会被自动清理碰到；
- **手动**：`npm run server:backup`（`-- --list` 列出现有备份、`-- --force` 立刻再备一份）；
- **恢复演练**：`npm run drill:restore` 走一整圈 —— 备份 → 先验备份文件本身 →
  **删库** → 只靠备份文件恢复 → 核对条数、金额、课时账本一致。
  备份最怕的是"以为有、真要用时打不开"，所以这一步刻意做成可重复执行的自检，
  而不是一份"记得每季度演练一次"的流程文档。

### 怎么用（后台）

**本机使用**要跑两个进程：`npm run server`（后端，默认 `127.0.0.1:4000`）与 `npm run dev`
（前端，`localhost:3000`），并在 `.env.local` 里设 `NEXT_PUBLIC_API_BASE` 指后端。
然后访问 `/admin` → 用账号 `admin` 与**后端启动时打印的那次口令**登录
（口令由服务端生成/持有，见下面「登录：从假登录到服务端会话」）。

**线上那份不一样**：`<站点>/admin/` 点开是「后台需要本机后端」的说明，**登不进去** ——
线上是静态导出，构建时没有后端地址，后台没有可连的服务端（见 7.1 节）。

| 想做的事 | 去哪 |
| --- | --- |
| 看今天谁上课、谁课时不够 | 今日概览 |
| 给学生建档、改课时、看 TA 的课 | 学生 → 点姓名展开 |
| 加老师、停用某位老师、看排课量 | 教师（「在职」可直接点开关） |
| 看教室今天满不满 | 教室 |
| 排课、改时间、标已上、取消 | 课程安排（保存前会报冲突） |
| 看这一周忙不忙、哪天有空档 | 日历 |
| 看数据在哪、备份有没有、谁改过什么 | 数据与备份 |

### 学生档案与课时记账

**信息采集表是「字段定义 + 键值对」**，不是几十个写死的输入框：

```
lib/backend/student-profile.ts   六节字段定义（基本与家庭 / 学业基础 / 学习习惯 /
                                 排课相关 / 心理与情绪健康 / 健康与特殊需求）
Student.profile                  键 → 值（文本 / 多选 / 表格）
```

- 表单与详情页共用同一份定义，因此不会出现「表单能填、详情页不显示」；
- **加字段不需要数据迁移**：老档案读不到新字段就是空值；
- 心理与情绪健康一节标了 `sensitive`，界面默认折叠并遮住内容 ——
  采集表写明这类内容「仅限指定人员查看」，界面至少要做到「不点开看不到」。

**课时按科目记账**（`Student.enrollments`），不再是学生身上的一个总数：

| 动作 | 服务方法 | 说明 |
| --- | --- | --- |
| 报课 | `students.enroll()` | 新开一条记录（科目 / 班型 / 课时 / 指定教师 / 日期） |
| 续费 | `students.renewEnrollment()` | 累加课时并留下流水，家长对账有据可查 |
| 退课 | `students.refundEnrollment()` | **标记退课并记日期，不删除** —— 否则课时去向说不清 |
| 上课扣课时 | `lessons.markCompleted()` | 按**该节课的科目**找对应报课记录扣减 |
| 手工调整 | `students.adjustEnrollmentLessons()` | 补录 / 纠错，同样留流水 |

扣课时找不到对应科目的报课记录时**不扣，并如实上报原因**
（`CompletionResult.skipped`）—— 排课与课时对不上时，这就是线索。

#### 课时流水（账本，v5）

只在报课记录上留一个 `usedLessons` 数字是不够的：家长问「上了 20 节为什么扣了 22 节」
答不出来，老师点错「标记已上」也无法补救。因此每一次课时变动都记一笔
`LessonTransaction`（+报课 / +续费 / -上课 / ±调整），并且**撤销是留痕**
（`reversedAt` 打时间戳）而不是删除。

- 上课扣减会记下 `lessonId`：能回答「这一节扣的是哪条报课」；
- **状态从「已上」改回去时按流水退回课时**（服务层在 `lessons.update()` 里处理）——
  之前只改状态不退课时，点错一次就白扣一节且没有痕迹；
- 退课时撤销的是「这节课对应的那些上课流水」，而不是简单减 1：
  同一节课被反复标记的边界情形下，按流水撤销才不会多退少退；
- **不变式（自检逐条校验）**：某条报课的 `usedLessons` 必须等于其有效「上课」
  流水合计，`totalLessons` 必须等于有效正向流水合计。种子数据与迁移都按这个
  口径生成，否则升级后对账数字会对不上。

`students.subjects`（列表里显示的在读科目）由报课记录推导，不单独维护，
避免「退了课但科目列表还挂着」。

### 动态追踪（课堂记录 / 作业记录 / 阶段测评）

采集表里的「动态追踪」是**多次记录**而不是档案字段，因此单独成三张表：

| 记录 | 挂在哪 | 入口 | 字段 |
| --- | --- | --- | --- |
| **课堂记录** | 课节（`lessonId` + `studentId`） | 课程安排 → 每节课的「课堂记录」 | 出勤 / 专注度 / 互动 / 状态评分 1–5 / 备注 |
| **作业记录** | 学生 | 学生详情 → 作业记录 | 日期 / 科目 / 提交情况 / 正确率 / 错题知识点 |
| **阶段测评** | 学生 | 学生详情 → 阶段测评 | 科目 / 分数 / **上次分数与趋势由服务算** / 薄弱知识点 |

两处刻意的服务端职责：

- **课堂记录用 upsert**（`lessonId + studentId` 唯一）：老师改完再保存必须更新同一条，
  而不是多出一条 —— 这是这类表单最容易写错的地方，自检专门有一条断言守住；
- **阶段测评的上次分数由服务带出**（新增时找同科目最近一条）：趋势是这条记录的核心
  信息，让页面自己找上一条，补录旧数据时顺序很容易对不上。

记录课堂表现**不影响课时**：扣课时只发生在「标记已上」，自检里也有一条断言
确认写记录不会改动课时余额。

### 课程库（`/admin/courses`，数据库 v12 起）

科目候选原先**只来自网站内容**（课程页卡片），于是想开一门网站上还没有的课（围棋、书法、
编程）只能手打进文本框，名字一歪（「围棋」/「围棋课」）课时与统计就对不上。
现在课程是数据：

- 网站课程在首次访问与「从网站同步」时自动进来（`origin: "网站"`，id 用卡片路径，
  因此重复同步不会重复添加）；机构自己加的课是 `origin: "后台"`，两者平起平坐 ——
  都能排课、能记课时、能挂到教师名下；
- **课程名是引用键**：排课科目、教师可带科目、报课科目都按名字引用，因此服务层强制
  「非空且唯一」（`validateCourse`）；两门都叫「数学」的课会让课时扣到哪一门说不清；
- 网站来源的课程**不能删**（删了下次同步又回来），不想再排就改成「暂未开放」——
  不给一个会自己复原的删除按钮；
- 同步**只增不改**：机构在后台改过的状态、班型、备注不会被一次同步冲掉；
- 边界：后台加课程**不会**让宣传网站上多出卡片（网站是静态内容，要改内容文件）。

课程候选项统一由 `api.courses.options()` 提供（网站课程 + 后台课程），
后台所有「选科目」的表单都用 `components/admin/useSubjectOptions.ts` 这个 hook：
先用内容里的课程立即渲染表单，挂载后再用课程库补齐 —— 服务层取不到时也是「少几门课」，
而不是「表单没法填」。

**与报价配置打通**（`lib/backend/pricing.ts` 的 `syncLibraryLinks` / `addLibraryCourseToPricing` /
`pricingStatusForCourses`）：报价配置里的课程可以带一个 `courseId` 指向课程库，于是

- 课程库改了名字 → 报价配置跟着改（否则家长看到的还是旧课名）；
- 课程库设为「暂未开放」→ 报价配置同步停用；重新开放则恢复；
- 课程库删掉某个后台新增的课程 → 报价配置里那一项**保留但置为暂未开放**（不静默消失，
  让机构自己决定去留）；
- 报价配置里与课程库**同名**的课程会自动补上关联（内容带过来的课程没有 courseId）。

同步放在**服务层**（`courses.create/update/remove/syncFromSite` 之后自动跑）而不是页面里：
改名发生在编辑课程的那一刻，页面可能根本没打开报价页 —— 依赖页面自觉调用一定会漏。

界面上：课程库每门课下面显示「报价 200 元/节（兴趣才艺）」或「未定价」，
报价页有「课程库课程定价」面板（多选课程 + 选阶段 / 新建阶段 + 填基础价），
给课程库的课定价（例如围棋 200 元/节）后，后台能给家长试算、教师课时费也算得出来，
导出内容替换 `data/site/pricing.md` 之后家长就能在报价页上选到它。

一个**真实的口径差异**（不要试图强行对齐）：报价配置里的课程名是「课程包」粒度
（九年级课本 / 雅思口语 / 小升初），课程库是「学科」粒度（初中数学 / 雅思 / 小学语文），
大多数名字本来就不一样 —— 因此自动认领只在同名时生效，其余靠显式定价。

教师「可带科目」用复选下拉（`components/admin/MultiSelect.tsx`）：候选有三十多门且会继续增加，
平铺复选框会把表单撑长，原生多选又要按住 Ctrl（老师第一次用基本都会选错）。

### 课表与占用（`/admin/timetable`）

排课之后最常被问的两件事，共用一个周视图、按页签切换：

- **教师课表**：某位老师这一周什么时候有课（哪天满、哪天空）；
- **教室占用**：某间教室这一周的占用与空档（接新学生时先找空档）。

底部还有一行「本周总览」：按教师或按教室列出本周课次与小时数，
用来回答「谁最忙」「哪间教室最紧」。

**空档卡片**（`lib/backend/timetable.ts`）：没排课、但还能排一节课的时间会显示成
虚线卡片，共三段 —— **课前**（上课时间开始 → 第一节课）、**课间**（两节课之间）、
**课后**（最后一节课 → 上课时间结束），每天栏头给出当天空档合计。
课表上写出来的是「谁什么时候上课」，而接新学生、安排补课、答应家长临时加课靠的
恰恰是**没写出来的那段空隙**，所以把它算出来显示。课表页与今日概览共用同一份计算。

口径都是刻意定的：

- **头尾按「上课时间」算**：窗口由 `options.ts` 的 `getClassHoursWindow()` 从内容的
  「上课时间」字段解析（默认 8:00–22:00），**不写死在代码里**；解析不出来就退回
  「只算课间空档」，宁可少显示也不猜错；课排在窗口之外时不产生空档；
- **已取消的课不算占用**（与统计口径一致）：取消后左右两段连成一整段；
- **用「到目前为止最晚的结束时间」往后比**：时间重叠时不会算出负数或假空档；
- **一节课都没有的那天不产生空档**：整天空着由页面空状态表达，否则会画出一张
  14 小时的卡片，与空状态重复。

纯函数，页面只负责按时间线渲染（`buildDayTimeline`），文案（`gapText`）也在模块里，
避免两个页面各写一套说法。

场地的三层信息：**用途**（上课用教室 / 自习室）、**容量**（自习室即座位数）、
**可用时段**（一周中哪几天 + 哪个时间段；**留空表示不限**）。

可用时段是一条可增删的「几天 + 一段」，覆盖「周一至周五 17:00–21:00」这种常见写法；
它不只是展示：排课时会检查这节课是否**完整落在**某行时段内，落在外面会提示
「所选教室在该时段不开放」并禁止保存。判定规则集中在 `lib/backend/availability.ts`：
没设时段＝不限、跨天行（结束早于开始）视为无效、星期必须匹配。

数据结构改动必须配迁移：`Classroom` 从 v1（只有名称/容量/备注）升到 v2 时，
`migrate()` 会给老数据补上 `kind` 与 `availability` 并落盘 —— 否则那份旧数据会缺字段，
界面上直接出现 undefined。自检里有一条用例专门灌入 v1 数据再读回来验证迁移。
（**历史**：这条要求的原话针对的是"**别人那份**旧数据会不会缺字段" ——
那时数据分散在每个使用者的本地存储里，"别人的那份"就是另一份库。
接后端之后库只有一份、位置也变了，但要求一点没变：要迁移的是**库里那份老数据**，
缺字段的下场同样是 undefined。）

排课的两个关键动作都放在服务层里，页面不自己算：

- **排课校验**（`lessons.findConflicts`）一次给出五类问题，页面逐条说明并禁止保存：

  | 校验 | 含义 |
  | --- | --- |
  | 教师 / 教室 / 学生冲突 | 时间重叠；**相邻时段不算冲突**、已取消的课不占时间、编辑自己不算冲突 |
  | `classroomClosed` | 落在教室可用时段之外 |
  | `overCapacity` | 学生数超过场地容量（8 人的教室不能排 10 个学生） |
  | `teacherSubjectMismatch` | 科目不在该教师的「可带科目」里（未登记科目时不报） |

  这里原先写的是"注意真实服务端**必须再校验一次** —— 客户端检查只是体验，不是保证"。
  **那条要求到现在仍然没有兑现，别以为接了后端就自动有了**：服务端跑的是同一份
  `api.ts`，所以校验逻辑确实在服务端手里，但**落库路径（`lessons.create` / `update`、
  安排补课）自己不复核**，仍然依赖表单在保存前先调一次 `findConflicts`。
  因此"检查只是体验"这句话今天依然成立 —— 它是接后端之后仍挂着的一条待办。
- **标记已上**（`lessons.markCompleted`）：状态改为「已上」并按课时扣减，
  **幂等** —— 重复点不会重复扣课时（这是同类系统最常见的数据事故）。

### 假登录：只是门，不是锁（**已迁移到服务端会话**）

> 这一节记录的是**当时**的形态与理由，保留下来是因为它解释了一个决策：
> 为什么那个阶段可以接受"口令写在前端"。**现在不要按它做。**
> 现状见下一节。历史内容：`admin` / 一个固定口令写在 `lib/auth/session.ts` 里，
> 静态站点没有服务端、仓库又是公开的，因此它只能挡住误点进来的访客；
> 当时数据本来也在访问者自己的浏览器里，所以风险有限。

### 登录：从假登录到服务端会话（现状）

数据搬进**服务端数据库**（钱、课时、家长联系方式）之后，"口令写在前端"就不再可接受了：
公开仓库里的口令等于没有口令。于是：

- 口令与校验都在服务端（`server/auth.mts`）：口令取自 `NEXGENEDU_ADMIN_PASSWORD`，
  否则**首次启动随机生成**并写入 `server/data/admin-credential.json`（0600），
  启动日志打印一次；**数据库里不存凭证** —— 否则"恢复三个月前的备份"会把口令一起回退；
- 前端只拿**令牌**（`lib/auth/token.ts`），`/api/call` 与老 REST 接口未登录一律 401；
- 鉴权是**结构性**的：`/api/` 下除 `/api/login`、`/api/logout`、`/api/session` 外
  一律要登录（一处闸门）。逐个接口加鉴权一定会漏 —— 第一版就漏了读接口；
- 操作日志的**操作人由会话决定**，前端说了不算（原先前端调 `setOperator`，
  而它经远端代理会静默变成 Promise，操作人一直是默认值）；
- 服务端默认只绑 `127.0.0.1`（`NEXGENEDU_HOST` 可改）：本机使用的系统没必要监听所有网卡。
  **历史**：早先是 `server.listen(PORT)`，那会监听所有网卡 —— 同一个 WiFi 下的任何设备
  都能连上；真要给别的设备用，得显式设 `NEXGENEDU_HOST=0.0.0.0`，那是一个有意识的决定，
  而不是默认行为；
- 令牌只放在**服务端内存**里（不落盘）：闲置有效期默认 12 小时
  （`NEXGENEDU_SESSION_MINUTES` 可配）、**每次使用都会续期**，
  并且**后端一重启就全部失效，需要重新登录**。这是刻意的取舍：
  把令牌持久化会立刻引入"过期与回收"那一整套逻辑，而本机使用下重新登录的成本很低；
- 由此 `lib/auth/session.ts` 的 `getSession` / `isLoggedIn` / `login` / `logout`
  **全部是异步的**：「已登录」现在只有服务端说了算（本地存着令牌不等于令牌还有效），
  同步返回一个本地状态会让人"看着进去了、点什么都失败"，比多等一次请求糟糕得多；
- **没有后端时不假装能登录**：线上静态站没有可连的服务端，登录直接不可用，
  界面明说原因与下一步（`loginUnavailableReason()` / `RequireAuth` 的「后台需要本机后端」），
  而不是退回"口令写在前端的假登录"或让人反复试口令 —— 那样只会制造
  "我在这儿录的数据去哪了"的困惑；
- 后台整棵子树仍包在 `components/admin/RequireAuth.tsx` 里、未登录不渲染界面，
  但那只是**体验层**的门；真正的边界在服务端。

### 管理后台数据变更方案（Phase 5 决策）

> 这是 Phase 5 时的决策记录，**已经不能用来理解今天的存储**：下面那张表是当时的形态。
> 保留它是因为"为什么当初要把数据分成两类"这个判断值得留下，但按它去理解现状会走错方向。

后台的排课、课程完成、课时扣减等「写操作」需要区分两类数据：

| 类别 | 来源 | 存储位置（**当时**） |
| --- | --- | --- |
| 基础档案：学生 / 教师 / 教室 | `data/*.md` | 只读，不修改 |
| 运行时数据：课程安排、课时流水 | 由数据层初始化 | 用户修改部分写入 `localStorage` |

- 原因：Markdown 在前端运行时不可写，后台若只读就无法演示排课；而基础档案无需在 UI 中编辑。
  （**这条已经过时**：现在学生、教师、教室都在后台里建档 / 编辑 / 删除，
  它们和排课、课时流水一样都是服务端库里的业务数据；`data/site/*.md` 只管**宣传网站**的内容。）
- 数据层对外暴露统一的 Provider 接口，`MarkdownDataProvider` 与（未来的）`PostgresDataProvider`
  实现同一套函数签名。（**设想没有这么落地**：接后端时没有 Provider、也没有换 PostgreSQL，
  而是给 `KeyValueStore` 加了一个 SQLite 实现 —— 见「教务后台的后端架构」。）
- 运行时数据通过 `createRuntimeStore(storageKey)` 工厂封装，为未来替换为 API 调用预留唯一改动点。
  （**同样没有按这个设想做**：这段代码里没有 `createRuntimeStore` 这个工厂。
  实际改动只有两处 —— 加一层代理（`lib/backend/remote.ts`）与加一个存储实现
  （`server/kv-store.mts`），业务代码与页面代码都没动。）
- **`localStorage` 仅为开发阶段的临时方案，不是数据库**：不承担并发、事务、多端一致性职责，
  校验逻辑（如排课冲突）必须在数据层重新执行一次，不能只依赖 UI。
  （**这条判断是对的，也已经兑现**：临时方案退役了，现在是服务端 SQLite。
  "校验不能只靠 UI"照旧成立 —— 而且比当时更值得警惕：服务端跑的是同一份实现、
  校验函数确实在服务端手里，但**落库路径自己不复核**，所以今天仍然只有页面那一次检查，
  详见后面「课表与占用」里排课校验那一条。）

### 后端开发方案（`docs/后端开发方案.md`）

接真实服务端的路线写在契约里（`contract.ts` + `docs/后台API约定.md`：方法清单、
服务端必须复核的那些校验、迁移六步），**落地方式**另有一份。这份方案的要点：

开发阶段用「本机 Node + SQLite 单文件」，不上 Docker —— 这台开发机的环境约束
（`~/.npm` 只读）让容器化收益为负，而 SQLite 单文件让备份=复制文件、迁移=搬文件。
业务规则是纯函数，服务端可以原样搬，不用重写。触发条件满足（多人并发 / 要常驻线上 /
要对外提供数据）时再换 Postgres 与容器，那一步**不需要改接口**。
数据库就是 **SQLite 单文件 + `better-sqlite3` 驱动**（WAL、一个请求一个事务、
`VACUUM INTO` 在线备份，即不停服务也能备份）；
服务端采用**路线 B**：给 `api.ts` 换一个 SQLite 支持的 `KeyValueStore`，再用
`POST /api/call { method, args }` 把方法暴露出去。

**这份方案已经落地**（当时的写法是"计划这么做"，现在是"就是这么做的"）：

- 服务端跑的就是 `lib/backend/api.ts` 这**同一份源码**，不是在服务端重写一遍 ——
  业务口径只有一份（代价与后续迁移见该文档 §5.3）；**历史**：这里原先说的是
  "服务端跑的就是**前端那一份** `api.ts`"，本意是同一份源码，但接后端之后前台走的是代理、
  本地实现只在没配后端时兜底，那句按字面读已经不准确，所以改成"同一份源码"；
- SQLite 存的是**整库一份 JSON 快照**（`kv` 表），不是按行存表：因此没有按行查询，
  并发写入以"整库替换"为单位；
- 备份与恢复也按方案落了地：每天自动一份、保留份数可配、恢复演练可重复执行
  （见「服务端备份」一节）。

**还没落地的一件事**（重要，别按已完成理解）：网站内容进库做**只读镜像**。
表已经建好了（`site_content`），但**同步脚本还没有写、表里是 0 行**；
网站内容的真源目前仍然只有 `data/site/*.md` 这一个。当初定的方向不变：
真源仍是内容文件，库里将来存的是只读镜像（单向、幂等、能看出过期），
以此保住免费静态托管与现有发布流程；将来若要改成"数据库做真源"，
只需把同步方向反过来，业务表与接口都不动。

## 9. 本机环境约束（重要）

开发机存在以下网络 / 权限限制，影响构建方式，**不是代码问题**：

1. **`~/.npm` 不可写**（沙箱限制在项目目录内）。
   → 所有 npm 命令使用项目本地 cache：`npm install --cache ./.npm-cache`。
   该目录已加入 `.gitignore`。
2. **Google Fonts 不可达**。
   → 不使用 `next/font/google`，改用系统字体栈，构建可完全离线完成。
3. **`github.com` 连接不稳定**（首次探测超时，推送时偶发 `Connection reset by peer`）。
   → 若 `git push` 失败，改用 HTTP/1.1 重试（已实测有效）：

   ```bash
   git -c http.version=HTTP/1.1 push origin main
   ```

## 10. Git

- 默认分支：`main`
- 远端：`origin` → `https://github.com/JeremyThierryChan/NexGenEdu.git`
- 首次提交已推送（`f7ed6fd`），本地 `main` 与 `origin/main` 一致。
- 后续每个 Phase 完成后 commit 并推送：

```bash
git push
```
